version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - errorlint

  settings:
    errcheck:
      check-type-assertions: true
