---
phase: 24-cpg-dedicated-skills-agent-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/cpg-operator.md
  - .claude/skills/cpg-triage/SKILL.md
  - .claude/skills/cpg-audit-onboard/SKILL.md
  - .claude/skills/cpg-policy-review/SKILL.md
  - .claude/skills/cpg-health-report/SKILL.md
  - .claude/skills/cpg-mcp-smoke/SKILL.md
  - README.md
autonomous: true
requirements: [SKL-01, SKL-02, SKL-03, SKL-04, SKL-05, SKL-06]
must_haves:
  truths:
    - "An operator invoking cpg-triage is routed through the live-session tool sequence (start_session -> get_status -> list_dropped_flows -> list_policies/get_policy/get_evidence per workload -> stop_session -> get_cluster_health) and delegates session-driving to the cpg-operator agent (SKL-01/SKL-06)"
    - "cpg-audit-onboard GUIDES the human through cpg bootstrap and cpg audit-window (CLI, Variant B), DRIVES capture via start_session with audit inclusion, and ends the enforce checklist with the HUMAN applying policies via kubectl apply — never an apply tool (SKL-02)"
    - "cpg-policy-review routes an offline CNP audit through cpg explain + get_evidence, pointing at README #l7-prerequisites / #explain-policies rather than restating them (SKL-03)"
    - "cpg-health-report turns cluster-health.json into a self-contained HTML infra-drop report by node/workload with Cilium remediation links, HTML-escaping every cluster-health.json string field (SKL-04)"
    - "cpg-mcp-smoke backtick-mentions all 9 registered tools (coverage floor) and routes to TestMCPE2EGracefulLifecycle run WITHOUT -short (SKL-05)"
    - "cpg-operator agent exists with name/description/tools frontmatter, tools limited to Bash, Read, and a description stating it is spawned by cpg-triage and cpg-audit-onboard (SKL-06)"
    - "README has an ## Agent tooling section listing all 5 skills"
  artifacts:
    - path: ".claude/agents/cpg-operator.md"
      provides: "SKL-06 single repo-local session-driving agent"
      contains: "cpg-triage"
    - path: ".claude/skills/cpg-triage/SKILL.md"
      provides: "SKL-01 live-session triage router"
      contains: "start_session"
    - path: ".claude/skills/cpg-audit-onboard/SKILL.md"
      provides: "SKL-02 onboarding router (guides CLI, drives capture, human applies)"
      contains: "cpg audit-window"
    - path: ".claude/skills/cpg-policy-review/SKILL.md"
      provides: "SKL-03 offline CNP audit router"
      contains: "cpg explain"
    - path: ".claude/skills/cpg-health-report/SKILL.md"
      provides: "SKL-04 cluster-health.json -> HTML report router"
      contains: "cluster-health.json"
    - path: ".claude/skills/cpg-mcp-smoke/SKILL.md"
      provides: "SKL-05 post-release smoke router (all 9 tools + e2e lifecycle)"
      contains: "get_bootstrap_policy"
    - path: "README.md"
      provides: "## Agent tooling section listing all 5 skills"
      contains: "## Agent tooling"
  key_links:
    - from: ".claude/skills/cpg-triage/SKILL.md"
      to: ".claude/agents/cpg-operator.md"
      via: "delegation reference"
      pattern: "cpg-operator"
    - from: ".claude/skills/cpg-mcp-smoke/SKILL.md"
      to: "TestMCPE2EGracefulLifecycle"
      via: "smoke routing target run without -short"
      pattern: "TestMCPE2EGracefulLifecycle"
---

<objective>
Author the six repo-local markdown artifacts of Phase 24 — five `.claude/skills/cpg-*/SKILL.md` workflow routers plus the single `.claude/agents/cpg-operator.md` subagent — and add the README "## Agent tooling" section. Every skill is a ROUTER: it names workflow steps and backtick-quoted tool names ONLY, never argument schemas or result shapes; live semantics are discovered at invocation time via `tools/list`. This plan produces the artifacts the Phase 24 consistency tripwire (plan 24-02, wave 2) enforces.

Purpose: give an LLM operator cpg-specific, genuinely useful onboarding/triage/review routing instead of generic prompting against raw tool schemas — with no third, drifting copy of tool semantics (SKL success criterion 5).

Output: `.claude/agents/cpg-operator.md`, `.claude/skills/cpg-{triage,audit-onboard,policy-review,health-report,mcp-smoke}/SKILL.md`, README "## Agent tooling" section.

NOTE: This plan writes ONLY markdown — no Go code, no `go.mod` change. The tripwire test that mechanically enforces these files lives in plan 24-02 (wave 2). Author every tool name backtick-quoted (e.g. `start_session`) so the wave-2 regex can see it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cpg-dedicated-skills-agent-tooling/24-RESEARCH.md
@.planning/phases/24-cpg-dedicated-skills-agent-tooling/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: cpg-operator agent + cpg-triage skill (the live-session pair)</name>
  <files>.claude/agents/cpg-operator.md, .claude/skills/cpg-triage/SKILL.md</files>
  <read_first>
    .claude/skills/desloppify/SKILL.md   # this repo's own skill: exact frontmatter shape (name + description only, no tools:), with positive "Use when" + negative "Do NOT" triggers
    .planning/phases/24-cpg-dedicated-skills-agent-tooling/24-RESEARCH.md  # Pattern 1 (SKILL.md frontmatter), Pattern 2 (agent frontmatter: name/description/tools), Pattern 3 (router-only refs), "MCP Tool Reference" (9 tools + one-line purposes), "cpg-triage -> live MCP session tools, in this order", Pitfall 2 (get_cluster_health after stop_session)
  </read_first>
  <action>Create `.claude/agents/cpg-operator.md` (SKL-06). Frontmatter: `name: cpg-operator`; `description:` stating it drives a live cpg MCP session lifecycle on behalf of `cpg-triage` and `cpg-audit-onboard` and is NEVER invoked directly — spawned by those two skills when a session must be driven (this phrasing keeps Claude Code's agent-selection from invoking it standalone); `tools: Bash, Read` (Bash to spawn `cpg mcp` and drive JSON-RPC over stdio; Read to inspect generated policy YAML / evidence the session writes to its tmpdir). Do NOT add `Write` or `Edit` — the agent drives and reports, never authors repo files (SEC-01 readonly discipline). Body: a short operating contract — always discover the exact tool surface via `tools/list` before calling any tool (never assume argument names from prose); drive the session, surface findings, stop cleanly. Create `.claude/skills/cpg-triage/SKILL.md` (SKL-01). Two-field frontmatter (`name: cpg-triage`, `description:` with positive trigger — "turn live cluster traffic into reviewable network policy right now" — and negative triggers pointing to `cpg-policy-review` for offline review and `cpg-audit-onboard` for new-namespace onboarding). Body routes the tested live-session sequence: delegate driving to the `cpg-operator` agent, then `start_session` -> poll `get_status` -> `list_dropped_flows` (classify: policy-actionable samples vs infra/transient/noise aggregates) -> per policy-actionable workload `list_policies` then `get_policy` paired with `get_evidence` -> `stop_session` -> `get_cluster_health` for the infra side. Include the Pitfall 2 decision point: call `stop_session` BEFORE expecting a full `get_cluster_health` report (a mid-capture "not ready" marker is not an error). Backtick-quote every tool name. NEVER restate any tool's argument schema or result shape (Router Principle) — discover live via `tools/list`. Commit: `feat(24): add cpg-operator agent + cpg-triage skill` (no Co-Authored-By).</action>
  <verify>
    <automated>test -f .claude/agents/cpg-operator.md && test -f .claude/skills/cpg-triage/SKILL.md && rtk proxy grep -q 'cpg-operator' .claude/skills/cpg-triage/SKILL.md && rtk proxy grep -q 'start_session' .claude/skills/cpg-triage/SKILL.md && rtk proxy grep -qE '^tools: *Bash, *Read' .claude/agents/cpg-operator.md && ! rtk proxy grep -qiE '^tools:.*(Write|Edit)' .claude/agents/cpg-operator.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - cpg-operator frontmatter has name/description/tools; tools == Bash, Read (no Write/Edit); description names both parent skills.
    - cpg-triage routes the full live-session sequence, delegates to cpg-operator, backtick-quotes tool names, restates no schema, notes stop-before-health.
  </acceptance_criteria>
  <done>Both files exist; verify command prints OK; every referenced tool name is one of the real 9 (start_session/get_status/stop_session/list_dropped_flows/list_policies/get_policy/get_evidence/get_cluster_health).</done>
</task>

<task type="auto">
  <name>Task 2: cpg-audit-onboard + cpg-policy-review skills</name>
  <files>.claude/skills/cpg-audit-onboard/SKILL.md, .claude/skills/cpg-policy-review/SKILL.md</files>
  <read_first>
    docs/bootstrap-runbook.md   # the 10-section onboarding flow cpg-audit-onboard ROUTES to: Prerequisites -> Bootstrap (cpg bootstrap | kubectl apply) -> Enable Per-Endpoint Audit Mode (cpg audit-window -n <ns> --ttl <dur>) -> Observe verdicts -> Capture (audit inclusion) -> Create and Apply Generated Policies (kubectl apply, the HUMAN step) -> Disable (automatic on audit-window exit) -> Verify -> Clean-up. Note the leading warning block confines the hyphenated policy-audit-mode token.
    cmd/cpg/explain.go          # cpg explain flag surface (NAMESPACE/WORKLOAD | path; --ingress/--egress/--port/--peer/--peer-cidr/--http-method/--http-path/--dns-pattern/--since/--samples-limit; --json/--format) — reference the command, do not restate every flag as schema
    .planning/phases/24-cpg-dedicated-skills-agent-tooling/24-RESEARCH.md  # "cpg-audit-onboard -> docs/bootstrap-runbook.md", "cpg-policy-review -> cpg explain + evidence", Pitfall 1 (policy-audit-mode token confinement — this file is NOT pinned but keep operational hygiene), Anti-Patterns
  </read_first>
  <action>Create `.claude/skills/cpg-audit-onboard/SKILL.md` (SKL-02). Frontmatter name/description (trigger: onboarding a brand-new namespace into enforced network policy; negative: not for triaging an already-running capture -> cpg-triage). Body GUIDES the human through the CLI steps that are human-run per the Variant B gate decision — `cpg bootstrap -n <ns>` (pipe to `kubectl apply -f -`) and `cpg audit-window -n <ns> --ttl <dur>` (foreground/supervised; auto-reverts on exit) — the skill instructs, it never runs these itself. It DRIVES the capture by delegating to the `cpg-operator` agent to call `start_session` with audit capture enabled (discover the exact argument name via `tools/list` — do NOT hardcode the arg schema; router-only). The enforce checklist ENDS with the HUMAN applying policies via `kubectl apply -f ./policies/<namespace>/` — there is NO apply tool and none must be invented (out-of-scope). Route to the runbook sections rather than duplicating them. When you must mention the daemon-wide setting, describe it as "the daemon-wide audit-mode setting"; use the per-endpoint form `PolicyAuditMode` (no hyphen) freely — do not introduce the bare hyphenated `policy-audit-mode` token. Create `.claude/skills/cpg-policy-review/SKILL.md` (SKL-03). Frontmatter name/description (trigger: audit already-generated CNPs offline for over-broad rules / L7 anchoring / missing DNS-53 companions / dedup sanity; negative: not for driving a live session -> cpg-triage). Body routes an offline checklist through `cpg explain <NAMESPACE/WORKLOAD | path>` (name the command + its filter/output flags at a routing level, not as a restated schema) paired with the `get_evidence` MCP tool for per-rule flow evidence, and points at README's "Explain policies" section and `README.md#l7-prerequisites` for the L7 anchoring rules rather than re-explaining them. Backtick-quote all tool names; restate no schemas. Commit: `feat(24): add cpg-audit-onboard + cpg-policy-review skills` (no Co-Authored-By).</action>
  <verify>
    <automated>test -f .claude/skills/cpg-audit-onboard/SKILL.md && test -f .claude/skills/cpg-policy-review/SKILL.md && rtk proxy grep -q 'cpg audit-window' .claude/skills/cpg-audit-onboard/SKILL.md && rtk proxy grep -q 'kubectl apply' .claude/skills/cpg-audit-onboard/SKILL.md && rtk proxy grep -q 'cpg explain' .claude/skills/cpg-policy-review/SKILL.md && rtk proxy grep -q 'get_evidence' .claude/skills/cpg-policy-review/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - audit-onboard guides cpg bootstrap + cpg audit-window (never runs them), drives capture via cpg-operator/start_session, ends with the human kubectl-applying — no apply tool referenced.
    - policy-review routes cpg explain + get_evidence and points at README #l7-prerequisites; no schema restatement in either file.
  </acceptance_criteria>
  <done>Both files exist; verify prints OK; audit-onboard mentions no apply tool and no bare hyphenated policy-audit-mode token in body prose.</done>
</task>

<task type="auto">
  <name>Task 3: cpg-health-report + cpg-mcp-smoke skills + README "## Agent tooling" section</name>
  <files>.claude/skills/cpg-health-report/SKILL.md, .claude/skills/cpg-mcp-smoke/SKILL.md, README.md</files>
  <read_first>
    pkg/hubble/health_writer.go   # ClusterHealthReport / HealthSession / HealthDropJSON field set (schema_version, session{started,ended,flows_seen,infra_drops_total}, drops[]{reason,class,count,remediation,by_node,by_workload}) — the field set the HTML report reflects
    README.md                     # lines ~557 (## MCP Server (cpg mcp)) and ~608 (## Label selection): insert "## Agent tooling" between them; mirror the existing MCP tool table's terse | col | col | style
    .planning/phases/24-cpg-dedicated-skills-agent-tooling/24-RESEARCH.md  # "cluster-health.json Schema" + rendering note (rows per drop, node/workload breakdown, remediation links, escape string fields), "Fake Relay E2E Harness" (TestMCPE2EGracefulLifecycle, run WITHOUT -short, one-time -race build), Pitfall 3 (short-mode skip), "MCP Tool Reference" (all 9 names for the smoke coverage floor), "README MCP tool table style to extend"
  </read_first>
  <action>Create `.claude/skills/cpg-health-report/SKILL.md` (SKL-04). Frontmatter name/description (trigger: turn a captured cluster-health.json into a shareable HTML infra-drop report; negative: not for generating or reviewing CiliumNetworkPolicies). Body routes: obtain `cluster-health.json` either directly from disk at `<session_tmpdir>/cluster-health.json` (post-`stop_session`, uncapped) or via the `get_cluster_health` MCP tool (server-capped at 100 node/workload entries) — instruct the skill to STATE which path it used. Render a self-contained, inline-styled HTML file (no external JS/CSS) with a header line from session flows_seen / infra_drops_total, one table row per drop entry (reason, class, count), an expandable by-node / by-workload breakdown, and a clickable Cilium remediation link wherever the drop's remediation field is non-empty. Instruct HTML-ESCAPING every cluster-health.json string field before embedding (self-XSS guardrail, V5). Describe the field set at a routing level (where to find it, what to render) — this is workflow guidance, not schema restatement. Create `.claude/skills/cpg-mcp-smoke/SKILL.md` (SKL-05). Frontmatter name/description (trigger: post-release smoke of a tagged cpg binary against the fake relay; negative: not a live-cluster workflow). Body routes to the EXISTING e2e harness — run `go test ./cmd/cpg/... -run TestMCPE2EGracefulLifecycle -v` (via `rtk proxy go test ...` in this repo) explicitly WITHOUT `-short` (Pitfall 3: the test t.Skips under short mode; a sub-second "pass" means it was skipped), and note the one-time `-race` binary build cost. Mention `TestMCPE2EUngracefulDisconnect` as the abrupt-disconnect companion scenario. To satisfy the coverage floor, the smoke skill MUST backtick-mention EVERY one of the 9 registered tools it validates in the handshake: `start_session`, `get_status`, `stop_session`, `list_dropped_flows`, `list_policies`, `get_policy`, `get_evidence`, `get_cluster_health`, `get_bootstrap_policy` — and assert the handshake tool count is 9 (the ROADMAP "8-tool" text predates Phase 22's get_bootstrap_policy). Edit `README.md`: insert a "## Agent tooling" section between "## MCP Server (cpg mcp)" and "## Label selection", with a terse `| Skill | Purpose |` table row for each of the 5 skills (cpg-triage, cpg-audit-onboard, cpg-policy-review, cpg-health-report, cpg-mcp-smoke) plus a line for the `cpg-operator` agent, mirroring the MCP tool table style and restating no schemas. Commit: `feat(24): add cpg-health-report + cpg-mcp-smoke skills + README agent tooling` (no Co-Authored-By).</action>
  <verify>
    <automated>test -f .claude/skills/cpg-health-report/SKILL.md && test -f .claude/skills/cpg-mcp-smoke/SKILL.md && rtk proxy grep -q 'TestMCPE2EGracefulLifecycle' .claude/skills/cpg-mcp-smoke/SKILL.md && rtk proxy grep -q 'get_bootstrap_policy' .claude/skills/cpg-mcp-smoke/SKILL.md && rtk proxy grep -q '## Agent tooling' README.md && for t in start_session get_status stop_session list_dropped_flows list_policies get_policy get_evidence get_cluster_health get_bootstrap_policy; do rtk proxy grep -q "$t" .claude/skills/cpg-mcp-smoke/SKILL.md || { echo "MISSING $t"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - health-report routes cluster-health.json -> escaped inline-styled HTML with node/workload breakdown + remediation links, and states which source path it used.
    - mcp-smoke routes to TestMCPE2EGracefulLifecycle WITHOUT -short, asserts count 9, and mentions all 9 tool names (coverage floor).
    - README has a "## Agent tooling" section listing all 5 skills + cpg-operator, between the MCP and Label sections.
  </acceptance_criteria>
  <done>All three files present; verify prints OK; smoke skill mentions all 9 tools; README section lists all 5 skills.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill/agent prose vs real MCP tool surface | markdown routers are an LLM-facing copy of tool NAMES; drift (phantom/uncovered names) misleads the operator — the wave-2 tripwire is the enforcing boundary |
| generated HTML vs operator's browser | cpg-health-report emits a local HTML file an operator opens; unescaped cluster-health.json fields would be a self-XSS surface |
| cpg-operator agent tool allowlist vs cpg mcp readonly posture | the agent must not gain a write verb the server itself lacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering (of the LLM's operating context) | skill/agent prose drifting from the real 9-tool surface | mitigate | Router Principle authoring (names only, no schemas) here; mechanically enforced by TestSkillsConsistencyTripwire in plan 24-02 |
| T-24-02 | Elevation of Privilege | cpg-operator tools allowlist scope creep to Write/Edit | mitigate | allowlist pinned to Bash, Read only; verify asserts no Write/Edit in frontmatter |
| T-24-03 | Tampering (self-XSS in locally-opened HTML) | cpg-health-report echoing cluster-health.json string fields unescaped | mitigate | skill instructs HTML-escaping every string field before embedding (closed-vocabulary source keeps severity low) |
| T-24-04 | Repudiation / unsafe-guidance | cpg-audit-onboard implying an apply tool or a daemon-wide policy-audit-mode | mitigate | enforce checklist ends with the human kubectl-applying (no apply tool); per-endpoint PolicyAuditMode only, no bare hyphenated token in body |
| T-24-SC | Tampering | npm/pip/cargo/go module installs | accept | markdown-only plan; zero new packages; `git diff go.mod` empty |
</threat_model>

<verification>
- All six artifacts + README section exist (the three per-task verify commands above print OK).
- No phantom tool names: every backtick-quoted `start_/get_/stop_/list_` token across the six files is one of the real 9 — mechanically proven by plan 24-02's TestSkillsConsistencyTripwire.
- `git diff --exit-code go.mod go.sum` clean (no dependency change this plan).
</verification>

<success_criteria>
- SKL-01..06 artifacts authored as workflow routers (names only, no schema restatement).
- cpg-mcp-smoke covers all 9 tools + routes to TestMCPE2EGracefulLifecycle without -short.
- cpg-audit-onboard guides CLI (never runs) + ends with the human applying; no apply tool.
- README "## Agent tooling" section lists all 5 skills.
</success_criteria>

<output>
Create `.planning/phases/24-cpg-dedicated-skills-agent-tooling/24-01-SUMMARY.md` when done.
</output>
