---
phase: 24-cpg-dedicated-skills-agent-tooling
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - cmd/cpg/skills_test.go
autonomous: true
requirements: [SKL-01, SKL-02, SKL-03, SKL-04, SKL-05, SKL-06]
must_haves:
  truths:
    - "TestSkillsConsistencyTripwire enumerates the live 9-tool registry via startInMemoryMCPSession (same in-memory round-trip mcp_harness_test.go/mcp_e2e_test.go use) — never a hardcoded literal list"
    - "The tripwire fails on any phantom tool name referenced in a .claude/skills/cpg-*/SKILL.md or .claude/agents/cpg-operator.md that is not in the live registry"
    - "The tripwire fails unless cpg-mcp-smoke/SKILL.md mentions every registered tool (coverage floor)"
    - "The tripwire pins the tool COUNT at 9 so adding a 10th tool forces a skill sweep"
    - "The tripwire pins the README '## Agent tooling' section + one assertion per skill name"
    - "The full race suite is green, including TestMCPE2EGracefulLifecycle run WITHOUT -short (the actual SKL-05 smoke target)"
  artifacts:
    - path: "cmd/cpg/skills_test.go"
      provides: "TestSkillsConsistencyTripwire: phantom-check + coverage-floor + count-pin + README pins"
      min_lines: 60
      exports: ["TestSkillsConsistencyTripwire"]
  key_links:
    - from: "cmd/cpg/skills_test.go"
      to: "startInMemoryMCPSession (mcp_harness_test.go)"
      via: "in-memory tools/list enumeration"
      pattern: "startInMemoryMCPSession"
    - from: "cmd/cpg/skills_test.go"
      to: ".claude/skills/cpg-*/SKILL.md + .claude/agents/cpg-operator.md + README.md"
      via: "../../ relative-path reads + regexp/strings.Contains pins"
      pattern: "\\.\\./\\.\\./\\.claude/skills"
---

<objective>
Build the Phase 24 consistency tripwire (SKL success criterion 5): one new Go test file `cmd/cpg/skills_test.go` containing `TestSkillsConsistencyTripwire`. It enumerates the exact MCP tool registry the server registers — live, via the in-memory harness already used by `mcp_harness_test.go`/`mcp_e2e_test.go`, never a hardcoded list — and cross-checks it against the six markdown artifacts authored in plan 24-01: (1) no phantom tool names, (2) `cpg-mcp-smoke` covers every registered tool, (3) the tool count is pinned at 9, (4) README has the "## Agent tooling" section listing all 5 skills. Then it gates the full race suite green, including the pre-existing `TestMCPE2EGracefulLifecycle` (the actual SKL-05 smoke target, run without `-short`).

Purpose: only a compiled, CI-run test can fail a build — markdown alone cannot self-enforce. This test is the mitigation for the exact drift the phase targets (a future tool added without a skill sweep).

Output: `cmd/cpg/skills_test.go`.

NOTE: This plan depends on 24-01 — the tripwire cannot pass until all six artifacts + the README section exist with correctly backtick-quoted tool names. Zero new test infrastructure and zero new dependencies: reuse `startInMemoryMCPSession` (same package `main`), stdlib `os`/`path/filepath`/`regexp`/`strings`/`testing`, and already-vendored `go-sdk` + `testify`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cpg-dedicated-skills-agent-tooling/24-RESEARCH.md
@.planning/phases/24-cpg-dedicated-skills-agent-tooling/24-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TestSkillsConsistencyTripwire (phantom-check + coverage-floor + count-pin + README pins)</name>
  <files>cmd/cpg/skills_test.go</files>
  <behavior>
    - Registry enumeration: liveToolRegistry(t) returns exactly the 9 tool names by connecting an in-memory client and calling ListTools — asserted len == 9 (const wantToolCount = 9) with a message telling the reader to sweep the skills before bumping the pin.
    - Phantom check: every backtick-quoted verb-prefixed token ((?:start|get|stop|list)_[a-z_]+) found in any cpg-*/SKILL.md or cpg-operator.md must exist in the registry; a made-up name (e.g. `get_nonexistent`) would fail with a message naming the file + phantom token.
    - Coverage floor: every registered tool name must appear (backtick-quoted) in cpg-mcp-smoke/SKILL.md; a missing one fails naming the tool.
    - README pins: README.md contains "## Agent tooling" and each of the 5 skill names (cpg-triage, cpg-audit-onboard, cpg-policy-review, cpg-health-report, cpg-mcp-smoke).
    - Path resolution: skills/agent/README read via ../../ relative paths (test cwd is cmd/cpg/), require.NotEmpty on the skills glob so a mislocated dir fails loudly rather than vacuously passing.
  </behavior>
  <read_first>
    cmd/cpg/mcp_harness_test.go   # startInMemoryMCPSession helper (~l.16-35) — reuse verbatim (same package main, no export needed); the mcp.NewClient/Connect/ListTools round-trip
    cmd/cpg/readme_compat_test.go # golden-pin convention: os.ReadFile("../../README.md") + strings.Contains + testify require/assert; confirms ../../ reaches repo root and the cwd assumption
    cmd/cpg/mcp_e2e_test.go       # line ~412: require.Len(toolsResult.Tools, 9, ...) — the same count this tripwire pins; confirms the 9-name set
    .planning/phases/24-cpg-dedicated-skills-agent-tooling/24-RESEARCH.md  # "Consistency Tripwire Design" (test sketch + liveToolRegistry + toolNameToken regex), Pitfall 4 (verb-prefix regex tradeoff, count-pin backstop), "README pin"
  </read_first>
  <action>Create `cmd/cpg/skills_test.go` (package main). Add `const wantToolCount = 9` with a comment: bump ONLY alongside a full `.claude/skills/` + `cpg-operator.md` sweep for the new tool. Add `var toolNameToken = regexp.MustCompile("`+"`"+`((?:start|get|stop|list)_[a-z_]+)`+"`"+`")` (matches backtick-quoted, verb-prefixed snake_case — the load-bearing convention all 9 current tools follow; deliberately narrow to exclude arg-name prose like all_namespaces). Implement `liveToolRegistry(t *testing.T) map[string]bool` reusing `startInMemoryMCPSession` (context.WithCancel + drain defers), `mcp.NewClient` + `Connect` + `cs.ListTools`, collecting `tool.Name` into a set — never a hardcoded literal list. Implement `TestSkillsConsistencyTripwire`: (a) `require.Len(registry, wantToolCount, ...)` with the sweep-before-bump message; (b) glob `../../.claude/skills/cpg-*/SKILL.md`, `require.NoError` + `require.NotEmpty`, append `../../.claude/agents/cpg-operator.md`; (c) for each path, `os.ReadFile`, run `toolNameToken.FindAllStringSubmatch`, `assert.True(registry[name], "%s references phantom tool %q ...", path, name)`, and when the path contains `cpg-mcp-smoke` record the name in a `coveredBySmoke` set; (d) for every registry name, `assert.True(coveredBySmoke[name], "cpg-mcp-smoke must mention every registered tool; missing %q", name)`; (e) read `../../README.md` and `assert.Contains` for `"## Agent tooling"` plus each of the 5 skill names. Use a small `os.ReadFile` + `require.NoError` inline helper (matching sibling golden-pin tests). Do NOT add this to `readme_compat_test.go` (keep that file scoped to COMPAT). No `go.mod` change. Commit: `test(24): add skills/agent consistency tripwire` (no Co-Authored-By).</action>
  <verify>
    <automated>rtk proxy go test ./cmd/cpg/... -run TestSkillsConsistencyTripwire -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestSkillsConsistencyTripwire passes against the 24-01 artifacts: registry len 9, no phantom tools, smoke covers all 9, README section + 5 skill names present.
    - Registry is enumerated live via startInMemoryMCPSession — no hardcoded tool-name list in the test.
    - go.mod / go.sum unchanged.
  </acceptance_criteria>
  <done>Named test green; `git diff --exit-code go.mod go.sum` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Full race-suite gate incl. the SKL-05 e2e smoke target (no -short)</name>
  <files>cmd/cpg/skills_test.go</files>
  <read_first>
    .planning/phases/24-cpg-dedicated-skills-agent-tooling/24-VALIDATION.md  # full-suite command (rtk proxy go test ./... -count=1 -race), phase-gate sampling rate, e2e-without-short note
    .planning/phases/24-cpg-dedicated-skills-agent-tooling/24-RESEARCH.md    # "Fake Relay E2E Harness": TestMCPE2EGracefulLifecycle skips under -short; one-time -race build; this IS the smoke skill's routed-to target, so it must be green here
  </read_first>
  <action>No new production code — this is the phase gate that proves the tripwire and the SKL-05 smoke target both pass in the full suite (sandbox denies `go` under `make`, so invoke `go test` directly via `rtk proxy`, never `make test`; run WITHOUT `-short` so the e2e lifecycle test does not silently skip — Pitfall 3). Run the full cmd/cpg package with race + the tripwire + the e2e smoke target explicitly, confirm all green, and confirm no dependency drift. If any failure surfaces a real phantom/coverage/count mismatch, that is the tripwire correctly firing — fix the offending 24-01 artifact (re-open plan 24-01 scope), do not weaken the test. No commit unless a fix is needed (this task's product is a green gate, not a code change).</action>
  <verify>
    <automated>rtk proxy go test ./cmd/cpg/... -run 'TestSkillsConsistencyTripwire|TestMCPE2EGracefulLifecycle' -race -count=1 -v -timeout 300s && rtk proxy go test ./cmd/cpg/... -count=1 -race -timeout 300s && git diff --exit-code go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - TestSkillsConsistencyTripwire and TestMCPE2EGracefulLifecycle both green under -race (no -short).
    - Full cmd/cpg package green; go.mod/go.sum unchanged.
  </acceptance_criteria>
  <done>Both targeted tests + the full cmd/cpg race suite pass; go.mod/go.sum diff empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown routers vs compiled registry | the tripwire is the only place that can fail a build on tool/skill drift — it reads developer-authored local markdown (not untrusted input) and the live in-memory registry |
| test regex coverage vs future tool names | the verb-prefix regex only matches start_/get_/stop_/list_ names; a non-conforming future name could bypass phantom/coverage checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | tripwire regex silently missing a future tool whose name lacks a start_/get_/stop_/list_ prefix | accept | documented tradeoff (RESEARCH Pitfall 4); the wantToolCount==9 pin is the independent backstop — any count change fails the test and forces a human to look at mcp.go's composition root and widen the prefix list if needed |
| T-24-06 | Tampering | count-pin bumped to N without a corresponding skill sweep | mitigate | the coverage-floor assertion fails until cpg-mcp-smoke mentions the new tool, so bumping the constant alone cannot make the suite green |
| T-24-07 | Tampering (false smoke pass) | e2e smoke silently skipped under -short | mitigate | gate runs the e2e target explicitly without -short; a sub-second pass is treated as a skip, not a pass |
| T-24-SC | Tampering | go module installs | accept | test-only change; stdlib + already-vendored go-sdk/testify; `git diff go.mod` empty |
</threat_model>

<verification>
- `rtk proxy go test ./cmd/cpg/... -run TestSkillsConsistencyTripwire -v -count=1` green.
- `rtk proxy go test ./cmd/cpg/... -count=1 -race -timeout 300s` green (includes TestMCPE2EGracefulLifecycle without -short).
- Registry enumerated live (no hardcoded name list); count pinned at 9; coverage floor + phantom + README pins all assert.
- `git diff --exit-code go.mod go.sum` clean.
</verification>

<success_criteria>
- SKL success criterion 5 satisfied: an automated consistency tripwire ties the skills/agent/README prose to the Go tool registry — phantom-check, coverage floor, count pin, README section.
- The SKL-05 smoke routing target (TestMCPE2EGracefulLifecycle) is proven green without -short in the same gate.
</success_criteria>

<output>
Create `.planning/phases/24-cpg-dedicated-skills-agent-tooling/24-02-SUMMARY.md` when done.
</output>
