# Requirements Archive — v1.1 Offline Replay & Policy Analysis

**Archived:** 2026-04-24
**Status:** ✅ All v1.1 requirements validated

Spec: `docs/superpowers/specs/2026-04-24-offline-replay-and-analysis-design.md`
Plan: `docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md`

## Offline Replay

- [x] **OFFL-01**: Tool ingests Hubble jsonpb dumps (`hubble observe --output jsonpb`) through a new `cpg replay <file>` subcommand — Phase 4 + Phase 6
- [x] **OFFL-02**: Replay supports stdin (`-`) and transparent gzip (`.gz`) — Phase 4
- [x] **OFFL-03**: Non-DROPPED verdicts and malformed lines are skipped with counters surfaced in the session summary — Phase 4

## Per-rule Evidence

- [x] **EVID-01**: Every rule emitted by `generate` / `replay` is attributed to its contributing flows (samples + counters + first/last seen) on disk — Phase 4 + Phase 5
- [x] **EVID-02**: Evidence lives under `$XDG_CACHE_HOME/cpg/evidence`, keyed by a hash of the output directory; `--evidence-dir` overrides the location — Phase 4
- [x] **EVID-03**: Evidence is bounded by `--evidence-samples` (per rule) and `--evidence-sessions` (per policy); FIFO by time — Phase 4 + Phase 6 (wiring fix)
- [x] **EVID-04**: Evidence merges across sessions mirror the merge semantics of the policy YAML (preserve rules not re-emitted in the current session) — Phase 4 + Phase 5 (session upsert by ID)

## Explain

- [x] **EXPL-01**: `cpg explain <NAMESPACE/WORKLOAD | path/to/policy.yaml>` reads the evidence file and renders per-rule flow attribution — Phase 6
- [x] **EXPL-02**: Filters: `--ingress` / `--egress`, `--port`, `--peer KEY=VAL`, `--peer-cidr`, `--since` — Phase 6
- [x] **EXPL-03**: Output formats: text (default, ANSI on TTY), `--json`, and `--format yaml` — Phase 6

## Dry-run

- [x] **DRYR-01**: `--dry-run` on `generate` and `replay` suppresses filesystem writes (policies and evidence) while running the full pipeline — Phase 5
- [x] **DRYR-02**: `--dry-run` prints a unified YAML diff against existing files on disk; `--no-diff` disables just the diff — Phase 5

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| OFFL-01 | Phase 4, 6 | ✓ Complete |
| OFFL-02 | Phase 4 | ✓ Complete |
| OFFL-03 | Phase 4 | ✓ Complete |
| EVID-01 | Phase 4, 5 | ✓ Complete |
| EVID-02 | Phase 4 | ✓ Complete |
| EVID-03 | Phase 4, 6 | ✓ Complete |
| EVID-04 | Phase 4, 5 | ✓ Complete |
| EXPL-01 | Phase 6 | ✓ Complete |
| EXPL-02 | Phase 6 | ✓ Complete |
| EXPL-03 | Phase 6 | ✓ Complete |
| DRYR-01 | Phase 5 | ✓ Complete |
| DRYR-02 | Phase 5 | ✓ Complete |

**Coverage:** 12/12 requirements complete.

## Notes

- No requirements dropped or deferred during v1.1 execution.
- No audit run before archival (bulk archival retroactively alongside v1.0; verification relied on the phase summaries, the 180-test green suite, and the working end-to-end `replay` + `explain` + `--dry-run` flows).
- v1.2 scope (L7 policies, `cpg apply`, Prometheus metrics, policy consolidation) is captured in `PROJECT.md` under "Planned" and will be redefined when `/gsd:new-milestone` runs.
