---
milestone: v1.6
milestone_name: Audit-Mode Onboarding & cpg-Dedicated Agent Tooling
audited: 2026-07-22
status: passed
phases_audited: [20, 21, 22, 23, 24]
requirements_total: 13
requirements_complete: 13
integration_verdict: passed
---

# Milestone v1.6 Audit

## Definition of Done — Verdict: PASSED

All 9 milestone phases complete (16-19 shipped with v1.5; 20-24 audited here). 13/13 v1.6 requirements Complete (AUD-01..04, SKL-01..06, COMPAT-01..03). Cross-phase integration verified WIRED 7/7 by the integration checker with full code-path traces; full suite green (`go test ./... -count=1 -race`, 13 packages).

## Phase Verification Summary

| Phase | Verification | Review | Notes |
|-------|-------------|--------|-------|
| 20 `--include-audit` | passed | fixed | AUD-01; byte-identical default behavior regression-tested |
| 21 Compat matrix + detection | (no VERIFICATION.md — see note) | 3 warnings fixed (`all_fixed`) | COMPAT-01..03; validated via VALIDATION strategy + review-fix + downstream consumption (phases 22/23 both consume `featureFloors`/`DetectCiliumVersion` in production paths) |
| 22 Bootstrap artifact | passed 6/6 | 2 warnings fixed (`all_fixed`) | AUD-02; #35558-safe form empirically verified; stdout-only kept SEC-01 allowlist at 5 entries |
| 23 Managed audit window | passed 6/6 | 2 critical + 4 warnings fixed over 2 iterations (`all_fixed`) | AUD-03/AUD-04; CLI-only per user gate decision; SEC-01 proof byte-identical + tripwire |
| 24 Skills & agent tooling | passed 6/6 | 1 warning fixed (`all_fixed`) | SKL-01..06; consistency tripwire pins the 9-tool registry |

**Phase 21 note (tracking hygiene, not a functional gap):** phase 21 predates this session's standardized verifier step; its `21-VALIDATION.md` frontmatter still reads `status: planned`. Its deliverables are proven by consumers: the `featureFloors` table and `DetectCiliumVersion` are load-bearing, tested dependencies of phases 22 and 23, and the README compat table is pinned by `readme_compat_test.go`.

## Integration Check (cross-phase wiring)

7/7 flows WIRED with file:line traces (see integration-checker report, summarized):
- 20→22: runbook capture step uses the real `--include-audit`/`include_audit` surfaces.
- 21→22: bootstrap gate reads the shared `featureFloors` table (no parallel mechanism); MCP surfaces compat info.
- 21→23: audit-window consumes `k8s.CiliumBinaryName(compat)` end-to-end into the exec argv.
- 22→23: runbook flows bootstrap → audit-window → capture → human apply with only real flags.
- 23 SEC-01: both structural proofs green; exactly 9 `mcp.AddTool` sites (zero added by the mutation phase).
- 24→all: skills route to the live 9-tool registry; tripwire green.
- E2E doc read-through: every runbook command block hits real commands/flags.

Two documentation-drift warnings raised by the checker were fixed pre-audit-close (`36e7fc2`): README's MCP tool table now lists `get_bootstrap_policy` (9th row), REQUIREMENTS.md COMPAT checkboxes + SKL-05 8-vs-9 prose reconciled.

## Tech Debt Carried Forward (accepted, documented)

- IN (ph.23): revert deadline scales `removeWait × N` despite concurrent fan-out — worst-case slow exit on large namespaces; upgrade path documented in review.
- IN (ph.23): stale `CheckDaemonAuditMode` doc comment vs `Open`'s warn-and-proceed handling.
- IN (ph.23): contrived top-level bare-`func()` tripwire evasion shape (defense-in-depth note, no live gap).
- IN (ph.22): builder's shared `t := true` backing var (future mutation footgun); `VersionSource` empty-vs-enum cosmetic.
- IN (ph.24): tripwire regex catches only backtick-quoted tool mentions (deliberate router-principle trade-off); smoke coverage floor is named-not-exercised for `get_bootstrap_policy`.
- Pre-v1.6 debt unchanged in PROJECT.md § Planned (LINT-01..03, RELSEC-01..02, replay exit parity, etc.).

## Deferred (tracked in REQUIREMENTS Future / PROJECT Planned)

- AUD-FUT-01 WebSocket exec fallback; AUD-FUT-02 cpg-managed CNP apply/delete behind launch flag.
- Variant A (MCP flag-gated audit window) — would require the SEC-01 two-mode mechanism decision then.
