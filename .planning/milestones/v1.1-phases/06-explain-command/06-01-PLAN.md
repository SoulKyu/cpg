---
phase: 06-explain-command
plan: 01
type: execute
wave: 1
depends_on: [04-offline-replay-core, 05-dry-run-pipeline]
files_modified:
  - cmd/cpg/commonflags.go
  - cmd/cpg/generate.go
  - cmd/cpg/generate_test.go
  - cmd/cpg/replay.go
  - cmd/cpg/replay_test.go
  - cmd/cpg/dryrun_test.go
  - cmd/cpg/explain.go
  - cmd/cpg/explain_target.go
  - cmd/cpg/explain_target_test.go
  - cmd/cpg/explain_filter.go
  - cmd/cpg/explain_filter_test.go
  - cmd/cpg/explain_render.go
  - cmd/cpg/explain_render_test.go
  - cmd/cpg/explain_test.go
  - cmd/cpg/testhelpers_test.go
  - cmd/cpg/main.go
  - README.md
  - go.mod
  - go.sum
autonomous: true
requirements:
  - EXPL-01
  - EXPL-02
  - EXPL-03
must_haves:
  truths:
    - "cmd/cpg/commonflags.go factors flags shared by generate and replay"
    - "cpg generate and cpg replay both honor --dry-run, --no-diff, --no-evidence, --evidence-dir, --evidence-samples, --evidence-sessions"
    - "cpg replay <file.jsonl> produces policies + evidence with source.type=replay"
    - "cpg replay supports `-` for stdin"
    - "cpg generate session source records source.type=live with the relay server address"
    - "cpg explain NAMESPACE/WORKLOAD reads evidence at $XDG_CACHE_HOME/cpg/evidence/<hash>/<ns>/<workload>.json and prints formatted text"
    - "cpg explain path/to/policy.yaml resolves NS + workload from the YAML"
    - "cpg explain rejects policies whose metadata.name does not start with 'cpg-'"
    - "Filters --ingress/--egress/--port/--peer/--peer-cidr/--since compose as logical AND"
    - "--peer-cidr requires rule CIDR to be fully contained in the filter CIDR"
    - "--json and --format yaml emit structured output"
    - "Missing evidence produces an actionable error naming the exact path checked"
    - "README documents replay, explain, dry-run workflows and updates Project structure + Flags"
    - "go build ./... and go test ./... pass"
  artifacts:
    - path: cmd/cpg/commonflags.go
      provides: "Shared cobra flags for generate and replay"
      exports: [addCommonFlags, parseCommonFlags]
    - path: cmd/cpg/replay.go
      provides: "cpg replay subcommand"
      exports: [newReplayCmd]
    - path: cmd/cpg/explain.go
      provides: "cpg explain subcommand"
      exports: [newExplainCmd]
---

<objective>
Ship the user-facing surface: factor shared flags, add `cpg replay` and `cpg explain`
subcommands, wire dry-run + evidence into generate, and document every workflow in
README.md. This plan closes milestone v1.1.
</objective>

<source_of_truth>
`docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md` — Tasks 13-20.
</source_of_truth>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13: Shared flag helper</name>
  <files>cmd/cpg/commonflags.go, cmd/cpg/generate.go, cmd/cpg/generate_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 13)
    - cmd/cpg/generate.go (current flag setup, generateFlags struct, parseGenerateFlags)
  </read_first>
  <action>
Execute master plan Task 13 verbatim. Create cmd/cpg/commonflags.go defining `commonFlags` struct, `addCommonFlags(cmd)`, `parseCommonFlags(cmd)`. Refactor generate.go: newGenerateCmd calls addCommonFlags instead of declaring flags locally; generateFlags embeds commonFlags; parseGenerateFlags delegates to parseCommonFlags for shared fields. Provide helpers `mustString/mustBool/mustDuration`. Ensure existing generate_test.go still passes (update any flag-path assertions if needed). Commit: `refactor(cmd): factor common flags into helper for generate and replay`.
  </action>
  <acceptance_criteria>
    - `grep -q 'func addCommonFlags' cmd/cpg/commonflags.go` matches
    - `grep -q 'func parseCommonFlags' cmd/cpg/commonflags.go` matches
    - `grep -q 'type commonFlags struct' cmd/cpg/commonflags.go` matches
    - `grep -q 'commonFlags' cmd/cpg/generate.go` matches
    - `go test ./cmd/cpg/... -count=1` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./cmd/cpg/... -count=1</automated>
  </verify>
  <done>Common flags live in one place and are reused by generate; tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 14: cpg replay command</name>
  <files>cmd/cpg/replay.go, cmd/cpg/replay_test.go, cmd/cpg/main.go, cmd/cpg/testhelpers_test.go, go.mod, go.sum</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 14)
    - cmd/cpg/commonflags.go
    - pkg/flowsource/file.go
    - pkg/hubble/pipeline.go
    - pkg/evidence/paths.go
  </read_first>
  <action>
Execute master plan Task 14 verbatim. Add dep: `go get github.com/google/uuid`. Create cmd/cpg/replay.go with newReplayCmd + runReplay that: builds flowsource.NewFileSource, resolves evidenceDir via evidence.DefaultEvidenceDir (fall-back to override), builds hubble.PipelineConfig with EvidenceEnabled, OutputHash (HashOutputDir), EvidenceCaps, SessionID (`<RFC3339>-<4hex>`), SessionSource{Type:"replay", File:absPath}, CPGVersion from `version` package var. Include a package-level `version = "dev"` in main.go if none exists. Register with `rootCmd.AddCommand(newReplayCmd())` in main.go. Add cmd/cpg/testhelpers_test.go exporting `initLoggerForTesting(t *testing.T)` that sets `logger = zap.NewNop()`. Add cmd/cpg/replay_test.go: TestReplayCommandProducesPoliciesAndEvidence that runs replay against testdata/flows/small.jsonl and asserts both policy YAMLs and evidence JSON are written. Also add helpers `absolutePath` and `isTerminal`. Commit: `feat(cli): cpg replay subcommand for offline jsonpb playback`.
  </action>
  <acceptance_criteria>
    - `grep -q 'google/uuid' go.mod` matches
    - `grep -q 'func newReplayCmd' cmd/cpg/replay.go` matches
    - `grep -q 'rootCmd.AddCommand(newReplayCmd())' cmd/cpg/main.go` matches
    - `grep -q 'func initLoggerForTesting' cmd/cpg/testhelpers_test.go` matches
    - `go test ./cmd/cpg/... -run Replay -v` exits 0
    - After the test, the evidence file path exists and contains `"type": "replay"` in the session source
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./cmd/cpg/... -count=1</automated>
  </verify>
  <done>`cpg replay <file>` ingests a jsonpb dump, writes policies and evidence, session source type is "replay".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15: Wire dry-run + evidence into cpg generate</name>
  <files>cmd/cpg/generate.go, cmd/cpg/dryrun_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 15)
    - cmd/cpg/generate.go (current runGenerate body)
    - cmd/cpg/replay.go (mirror evidence + dry-run wiring)
  </read_first>
  <action>
Execute master plan Task 15 verbatim. In runGenerate, after clusterPolicies are loaded, populate hubble.PipelineConfig with DryRun, DryRunDiff (= !f.dryRunNoDiff), DryRunColor (= isTerminal(os.Stdout)), EvidenceEnabled, EvidenceDir (from resolveEvidenceDir helper), OutputHash (HashOutputDir(outputDir)), EvidenceCaps, SessionID (RFC3339 + uuid), SessionSource{Type:"live", Server: server}, CPGVersion. Add `resolveEvidenceDir(override string)` helper. Write cmd/cpg/dryrun_test.go with TestReplayDryRunWritesNothing asserting no files land in the output dir or evidence dir when `--dry-run --no-diff` is passed. Commit: `feat(cli): wire dry-run and evidence flags into generate subcommand`.
  </action>
  <acceptance_criteria>
    - `grep -q 'EvidenceEnabled' cmd/cpg/generate.go` matches
    - `grep -q 'DryRun' cmd/cpg/generate.go` matches
    - `grep -q 'SessionSource' cmd/cpg/generate.go` matches
    - `grep -q 'Type: "live"' cmd/cpg/generate.go` matches
    - `go test ./cmd/cpg/... -run DryRun -v` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./cmd/cpg/... -count=1</automated>
  </verify>
  <done>Generate and replay share the same evidence + dry-run plumbing; dry-run leaves disk untouched in both commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 16: Explain target resolver + filters</name>
  <files>cmd/cpg/explain_target.go, cmd/cpg/explain_target_test.go, cmd/cpg/explain_filter.go, cmd/cpg/explain_filter_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 16)
    - pkg/evidence/schema.go (RuleEvidence, PeerRef)
  </read_first>
  <action>
Execute master plan Task 16 verbatim. Create explain_target.go with `explainTarget` struct and `resolveExplainTarget(arg string)` that either splits NAMESPACE/WORKLOAD or parses a YAML file (requires metadata.name to start with `cpg-`). Create explain_filter.go with `explainFilter` struct + `match(evidence.RuleEvidence) bool` + `parsePeerLabel(s string)`. CIDR filter requires `filter.Contains(ruleIP)` AND `ruleMaskBits >= filterMaskBits`. Write tests per master plan. Commit: `feat(cli): explain target resolver and filter matchers`.
  </action>
  <acceptance_criteria>
    - `grep -q 'func resolveExplainTarget' cmd/cpg/explain_target.go` matches
    - `grep -q 'cpg-' cmd/cpg/explain_target.go` matches (prefix check for YAML form)
    - `grep -q 'func (f explainFilter) match' cmd/cpg/explain_filter.go` matches
    - `go test ./cmd/cpg/... -run "Target|Filter" -v` exits 0
    - CIDR filter rejects a rule CIDR broader than the filter CIDR
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/cpg/... -run "Target|Filter" -count=1</automated>
  </verify>
  <done>Target resolver handles both forms with clear error messages; filter matchers compose correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17: Explain renderers</name>
  <files>cmd/cpg/explain_render.go, cmd/cpg/explain_render_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 17)
    - pkg/evidence/schema.go
  </read_first>
  <action>
Execute master plan Task 17 verbatim. Implement `renderText(w io.Writer, pe PolicyEvidence, matched []RuleEvidence, samplesLimit int, color bool)` with header, per-rule block, sample flow table, and a branch that lists available rules when matched is empty. Implement `renderJSON` and `renderYAML` emitting `{policy, sessions, matched_rules}`. Also include `peerSummary`, `fmtEndpoint`, `colorizer`, and ANSI constants per master plan. Commit: `feat(cli): text/json/yaml renderers for cpg explain`.
  </action>
  <acceptance_criteria>
    - `grep -q 'func renderText' cmd/cpg/explain_render.go` matches
    - `grep -q 'func renderJSON' cmd/cpg/explain_render.go` matches
    - `grep -q 'func renderYAML' cmd/cpg/explain_render.go` matches
    - `go test ./cmd/cpg/... -run Render -v` exits 0 (all 4 tests)
    - Empty-match rendering contains "Available rules:"
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/cpg/... -run Render -count=1</automated>
  </verify>
  <done>All three renderers emit the documented shapes; text format uses ANSI only when color=true.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18: cpg explain command wiring</name>
  <files>cmd/cpg/explain.go, cmd/cpg/explain_test.go, cmd/cpg/main.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 18)
    - cmd/cpg/explain_target.go, cmd/cpg/explain_filter.go, cmd/cpg/explain_render.go
    - pkg/evidence/paths.go, pkg/evidence/reader.go
  </read_first>
  <action>
Execute master plan Task 18 verbatim. Implement newExplainCmd + runExplain that: resolves target, resolves evidence dir (flag override or XDG default), computes HashOutputDir, calls evidence.NewReader + Reader.Read, wraps fs.ErrNotExist into a user-facing error naming the exact path checked, builds explainFilter via buildFilter (rejects --ingress + --egress as mutually exclusive), picks format (json shortcut via --json, else --format), dispatches to the appropriate renderer. Add `asFile(w interface{}) *os.File` TTY helper. Register command in main.go. Add explain_test.go with the 3 tests from master plan (seed evidence file via HashOutputDir, run the command, verify output). Commit: `feat(cli): cpg explain subcommand with filters and multi-format output`.
  </action>
  <acceptance_criteria>
    - `grep -q 'func newExplainCmd' cmd/cpg/explain.go` matches
    - `grep -q 'rootCmd.AddCommand(newExplainCmd())' cmd/cpg/main.go` matches
    - `grep -q 'no evidence found' cmd/cpg/explain.go` matches (lowercase substring)
    - `go test ./cmd/cpg/... -run Explain -v` exits 0 (all 3 tests)
    - `go build ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./cmd/cpg/... -count=1</automated>
  </verify>
  <done>`cpg explain` works end-to-end in text and JSON modes, errors cleanly when evidence is missing, and is wired into the root command.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 19: README additions</name>
  <files>README.md</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 19)
    - README.md (current structure)
  </read_first>
  <action>
Execute master plan Task 19 verbatim. Insert:
1. Offline replay Quick start block right after the existing `## Quick start`.
2. Full `## Offline replay` section after `## What it generates`.
3. Full `## Explain policies` section after `## Unhandled flows`.
4. Full `## Dry-run` section under `## Deduplication`.
5. Append new flags + new subcommand reference blocks to `## Flags`.
6. Replace `## Project structure` block to list pkg/flowsource, pkg/evidence, pkg/diff.
Commit: `docs: document cpg replay, explain, and dry-run workflows`.
  </action>
  <acceptance_criteria>
    - `grep -q '## Offline replay' README.md` matches
    - `grep -q '## Explain policies' README.md` matches
    - `grep -q '## Dry-run' README.md` matches
    - `grep -q 'cpg replay' README.md` matches
    - `grep -q 'cpg explain' README.md` matches
    - `grep -q 'pkg/flowsource' README.md` matches
    - `grep -q 'pkg/evidence' README.md` matches
    - `grep -q 'pkg/diff' README.md` matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## " README.md</automated>
  </verify>
  <done>README documents every new workflow and lists the new packages in the project structure block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 20: Release trigger (Release-As 1.6.0)</name>
  <files>.</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 20)
    - .github/workflows/release.yml (to confirm release-please wiring)
  </read_first>
  <action>
After phases 4, 5, and 6 are committed to the branch and ready for PR: create an empty commit with release-please directive to force the 1.6.0 bump, since refactor/docs/test commits alone don't trigger a minor bump by default. Run from the feature branch:

```
git commit --allow-empty -m "chore: release 1.6.0

Release-As: 1.6.0"
```

Do NOT push directly to master. Create the PR via `gh pr create` or the web UI; release-please opens its own PR once the feature PR is merged.
  </action>
  <acceptance_criteria>
    - `git log -1 --pretty=%s%n%n%b` shows `chore: release 1.6.0` and `Release-As: 1.6.0`
    - No direct push to master (verified by branch being the feature branch)
  </acceptance_criteria>
  <verify>
    <automated>git log -1 --pretty=%B | grep -q "Release-As: 1.6.0"</automated>
  </verify>
  <done>Feature branch ready for PR; release-please will pick up the Release-As directive after merge.</done>
</task>

</tasks>

<verification>
- `cpg replay --help` prints the new subcommand
- `cpg explain --help` prints the new subcommand
- `go build ./... && go test ./... -count=1 -race` passes
- README sections exist and are cross-linked from the Flags block
</verification>

<success_criteria>
- Users can run `cpg replay <file.jsonl>` end-to-end and see policies + evidence on disk
- `cpg explain NAMESPACE/WORKLOAD` surfaces flow evidence in text, JSON, and YAML
- Dry-run works identically on generate and replay
- Documentation covers every new workflow; project structure block reflects the new packages
</success_criteria>

<output>
`.planning/phases/06-explain-command/06-01-SUMMARY.md` after completion.
</output>
