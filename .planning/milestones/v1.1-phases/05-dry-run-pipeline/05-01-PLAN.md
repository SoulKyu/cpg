---
phase: 05-dry-run-pipeline
plan: 01
type: execute
wave: 1
depends_on: [04-offline-replay-core]
files_modified:
  - pkg/diff/yaml.go
  - pkg/diff/yaml_test.go
  - pkg/hubble/pipeline.go
  - pkg/hubble/writer.go
  - pkg/hubble/writer_dryrun_test.go
  - pkg/hubble/evidence_writer.go
  - pkg/hubble/evidence_writer_test.go
  - pkg/output/writer.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - DRYR-01
  - DRYR-02
must_haves:
  truths:
    - "pkg/diff.UnifiedYAML returns empty string on identical inputs and a unified diff otherwise"
    - "ANSI colors apply only when explicitly requested (color=true)"
    - "policyWriter skips all filesystem writes when DryRun is true"
    - "Dry-run emits `would write policy` INFO log per policy event"
    - "Dry-run emits unified diff to stdout (or injected sink) when DryRunDiff is true and a file already exists"
    - "SessionStats gains PoliciesWouldWrite / PoliciesWouldSkip counters"
    - "PolicyEvent channel is fanned out to policyWriter and evidenceWriter without blocking"
    - "evidenceWriter converts RuleAttribution to RuleEvidence and delegates to pkg/evidence.Writer"
    - "Evidence writes are skipped in dry-run mode"
    - "go build ./... and go test ./... pass"
  artifacts:
    - path: pkg/diff/yaml.go
      provides: "Unified YAML diff with optional ANSI coloring"
      exports: [UnifiedYAML]
    - path: pkg/hubble/evidence_writer.go
      provides: "Evidence-writer goroutine fed from fanned-out PolicyEvent channel"
      exports: [newEvidenceWriter]
    - path: pkg/output/writer.go
      provides: "ReadExisting and OutputDir helpers for dry-run diff support"
      exports: [ReadExisting, OutputDir]
---

<objective>
Add dry-run mode (no-write + unified diff) and wire evidence capture into the streaming
pipeline via a fan-out from the single PolicyEvent channel.
</objective>

<source_of_truth>
`docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md` — Tasks 10, 11, 12.
</source_of_truth>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10: YAML unified diff package</name>
  <files>pkg/diff/yaml.go, pkg/diff/yaml_test.go, go.mod, go.sum</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 10)
  </read_first>
  <action>
Execute master plan Task 10 verbatim. Add dependency:
`go get github.com/pmezard/go-difflib/difflib`. Write 4 tests (identical → empty, addition, deletion, headers). Implement `UnifiedYAML(aName, bName string, a, b []byte, color bool)`. Colors wrap `+` lines green and `-` lines red via ANSI escape codes; header lines (`+++`, `---`) are not colored. Commit: `feat(diff): unified YAML diff with optional ANSI colors`.
  </action>
  <acceptance_criteria>
    - `grep -q 'pmezard/go-difflib' go.mod` matches
    - `grep -q 'func UnifiedYAML' pkg/diff/yaml.go` matches
    - `go test ./pkg/diff/... -v` exits 0 (4 tests)
    - Identical inputs return "" (empty diff)
    - Addition/deletion diffs include `+`/`-` prefixes and unified-diff headers
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/diff/... -count=1</automated>
  </verify>
  <done>YAML diff renders unified format; colors are opt-in; empty string signals no change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 11: policyWriter dry-run branch</name>
  <files>pkg/hubble/pipeline.go, pkg/hubble/writer.go, pkg/hubble/writer_dryrun_test.go, pkg/output/writer.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 11)
    - pkg/hubble/pipeline.go (PipelineConfig, SessionStats)
    - pkg/hubble/writer.go (policyWriter + handle/skipForClusterMatch/skipForCrossFlushMatch)
    - pkg/output/writer.go (current Writer implementation)
  </read_first>
  <action>
Execute master plan Task 11 verbatim:
1. Extend PipelineConfig with DryRun, DryRunDiff, DryRunColor fields.
2. Extend SessionStats with PoliciesWouldWrite + PoliciesWouldSkip and include in Log().
3. Extend policyWriter struct with dryRun, dryRunDiff, dryRunColor, diffOut (io.Writer).
4. Replace handle() to branch on dryRun: skip write + log "would write" + optional diff.
5. Add bumpSkip() and dryRunEmit() helpers.
6. Add ReadExisting(ns, workload) + OutputDir() to pkg/output.Writer (non-breaking additions).
7. Import pkg/diff, sigs.k8s.io/yaml (aliased sigyaml), io, os, path/filepath in writer.go.
8. Add writer_dryrun_test.go with TestDryRunDoesNotWriteToDisk and TestDryRunEmitsDiffWhenExistingChanges.
Commit: `feat(hubble): dry-run mode in policyWriter with YAML unified diff`.
  </action>
  <acceptance_criteria>
    - `grep -q 'DryRun ' pkg/hubble/pipeline.go` matches
    - `grep -q 'PoliciesWouldWrite' pkg/hubble/pipeline.go` matches
    - `grep -q 'dryRun ' pkg/hubble/writer.go` matches
    - `grep -q 'diff.UnifiedYAML' pkg/hubble/writer.go` matches
    - `grep -q 'func (w \*Writer) OutputDir()' pkg/output/writer.go` matches
    - `grep -q 'func (w \*Writer) ReadExisting(' pkg/output/writer.go` matches
    - `go test ./pkg/hubble/... -run DryRun -v` exits 0 (2 tests pass)
    - `go test ./... -count=1` exits 0 (no regression)
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./... -count=1</automated>
  </verify>
  <done>Dry-run suppresses all filesystem writes, logs an accurate "would write" event, prints a unified diff to stdout (or injected sink) when an existing file differs, and propagates dry-run counters in SessionStats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12: Evidence writer goroutine + pipeline fan-out</name>
  <files>pkg/hubble/evidence_writer.go, pkg/hubble/evidence_writer_test.go, pkg/hubble/pipeline.go, pkg/policy/builder.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 12)
    - pkg/evidence/writer.go, pkg/evidence/schema.go
    - pkg/policy/attribution.go
    - pkg/hubble/pipeline.go (RunPipelineWithSource body)
    - pkg/hubble/aggregator.go (to understand Aggregator.tracker flow)
  </read_first>
  <action>
Execute master plan Task 12 verbatim:
1. Create pkg/hubble/evidence_writer.go with:
   - `evidenceWriter` struct + `newEvidenceWriter(evidenceDir, outputHash string, caps evidence.MergeCaps, session evidence.SessionInfo, logger *zap.Logger)`
   - `handle(pe policy.PolicyEvent)` that converts RuleAttribution → RuleEvidence and calls evidence.Writer.Write
   - `finalize(flowsIngested, flowsUnhandled int64)` that updates session counters by merging a zero-rule update for every seen workload
   - helpers: `splitKey`, `convert` (RuleAttribution → RuleEvidence), `convertPeer`, `convertSample`, `endpointFromFlow`, `portFromKey`
2. Export `FlowTime` from pkg/policy/builder.go (rename existing `flowTime` → `FlowTime`; update local callers). The evidence writer uses `policy.FlowTime(f)` for sample timestamps.
3. Extend PipelineConfig with: EvidenceEnabled (bool), EvidenceDir (string), OutputHash (string), EvidenceCaps (evidence.MergeCaps), SessionID (string), SessionSource (evidence.SourceInfo), CPGVersion (string).
4. In RunPipelineWithSource, replace the single `policies` channel with a fan-out pattern: aggregator → `policies` → tee goroutine → `policyCh` + `evidenceCh`. Two consumer goroutines read the channels independently. When EvidenceEnabled is false OR DryRun is true, the evidence consumer drains events without writing.
5. After g.Wait(), if evidence writer was active, call ew.session.EndedAt = time.Now() and ew.finalize(int64(stats.FlowsSeen), int64(stats.LostEvents)).
6. Write evidence_writer_test.go: TestEvidenceWriterPersistsAttribution — construct a PolicyEvent with Attribution, call ew.handle + ew.finalize, assert the JSON on disk matches (sessions length 1, rule samples length 1, src.namespace == "default").
Commit: `feat(hubble): evidence writer goroutine fanned out from policy channel`.
  </action>
  <acceptance_criteria>
    - `grep -q 'func newEvidenceWriter' pkg/hubble/evidence_writer.go` matches
    - `grep -q 'func policy.FlowTime\\|func FlowTime' pkg/policy/builder.go` matches (exported)
    - `grep -q 'EvidenceEnabled' pkg/hubble/pipeline.go` matches
    - `grep -q 'policyCh' pkg/hubble/pipeline.go` matches AND `grep -q 'evidenceCh' pkg/hubble/pipeline.go` matches
    - `go test ./pkg/hubble/... -run Evidence -v` exits 0
    - `go test ./... -count=1` exits 0 (pre-existing tests still pass)
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./... -count=1</automated>
  </verify>
  <done>PolicyEvent channel is fanned out; evidence writer persists per-policy JSON when enabled and is skipped when dry-run is active.</done>
</task>

</tasks>

<verification>
- `go build ./...` exit 0
- `go test ./... -count=1 -race` exit 0
- pkg/diff present with UnifiedYAML + tests
- Dry-run mode verified end-to-end via writer_dryrun_test.go
- evidence_writer_test.go confirms the fan-out + merge produces valid JSON on disk
</verification>

<success_criteria>
- DRYR-01: `--dry-run` propagates through the pipeline and suppresses all filesystem writes (policy + evidence)
- DRYR-02: Unified YAML diff is produced for any policy whose on-disk YAML differs from the in-memory render, unless --no-diff via DryRunDiff=false
- Evidence capture runs in parallel with policy writing via a single-source fan-out — neither consumer blocks the other
</success_criteria>

<output>
`.planning/phases/05-dry-run-pipeline/05-01-SUMMARY.md` after completion.
</output>
