---
phase: 04-offline-replay-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/flowsource/source.go
  - pkg/flowsource/source_test.go
  - pkg/flowsource/file.go
  - pkg/flowsource/file_test.go
  - pkg/hubble/pipeline.go
  - pkg/evidence/schema.go
  - pkg/evidence/schema_test.go
  - pkg/evidence/paths.go
  - pkg/evidence/paths_test.go
  - pkg/evidence/merge.go
  - pkg/evidence/merge_test.go
  - pkg/evidence/writer.go
  - pkg/evidence/writer_test.go
  - pkg/evidence/reader.go
  - pkg/policy/attribution.go
  - pkg/policy/attribution_test.go
  - pkg/policy/builder.go
  - pkg/policy/builder_attribution_test.go
  - pkg/hubble/aggregator.go
  - testdata/flows/small.jsonl
  - testdata/flows/with_non_dropped.jsonl
  - testdata/flows/malformed.jsonl
  - testdata/flows/empty.jsonl
  - testdata/flows/small.jsonl.gz
  - go.mod
  - go.sum
autonomous: true
requirements:
  - OFFL-01
  - OFFL-02
  - OFFL-03
  - EVID-01
  - EVID-02
  - EVID-03
  - EVID-04
must_haves:
  truths:
    - "pkg/flowsource defines the FlowSource interface formerly in pkg/hubble"
    - "pkg/flowsource/file.go streams DROPPED flows from a Hubble jsonpb file (and stdin)"
    - "pkg/flowsource/file.go decompresses .gz transparently"
    - "Non-DROPPED verdicts and malformed lines are counted and skipped"
    - "pkg/evidence persists per-rule attribution under <evidence-dir>/<output-dir-hash>/<ns>/<workload>.json"
    - "Evidence schema_version = 1; reader refuses other versions"
    - "Evidence writes are atomic (temp file + rename)"
    - "Evidence merges cap samples per rule and sessions per policy via FIFO (newest kept)"
    - "pkg/policy.RuleKey.String() is deterministic regardless of label insertion order"
    - "pkg/policy.BuildPolicy returns (cnp, []RuleAttribution); existing callers migrated"
    - "go build ./... succeeds"
    - "go test ./... passes (all existing tests + new packages)"
  artifacts:
    - path: pkg/flowsource/source.go
      provides: "FlowSource interface decoupled from pkg/hubble"
      exports: [FlowSource]
    - path: pkg/flowsource/file.go
      provides: "jsonpb file source with DROPPED filter, gzip support, stdin support"
      exports: [FileSource, NewFileSource, FileSourceStats]
    - path: pkg/evidence/schema.go
      provides: "JSON schema v1 for PolicyEvidence, RuleEvidence, FlowSample, PeerRef"
      exports: [PolicyEvidence, RuleEvidence, FlowSample, PeerRef, SessionInfo, SourceInfo, PolicyRef, FlowEndpoint, SchemaVersion, NewSkeleton]
    - path: pkg/evidence/paths.go
      provides: "XDG cache resolution and output-dir hashing"
      exports: [HashOutputDir, DefaultEvidenceDir, ResolvePolicyPath]
    - path: pkg/evidence/merge.go
      provides: "Merge semantics with FIFO caps"
      exports: [Merge, MergeCaps]
    - path: pkg/evidence/writer.go
      provides: "Atomic load-merge-write writer"
      exports: [Writer, NewWriter]
    - path: pkg/evidence/reader.go
      provides: "PolicyEvidence reader with schema version check"
      exports: [Reader, NewReader, IsNotExist]
    - path: pkg/policy/attribution.go
      provides: "RuleKey + RuleAttribution types and deterministic string encoding"
      exports: [RuleKey, RuleAttribution, Peer, PeerType, PeerEndpoint, PeerCIDR, PeerEntity, AttributionOptions]
---

<objective>
Build the offline-replay foundation: FlowSource refactor, jsonpb file ingestion (with
gzip), the evidence persistence layer, and per-rule attribution from the policy builder.
This plan establishes every new package the rest of milestone v1.1 depends on.
</objective>

<source_of_truth>
**Primary reference:** `docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md`
**Design spec:** `docs/superpowers/specs/2026-04-24-offline-replay-and-analysis-design.md`

Every task below maps 1:1 to a task in the master plan; follow that plan verbatim for
code, test content, and commit messages. The tasks are listed in their required order.
</source_of_truth>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote FlowSource interface to pkg/flowsource</name>
  <files>pkg/flowsource/source.go, pkg/flowsource/source_test.go, pkg/hubble/pipeline.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 1)
    - pkg/hubble/pipeline.go (current FlowSource interface location)
    - pkg/hubble/client.go (current implementation)
  </read_first>
  <action>
Execute master plan "Task 1: Promote FlowSource interface to pkg/flowsource" verbatim:
1. Create pkg/flowsource/source_test.go with the interface-contract test shown in the master plan.
2. Create pkg/flowsource/source.go with the FlowSource interface (take code from the master plan verbatim).
3. Edit pkg/hubble/pipeline.go: remove the local FlowSource interface, update imports to consume
   `github.com/SoulKyu/cpg/pkg/flowsource`, change RunPipelineWithSource signature to accept
   `flowsource.FlowSource`. Remove the now-unused `flowpb` import from pipeline.go.
4. `go build ./... && go test ./...` must pass.
5. Commit: `refactor: promote FlowSource interface to pkg/flowsource`
  </action>
  <acceptance_criteria>
    - File pkg/flowsource/source.go exists and declares `type FlowSource interface`
    - `grep -q "FlowSource interface" pkg/hubble/pipeline.go` → no match (removed)
    - `grep -q "flowsource.FlowSource" pkg/hubble/pipeline.go` → match
    - `go build ./...` exit 0
    - `go test ./pkg/flowsource/... -v` exits 0 with `TestFlowSourceInterfaceSatisfied` passing
    - `go test ./...` exits 0 (all pre-existing tests continue to pass)
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./... -count=1</automated>
  </verify>
  <done>pkg/flowsource declares the FlowSource interface. pkg/hubble uses it. Full test suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evidence JSON schema</name>
  <files>pkg/evidence/schema.go, pkg/evidence/schema_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 2)
    - docs/superpowers/specs/2026-04-24-offline-replay-and-analysis-design.md (Section 5.1)
  </read_first>
  <action>
Execute master plan Task 2 verbatim: create `pkg/evidence/schema_test.go` then
`pkg/evidence/schema.go` with the PolicyEvidence / SessionInfo / RuleEvidence /
PeerRef / FlowSample / FlowEndpoint / PolicyRef types and `SchemaVersion` const + `NewSkeleton` helper. All JSON field names exactly as documented in the master plan.
Commit: `feat(evidence): add JSON schema for per-rule attribution`.
  </action>
  <acceptance_criteria>
    - `grep -q 'const SchemaVersion = 1' pkg/evidence/schema.go` matches
    - `grep -q 'type PolicyEvidence struct' pkg/evidence/schema.go` matches
    - `grep -q 'type RuleEvidence struct' pkg/evidence/schema.go` matches
    - `grep -q 'schema_version' pkg/evidence/schema.go` matches (JSON tag)
    - `go test ./pkg/evidence/... -run TestSchemaRoundtrip -v` exits 0
    - `go test ./pkg/evidence/... -run TestSchemaEmptySkeleton -v` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/evidence/... -count=1</automated>
  </verify>
  <done>Schema types compile, JSON round-trip test passes, NewSkeleton returns a well-formed empty PolicyEvidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: XDG cache paths + output-dir hash</name>
  <files>pkg/evidence/paths.go, pkg/evidence/paths_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 3)
  </read_first>
  <action>
Execute master plan Task 3 verbatim: write tests covering hash stability (trailing slashes / `./` segments do not affect the hash, different inputs yield different hashes), XDG_CACHE_HOME honored, HOME fallback, `ResolvePolicyPath` joining. Implement `HashOutputDir` (SHA-256 first 12 hex chars), `DefaultEvidenceDir`, `ResolvePolicyPath`. Commit: `feat(evidence): XDG-aware path resolver with output-dir hash`.
  </action>
  <acceptance_criteria>
    - `go test ./pkg/evidence/... -run Hash -v` passes
    - `go test ./pkg/evidence/... -run Default -v` passes
    - `HashOutputDir("/a/")` equals `HashOutputDir("/a")` (verified by test)
    - `DefaultEvidenceDir()` returns a path containing `cpg/evidence`
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/evidence/... -count=1</automated>
  </verify>
  <done>Path helpers implemented, XDG + HOME fallback tested, hash stable across equivalent inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Evidence merge semantics with FIFO caps</name>
  <files>pkg/evidence/merge.go, pkg/evidence/merge_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 4)
    - pkg/evidence/schema.go
  </read_first>
  <action>
Execute master plan Task 4 verbatim. Write 5 tests covering: new rule appended, existing
rule merged (counts, first/last seen, samples, sessions), MaxSamples FIFO newest-kept,
MaxSessions FIFO, rules not in new session preserved. Implement `Merge(existing *PolicyEvidence, session SessionInfo, newRules []RuleEvidence, caps MergeCaps)` with deterministic sort by (direction, key) on rules and ascending time on samples. Commit: `feat(evidence): merge semantics with FIFO sample/session caps`.
  </action>
  <acceptance_criteria>
    - `grep -q 'func Merge(' pkg/evidence/merge.go` matches
    - `grep -q 'type MergeCaps struct' pkg/evidence/merge.go` matches
    - All 5 Merge tests pass: `go test ./pkg/evidence/... -run Merge -v` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/evidence/... -run Merge -count=1</automated>
  </verify>
  <done>Merge correctly accumulates counts, preserves first_seen, caps samples and sessions FIFO, keeps rules not re-emitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Evidence atomic writer + reader</name>
  <files>pkg/evidence/writer.go, pkg/evidence/reader.go, pkg/evidence/writer_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 5)
    - pkg/evidence/schema.go, pkg/evidence/merge.go, pkg/evidence/paths.go
  </read_first>
  <action>
Execute master plan Task 5 verbatim. Implement `pkg/evidence/reader.go` first (Reader + NewReader + IsNotExist), then `pkg/evidence/writer.go` (Writer + NewWriter with atomic write via temp file + rename). Writer must refuse existing evidence whose schema_version differs from the compiled-in version. Tests: creates file, merges with existing, refuses unknown schema version. Commit: `feat(evidence): atomic writer + reader with schema version check`.
  </action>
  <acceptance_criteria>
    - `grep -q 'func (w \*Writer) Write(' pkg/evidence/writer.go` matches
    - `grep -q 'func (r \*Reader) Read(' pkg/evidence/reader.go` matches
    - `grep -q 'os.Rename' pkg/evidence/writer.go` matches (atomic write)
    - Reader returns wrapped fs.ErrNotExist detectable via errors.Is
    - Schema version mismatch returns an error mentioning "schema_version"
    - `go test ./pkg/evidence/... -run Writer -v` exits 0 (all 3 tests)
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/evidence/... -count=1</automated>
  </verify>
  <done>Writer persists merged evidence atomically. Reader loads with schema version validation. Round-trip via a temp directory verified end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: RuleKey + RuleAttribution types</name>
  <files>pkg/policy/attribution.go, pkg/policy/attribution_test.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 6)
    - pkg/policy/builder.go (to see peer types being represented)
  </read_first>
  <action>
Execute master plan Task 6 verbatim. Types: PeerType, Peer, RuleKey, RuleAttribution, AttributionOptions. RuleKey.String() encodes as "direction:peer-encoding:protocol:port"; endpoint peers sort labels alphabetically. Tests: endpoint/cidr/entity encoding, determinism regardless of map insertion order. Commit: `feat(policy): add RuleKey and RuleAttribution types`.
  </action>
  <acceptance_criteria>
    - `grep -q 'type RuleKey struct' pkg/policy/attribution.go` matches
    - `grep -q 'type RuleAttribution struct' pkg/policy/attribution.go` matches
    - `go test ./pkg/policy/... -run RuleKey -v` exits 0 (all 4 tests)
    - Two RuleKeys with same labels in different insertion order produce identical String()
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/policy/... -run RuleKey -count=1</automated>
  </verify>
  <done>Rule identity is stable and sortable, peer types encode correctly, Attribution tracks flow samples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Track attribution during BuildPolicy</name>
  <files>pkg/policy/builder.go, pkg/policy/builder_attribution_test.go, pkg/hubble/aggregator.go</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 7)
    - pkg/policy/builder.go (current BuildPolicy + peerBuckets + groupFlows)
    - pkg/policy/attribution.go (RuleKey, RuleAttribution)
    - pkg/hubble/aggregator.go (current BuildPolicy caller)
  </read_first>
  <action>
Execute master plan Task 7 verbatim. Extend peerRules with per-key attribution
(map[string]*RuleAttribution + recordAttribution helper), change BuildPolicy signature to
`(cnp *ciliumv2.CiliumNetworkPolicy, attribution []RuleAttribution)` with trailing
`opts AttributionOptions`. Thread opts through buildIngressRules / buildEgressRules /
groupFlows. Add ruleKeyFor and protoDisplayName helpers. Add FlowTime (exported) in
pkg/policy/builder.go. Extend PolicyEvent with Attribution field. Aggregator accepts
maxSamples and passes AttributionOptions{MaxSamples: a.maxSamples} into BuildPolicy.
Migrate every existing BuildPolicy call site in the repo (add AttributionOptions{} and
ignore attribution with _). Commit: `feat(policy): BuildPolicy returns per-rule attribution`.
  </action>
  <acceptance_criteria>
    - `grep -q 'BuildPolicy.*RuleAttribution' pkg/policy/builder.go` matches
    - `grep -q 'AttributionOptions' pkg/policy/builder.go` matches
    - `grep -q 'Attribution \[\]RuleAttribution' pkg/policy/event.go pkg/policy/builder.go pkg/policy/pipeline.go 2>/dev/null | head -1` matches (PolicyEvent extended)
    - `go test ./pkg/policy/... -run BuildPolicyEmitsAttribution -v` exits 0
    - `go build ./...` exits 0 (all call sites migrated)
    - `go test ./...` exits 0 (no regression)
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./... -count=1</automated>
  </verify>
  <done>BuildPolicy exposes per-rule flow attribution. Every emitted rule has a matching RuleAttribution entry. Aggregator forwards AttributionOptions. PolicyEvent carries the attribution downstream.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Jsonpb file source (plain text) + fixtures</name>
  <files>pkg/flowsource/file.go, pkg/flowsource/file_test.go, testdata/flows/small.jsonl, testdata/flows/with_non_dropped.jsonl, testdata/flows/malformed.jsonl, testdata/flows/empty.jsonl</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 8)
    - pkg/flowsource/source.go (interface to satisfy)
  </read_first>
  <action>
Execute master plan Task 8 verbatim. Create the 4 jsonl fixtures with exact content from the master plan. Write file_test.go covering: happy path, non-DROPPED skipped, malformed counted and skipped, empty file, missing file error, context cancellation. Implement pkg/flowsource/file.go with protojson parsing of observer.GetFlowsResponse, 10 MiB scanner buffer, atomic counters (LinesRead, FlowsEmitted, NonDroppedSkipped, Malformed), stdin support via "-". Gzip branch returns "gzip not yet supported" (enabled in Task 9). Commit: `feat(flowsource): jsonpb file source with DROPPED filter and error counters`.
  </action>
  <acceptance_criteria>
    - Files exist: testdata/flows/{small,with_non_dropped,malformed,empty}.jsonl
    - `grep -q 'func NewFileSource' pkg/flowsource/file.go` matches
    - `grep -q 'observer.GetFlowsResponse' pkg/flowsource/file.go` matches
    - `grep -q 'flowpb.Verdict_DROPPED' pkg/flowsource/file.go` matches
    - All 6 file-source tests pass: `go test ./pkg/flowsource/... -v` exits 0
    - FileSourceStats counters match per test fixture (3 dropped in small, 2 non-dropped skipped, 1 malformed)
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/flowsource/... -count=1</automated>
  </verify>
  <done>FileSource streams DROPPED flows from Hubble jsonpb, correctly filters non-DROPPED, logs and counts malformed lines, handles stdin and empty files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: Gzip support</name>
  <files>pkg/flowsource/file.go, pkg/flowsource/file_test.go, testdata/flows/small.jsonl.gz</files>
  <read_first>
    - docs/superpowers/plans/2026-04-24-offline-replay-and-analysis.md (Task 9)
    - pkg/flowsource/file.go (current openReader stub)
  </read_first>
  <action>
Execute master plan Task 9 verbatim. Add TestFileSourceGzip. Create the gzip fixture via
`gzip -k testdata/flows/small.jsonl`. Replace `openReader` to use `compress/gzip` when the
extension is `.gz`. Ensure both the gzip.Reader and the underlying file are closed via the
cleanup function. Commit: `feat(flowsource): transparent gzip decompression for .gz replay files`.
  </action>
  <acceptance_criteria>
    - testdata/flows/small.jsonl.gz exists and is non-empty
    - `grep -q 'gzip.NewReader' pkg/flowsource/file.go` matches
    - `grep -q 'compress/gzip' pkg/flowsource/file.go` matches
    - `go test ./pkg/flowsource/... -run Gzip -v` exits 0 and emits 3 flows
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/flowsource/... -count=1</automated>
  </verify>
  <done>`.gz` replay files are transparently decompressed; gzip fixture passes the same assertions as the plain-text small.jsonl.</done>
</task>

</tasks>

<verification>
- `go build ./...` compiles cleanly
- `go test ./... -count=1 -race` passes
- New packages exist and export their documented surfaces
- Fixtures present under testdata/flows/
- All 9 master-plan tasks 1-9 committed individually (git log shows 9 new commits on top of prior HEAD, each with the commit message dictated by the master plan)
</verification>

<success_criteria>
- pkg/flowsource, pkg/evidence, pkg/policy/attribution.go all exist with the documented public APIs
- BuildPolicy emits attribution; aggregator forwards it via PolicyEvent
- FileSource parses Hubble jsonpb (including gzip) and filters DROPPED flows
- Every change is verifiable with a `grep` on the repo or a `go test` invocation
</success_criteria>

<output>
After completion, create `.planning/phases/04-offline-replay-core/04-01-SUMMARY.md` with:
- Which master-plan tasks landed (1-9)
- New package surfaces
- Lines-of-code delta
- Any deviations from the master plan (should be none)
</output>
