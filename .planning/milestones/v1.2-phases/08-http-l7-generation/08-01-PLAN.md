---
phase: 08-http-l7-generation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pkg/policy/l7.go
  - pkg/policy/l7_test.go
autonomous: true
requirements: [HTTP-02, HTTP-03, HTTP-05]
must_haves:
  truths:
    - "Pure function extractHTTPRules(*flowpb.Flow) returns []api.PortRuleHTTP from Flow.L7.Http records"
    - "HTTP method is uppercased and trimmed; empty method drops the entry"
    - "HTTP path is emitted as ^regexp.QuoteMeta(path)$ (anchored, escaped); empty path becomes ^/$"
    - "Query string and fragment are stripped from path before regex emission"
    - "Headers / Host / HostExact are NEVER populated on returned entries (HTTP-05 anti-feature)"
    - "Nil-safe: Flow with nil L7 or nil L7.Http returns empty slice"
  artifacts:
    - path: "pkg/policy/l7.go"
      provides: "HTTP L7 extraction primitives"
      exports: ["extractHTTPRules", "normalizeHTTPMethod"]
    - path: "pkg/policy/l7_test.go"
      provides: "TDD coverage + HTTP-05 anti-feature lint test"
  key_links:
    - from: "pkg/policy/l7.go::extractHTTPRules"
      to: "regexp.QuoteMeta"
      via: "stdlib"
      pattern: "regexp\\.QuoteMeta"
    - from: "pkg/policy/l7.go::normalizeHTTPMethod"
      to: "strings.ToUpper"
      via: "stdlib"
      pattern: "strings\\.ToUpper"
---

<objective>
Land the pure-function HTTP L7 extraction primitives that Plan 08-02 will wire into BuildPolicy.

Purpose: Isolate the HTTP method/path normalization + regex anchoring + anti-feature logic as a small, side-effect-free unit so it can be exhaustively unit-tested before any builder integration. Codifies the HTTP-02 (method casing), HTTP-03 (path regex anchoring), and HTTP-05 (no headers/host) invariants in one place.
Output: New `pkg/policy/l7.go` with `extractHTTPRules` and `normalizeHTTPMethod` helpers; new `pkg/policy/l7_test.go` with table-driven tests including the HTTP-05 anti-feature lint test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-http-l7-generation/08-CONTEXT.md
@pkg/policy/builder.go
@pkg/policy/dedup.go

<interfaces>
<!-- Cilium types and Hubble flow accessors. extractHTTPRules consumes Flow, returns []PortRuleHTTP. -->

From github.com/cilium/cilium/pkg/policy/api (vendored v1.19.1):
```go
// PortRuleHTTP — Cilium L7 HTTP matcher. Path is RE2 regex, Method is regex.
type PortRuleHTTP struct {
    Path          string
    Method        string
    Host          string                // HTTP-05: NEVER populated by cpg
    HostExact     string                // HTTP-05: NEVER populated by cpg
    Headers       []string              // HTTP-05: NEVER populated by cpg
    HeaderMatches []*HeaderMatch        // HTTP-05: NEVER populated by cpg
}
```

From github.com/cilium/cilium/api/v1/flow:
```go
// Layer7 — proto oneof; GetHttp() returns *HTTP or nil.
func (*Flow) GetL7() *Layer7
func (*Layer7) GetHttp() *HTTP

// HTTP — request access-log record from Envoy.
type HTTP struct {
    Code     uint32           // 0 for requests
    Method   string           // wire-form, may be lowercase in some captures
    Url      string           // full URL or path; may include ?query and #fragment
    Protocol string
    Headers  []*HTTPHeader    // intentionally ignored by cpg (HTTP-05)
}
```

Note on Url-vs-Path: Hubble's HTTP message exposes only `Url` (no separate Path field). Implementation must parse the URL to extract the path component, stripping query/fragment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for HTTP extraction primitives</name>
  <files>pkg/policy/l7_test.go</files>
  <behavior>
    Table-driven tests, each MUST fail until Task 2 implements the helpers:

    extractHTTPRules cases:
      - nil Flow → empty slice
      - Flow with nil L7 → empty slice
      - Flow with L7 but nil Http → empty slice
      - GET / → [{Method: "GET", Path: "^/$"}]
      - GET /api/v1/users → [{Method: "GET", Path: "^/api/v1/users$"}]
      - POST /api/v1/users → [{Method: "POST", Path: "^/api/v1/users$"}]
      - mixed-case method "get" → Method "GET"
      - mixed-case "  PoSt  " (whitespace) → Method "POST"
      - empty method, valid path → empty slice (drop entry per CONTEXT decisions)
      - empty path (Url=""), GET → [{Method: "GET", Path: "^/$"}]
      - path with query "/x?a=b" → Path "^/x$" (query stripped)
      - path with fragment "/x#frag" → Path "^/x$" (fragment stripped)
      - path with regex metachar "/api/v1.0/users" → Path uses regexp.QuoteMeta so "." escaped: "^/api/v1\\.0/users$"
      - full URL "http://host/path?q=1" → Path "^/path$"

    normalizeHTTPMethod cases:
      - "GET" → "GET"
      - "get" → "GET"
      - "  PoSt  " → "POST"
      - "" → ""

    HTTP-05 anti-feature lint test (TestExtractHTTPRules_NeverEmitsHeaders):
      - Construct Flow with HTTP record carrying non-empty Headers slice
      - Assert every returned PortRuleHTTP entry has Headers == nil, Host == "", HostExact == "", HeaderMatches == nil
      - Even a "header-rich" input must produce header-less output

    Path-anchoring property test (TestExtractHTTPRules_PathAnchored):
      - For each test path, compile the generated regex via regexp.MustCompile
      - Assert it matches the literal observed path
      - Assert it does NOT match path with prefix ("/evil"+path)
      - Assert it does NOT match path with suffix (path+"/extra")
  </behavior>
  <action>
    Create `pkg/policy/l7_test.go`. Use table-driven subtests with descriptive names. Follow existing test conventions in the package (see `pkg/policy/dedup_test.go` if present, or `pkg/policy/merge_test.go`).

    Build synthetic *flowpb.Flow values inline; the only field needed is Flow.L7 = &flowpb.Layer7{Record: &flowpb.Layer7_Http{Http: &flowpb.HTTP{Method: ..., Url: ...}}}.

    Tests MUST compile but fail at runtime (functions do not yet exist → use a pre-Task-2 commit that adds stub signatures returning nil to allow compile-then-fail; OR commit failing-to-compile tests and let RED be a compile error). Choose: pre-add stub `extractHTTPRules(f *flowpb.Flow) []api.PortRuleHTTP { return nil }` and `normalizeHTTPMethod(s string) string { return s }` in `pkg/policy/l7.go` so tests compile and fail at runtime — clean RED state.

    Commit: `test(08-01): add failing tests for HTTP extraction primitives`
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg &amp;&amp; go test ./pkg/policy/ -run 'TestExtractHTTPRules|TestNormalizeHTTPMethod' -v 2>&amp;1 | grep -E 'FAIL|PASS' | head -40</automated>
  </verify>
  <done>Tests exist, compile, and FAIL when run. Stub functions in l7.go so package builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement extractHTTPRules + normalizeHTTPMethod (GREEN)</name>
  <files>pkg/policy/l7.go</files>
  <behavior>
    All Task 1 tests pass. Implementation MUST:
      - Return nil/empty slice on nil-safe paths (no panics)
      - Use net/url.Parse to extract path from Url (handles full URLs and bare paths)
      - Strip query and fragment via url.URL.Path (already does this)
      - Apply regexp.QuoteMeta then wrap with ^ ... $
      - Empty path → "^/$"
      - normalizeHTTPMethod: strings.ToUpper(strings.TrimSpace(s))
      - Drop entries when normalized method is empty (return slice without that entry)
      - NEVER populate Host, HostExact, Headers, HeaderMatches on the returned PortRuleHTTP
  </behavior>
  <action>
    Implement the helpers in `pkg/policy/l7.go`. Replace the stubs from Task 1.

    File-level godoc block MUST document the HTTP-05 anti-feature contract: "Headers, Host, HostExact and HeaderMatches are intentionally NEVER populated by extractHTTPRules. See REQUIREMENTS.md HTTP-05 — emitting these would risk leaking Authorization/Cookie tokens into committed policy YAML."

    Use only stdlib (`net/url`, `regexp`, `strings`) plus `flowpb "github.com/cilium/cilium/api/v1/flow"` and `"github.com/cilium/cilium/pkg/policy/api"`.

    The exported surface is package-private — both helpers lowercase. Per CONTEXT decisions: "write the helper as a package-private normalizeHTTPMethod ... internal contract".

    Commit: `feat(08-01): implement HTTP L7 extraction primitives`
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg &amp;&amp; go test ./pkg/policy/ -run 'TestExtractHTTPRules|TestNormalizeHTTPMethod' -v &amp;&amp; go vet ./pkg/policy/...</automated>
  </verify>
  <done>All Task 1 tests pass GREEN. `go vet ./...` clean. `go test ./...` clean (no regressions in other packages).</done>
</task>

</tasks>

<verification>
1. `go test ./pkg/policy/ -run 'TestExtractHTTPRules|TestNormalizeHTTPMethod' -v` — all green.
2. `go test ./...` — full suite green (zero behavior change to builder/merge — l7.go is unreferenced from production code at end of this plan).
3. `go vet ./...` — clean.
4. Phase 7's `TestReplay_L7FlagByteStable` still passes (no codegen change yet).
5. Manual grep: `rg 'Headers:|Host:|HostExact:' pkg/policy/l7.go` returns no matches in production code (only the godoc anti-feature comment).
</verification>

<success_criteria>
- HTTP-02 enforced via `normalizeHTTPMethod` unit tests (mixed case → uppercase).
- HTTP-03 enforced via path-anchoring property test (regex compiles, matches only literal observed path).
- HTTP-05 enforced via writer-side lint test (no Headers/Host/HostExact ever populated).
- Pure functions; no consumers yet (Plan 08-02 wires them into BuildPolicy).
- Byte-stability for `--l7=false` PRESERVED (this plan does not modify builder/merge/dedup).
</success_criteria>

<output>
After completion, create `.planning/phases/08-http-l7-generation/08-01-SUMMARY.md` summarizing: helpers introduced, test cases covered, anti-feature contract, and that no production codepath calls the new helpers yet (Plan 08-02 integrates).
</output>
