---
phase: 08-http-l7-generation
plan: 02
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - pkg/policy/builder.go
  - pkg/policy/attribution.go
  - pkg/policy/builder_l7_test.go
autonomous: true
requirements: [HTTP-01, HTTP-04]
must_haves:
  truths:
    - "BuildPolicy attaches L7 HTTP rules to matching L4 PortRule when AttributionOptions.L7Enabled is true and Flow.L7.Http is non-nil"
    - "Multiple distinct (method, path) observations on same (src, dst, port) merge into ONE PortRule with combined Rules.HTTP slice (HTTP-04)"
    - "Multiple ports produce separate PortRule entries; HTTP rules attach to matching port only"
    - "L7Enabled=false produces byte-identical CNP YAML to v1.1 for any input (including L7-bearing flows)"
    - "RuleAttribution.RuleKey.L7 is populated with Protocol=http + Method + Path so two rules differing only in L7 do not dedup"
  artifacts:
    - path: "pkg/policy/builder.go"
      provides: "BuildPolicy with L7 codegen branch"
      contains: "L7Enabled"
    - path: "pkg/policy/attribution.go"
      provides: "AttributionOptions with L7Enabled field"
      contains: "L7Enabled bool"
    - path: "pkg/policy/builder_l7_test.go"
      provides: "TDD coverage of HTTP-01 + HTTP-04 + byte-stability"
  key_links:
    - from: "pkg/policy/builder.go::groupFlows"
      to: "pkg/policy/l7.go::extractHTTPRules"
      via: "function call when opts.L7Enabled && f.GetL7().GetHttp() != nil"
      pattern: "extractHTTPRules"
    - from: "pkg/policy/builder.go"
      to: "PortRule.Rules"
      via: "attach *api.L7Rules{HTTP: ...} to matching PortRule"
      pattern: "Rules:\\s*&api\\.L7Rules"
    - from: "RuleKey.L7"
      to: "L7Discriminator{Protocol: http, HTTPMethod, HTTPPath}"
      via: "ruleKeyFor sets L7 when http rules present"
      pattern: "L7Discriminator\\{"
---

<objective>
Wire the Plan 08-01 extractor into BuildPolicy so that `Flow.L7.Http` records become `toPorts[].rules.http[]` blocks on the matching L4 port entry, with proper merging into a single PortRule per (port, protocol) tuple.

Purpose: Lights up HTTP-01 (HTTP rule emission) and HTTP-04 (single-PortRule merge). Adds `L7Enabled` to `AttributionOptions` so the codegen branch is gated — when off, output is byte-identical to v1.1.
Output: Modified `BuildPolicy` and supporting `peerRules` structure carry HTTP rules per (port, protocol). `AttributionOptions.L7Enabled` flag gates the path. RuleKey carries L7 discriminator end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-http-l7-generation/08-CONTEXT.md
@.planning/phases/08-http-l7-generation/08-01-PLAN.md
@pkg/policy/builder.go
@pkg/policy/merge.go
@pkg/policy/attribution.go
@pkg/policy/dedup.go
@pkg/policy/l7.go

<interfaces>
<!-- Existing builder structures and Phase 7 merge/normalize. -->

From pkg/policy/builder.go:
```go
type peerRules struct {
    ports      []api.PortProtocol
    icmpFields []api.ICMPField
    seen       map[string]struct{}
    attrib     map[string]*RuleAttribution
}

func (pr *peerRules) addFlow(fp *flowProto)
func ingressRulesFrom(common api.IngressCommonRule, ports []api.PortProtocol, icmps []api.ICMPField) []api.IngressRule
func egressRulesFrom(common api.EgressCommonRule, ports []api.PortProtocol, icmps []api.ICMPField) []api.EgressRule
```

From pkg/policy/attribution.go:
```go
type AttributionOptions struct {
    MaxSamples int
    // EXTEND in this plan: L7Enabled bool
}

type L7Discriminator struct {
    Protocol     string
    HTTPMethod   string
    HTTPPath     string
    DNSMatchName string
}
```

From pkg/policy/merge.go (Phase 7) — already preserves Rules across merges:
- mergePortRules groups by (port, protocol); union-merges Rules.HTTP via httpRuleKey.

From pkg/policy/dedup.go (Phase 7) — already sorts L7:
- normalizeRule → sortL7Rules sorts HTTP by (Method, Path).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing builder tests for HTTP attachment + merge + byte-stability</name>
  <files>pkg/policy/builder_l7_test.go</files>
  <behavior>
    Test cases — all MUST initially FAIL:

    TestBuildPolicy_L7Disabled_ByteIdentical:
      - Build flows: 1 L4-only flow + 1 flow carrying Flow.L7.Http (GET /api/users)
      - Run BuildPolicy with AttributionOptions{L7Enabled: false}
      - Run BuildPolicy with same flows, L7-bearing flow STRIPPED of L7 (vanilla v1.1 input)
      - Assert YAML-marshaled CNPs are byte-identical
      - Critical: even with L7Enabled=false, presence of L7 records on flows must NOT change output

    TestBuildPolicy_L7Enabled_SingleHTTPRule:
      - Build flows: ingress flow on port 80/TCP carrying GET /api/v1/users
      - Run BuildPolicy with L7Enabled=true
      - Assert generated CNP has exactly one Ingress rule
      - Assert that ingress rule's ToPorts has one PortRule with Ports=[{80,TCP}]
      - Assert PortRule.Rules != nil and len(Rules.HTTP) == 1
      - Assert Rules.HTTP[0] == {Method: "GET", Path: "^/api/v1/users$"}

    TestBuildPolicy_L7Enabled_MultiHTTPRule_SamePort_SinglePortRule:
      - HTTP-04 invariant. Build 3 flows on same (src, dst, port=80/TCP):
        GET /api/users, POST /api/users, GET /healthz
      - Assert ToPorts has exactly ONE PortRule (not three)
      - Assert that PortRule.Rules.HTTP has 3 entries (after Phase 7 normalize sorts them)
      - Methods/paths after sort: GET ^/api/users$, GET ^/healthz$, POST ^/api/users$

    TestBuildPolicy_L7Enabled_MultiPort_SeparatePortRules:
      - Flows on port 80 (HTTP) AND port 443 (HTTPS, also carries HTTP record)
      - Assert ToPorts has TWO PortRule entries, one per port, each with its own Rules.HTTP

    TestBuildPolicy_L7Enabled_RuleKeyDiscriminator:
      - Build 2 flows on same (src, dst, port=80/TCP): GET /a and POST /a
      - Run BuildPolicy with MaxSamples=1, L7Enabled=true
      - Assert returned []RuleAttribution has 2 distinct entries (not deduplicated)
      - Assert their RuleKey.L7 fields differ (Protocol="http", method/path differ)

    TestBuildPolicy_L7Enabled_NoL7RecordsOnFlow_NoHTTPBlock:
      - Build flows on port 80, no Flow.L7 set
      - Run with L7Enabled=true
      - Assert PortRule.Rules == nil (no empty rules.http: [] block)

    TestBuildPolicy_L7Enabled_PartialL7_EmptyMethodSkipped:
      - Build flow with L7.Http but Method=""
      - Run with L7Enabled=true on port 80
      - Assert PortRule.Rules == nil (extractHTTPRules drops empty-method entries → no rules → no Rules)
  </behavior>
  <action>
    Create `pkg/policy/builder_l7_test.go`. Build *flowpb.Flow with full L4 (TCP DestinationPort) plus optional Flow.L7. Helper `mkHTTPFlow(srcLabels, dstLabels []string, port uint32, method, url string) *flowpb.Flow`.

    Use `pkg/policy/normalizeRule` (from Phase 7) before YAML-marshal in byte-stability tests so list-ordering is deterministic.

    For byte-stability: marshal both CNPs via sigs.k8s.io/yaml and compare bytes (mirrors approach in cmd/cpg/replay_test.go).

    Tests MUST compile; they fail because BuildPolicy currently ignores L7 entirely. AttributionOptions does not yet have L7Enabled — add as field in this same TDD red commit (compile fix) BUT have BuildPolicy ignore it; tests fail at assertions.

    Commit: `test(08-02): add failing builder tests for HTTP L7 attachment`
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg &amp;&amp; go build ./pkg/policy/ &amp;&amp; go test ./pkg/policy/ -run TestBuildPolicy_L7 -v 2>&amp;1 | grep -E 'FAIL|PASS' | head -40</automated>
  </verify>
  <done>Tests compile and FAIL on assertions (not compile errors). AttributionOptions.L7Enabled field exists but BuildPolicy still ignores it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement L7 attachment in BuildPolicy + RuleKey L7 discriminator (GREEN)</name>
  <files>pkg/policy/builder.go, pkg/policy/attribution.go</files>
  <behavior>
    All Task 1 tests pass. Implementation:
      - `peerRules` gains `httpRules map[string][]api.PortRuleHTTP` (key: "<port>/<proto>") and `httpSeen map[string]map[string]struct{}` for in-bucket dedup.
      - `groupFlows` calls `extractHTTPRules(f)` only when `opts.L7Enabled && f.GetL7().GetHttp() != nil`. Resulting rules are appended to `httpRules[portProtoKey]`, deduped by `httpRuleKey` (already exists in merge.go).
      - `ingressRulesFrom`/`egressRulesFrom` gain a parallel `httpByPort map[string][]api.PortRuleHTTP` argument. When a port matches a key with non-empty HTTP rules, the constructed PortRule gets `Rules: &api.L7Rules{HTTP: httpByPort[key]}`. Single PortRule per (port, proto) — HTTP-04.
      - `recordAttribution` accepts an optional L7Discriminator and stores it on RuleKey when HTTP rules are emitted for that flow. New rule-key entries per (method, path) — two rules differing only by L7 do NOT dedup.

    Critical: when `opts.L7Enabled == false`, `httpRules` map stays nil/empty → ingressRulesFrom/egressRulesFrom take the v1.1 codepath (no Rules attached) → byte-identical YAML.
  </behavior>
  <action>
    Edit `pkg/policy/attribution.go`:
      - Add `L7Enabled bool` field to `AttributionOptions` struct (with godoc: "When true, BuildPolicy invokes the HTTP L7 codegen branch. When false (default), L7 records on flows are ignored — output is byte-identical to v1.1.").

    Edit `pkg/policy/builder.go`:
      - Extend `peerRules` with `httpRules map[string][]api.PortRuleHTTP` and `httpSeen map[string]map[string]struct{}`. Initialize in `newPeerRules`.
      - Add helper `(pr *peerRules) addHTTPRules(portProtoKey string, rules []api.PortRuleHTTP)` that dedups via `httpRuleKey` (re-use the helper from merge.go — same package).
      - In `groupFlows`, after `addFlow(fp)` and `recordAttribution(...)`, branch:
        ```go
        if opts.L7Enabled {
            if h := f.GetL7().GetHttp(); h != nil {
                rules := extractHTTPRules(f)
                if len(rules) > 0 {
                    portKey := strconv.FormatUint(uint64(fp.port), 10) + "/" + string(fp.proto)
                    bucket.addHTTPRules(portKey, rules)
                    // Record per-(method, path) attribution so RuleKey discriminator differs
                    for _, r := range rules {
                        l7 := &L7Discriminator{Protocol: "http", HTTPMethod: r.Method, HTTPPath: r.Path}
                        bucket.recordAttribution(ruleKeyForL7(direction, peer, fp, l7), f, opts.MaxSamples)
                    }
                }
            }
        }
        ```
        (Replace `bucket` with the actual entity/cidr/endpoint bucket for the flow.)
      - Add `ruleKeyForL7(direction, peer, fp, l7)` mirroring `ruleKeyFor` but with `L7: l7`.
      - Refactor `ingressRulesFrom` / `egressRulesFrom` to accept `httpByPort map[string][]api.PortRuleHTTP`. When constructing the PortRule, set `Rules: &api.L7Rules{HTTP: httpByPort[key]}` only when non-empty.
      - Update all call sites in `buildIngressRules` / `buildEgressRules` to pass `bucket.httpRules`.

    Reference: per D-04 in 08-CONTEXT.md decisions — `pkg/hubble/aggregator.go::AggKey` does NOT change (L7 is a property of the port-rule inside the bucket, not the bucket itself).

    Commit: `feat(08-02): wire HTTP L7 codegen into BuildPolicy (HTTP-01, HTTP-04)`
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg &amp;&amp; go test ./pkg/policy/ -v &amp;&amp; go vet ./pkg/policy/...</automated>
  </verify>
  <done>All Task 1 tests GREEN. All existing tests in pkg/policy/ still GREEN (no regression on builder, merge, dedup, or attribution). go vet clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-package regression — Phase 7 byte-stability test stays green</name>
  <files>pkg/policy/builder_l7_test.go</files>
  <behavior>
    Add one final regression test in builder_l7_test.go:

    TestBuildPolicy_L7Enabled_NoL7RecordsAcrossAllFlows_ByteIdenticalToL7Disabled:
      - Build flows: ZERO carry Flow.L7 (mirrors what fixtures/flows/small.jsonl looks like)
      - Run BuildPolicy(L7Enabled=true) and BuildPolicy(L7Enabled=false)
      - Marshal both CNPs to YAML after normalizeRule
      - Assert byte-identical
      - This is the unit-level mirror of cmd/cpg/replay_test.go::TestReplay_L7FlagByteStable
  </behavior>
  <action>
    Append the test to builder_l7_test.go. No production code changes — should pass on the implementation from Task 2.

    Run the full test suite to confirm no regression in cmd/cpg/replay_test.go::TestReplay_L7FlagByteStable.

    Commit: `test(08-02): assert byte-stability for L7-empty input across L7Enabled toggle`
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg &amp;&amp; go test ./... 2>&amp;1 | tail -30</automated>
  </verify>
  <done>Full repo `go test ./...` clean. Phase 7's TestReplay_L7FlagByteStable still passes.</done>
</task>

</tasks>

<verification>
1. `go test ./pkg/policy/ -v` — all tests green, including TestBuildPolicy_L7Enabled_* and TestBuildPolicy_L7Disabled_ByteIdentical.
2. `go test ./...` — full repo green; Phase 7's `TestReplay_L7FlagByteStable` (cmd/cpg) still passes.
3. `go vet ./...` — clean.
4. Manual: `rg 'L7Enabled' pkg/policy/` shows the field defined and read by groupFlows; `rg 'L7Discriminator' pkg/policy/` shows it populated when HTTP rules emit.
5. Pipeline integration deferred to Plan 08-03 — `pkg/hubble/pipeline.go::PipelineConfig.L7Enabled` is NOT yet forwarded to AttributionOptions in this plan.
</verification>

<success_criteria>
- HTTP-01: BuildPolicy emits `toPorts[].rules.http[]` for L7-bearing flows when L7Enabled=true.
- HTTP-04: Multiple (method, path) on same (port, proto) merge into a single PortRule with combined HTTP slice.
- Byte-stability under L7Enabled=false preserved (verified by both unit test and unchanged Phase 7 replay test).
- RuleKey carries L7 discriminator so attribution doesn't collide on (method, path).
- No changes to AggKey (per D-04 architecture decision).
</success_criteria>

<output>
After completion, create `.planning/phases/08-http-l7-generation/08-02-SUMMARY.md` summarizing: peerRules extension, AttributionOptions.L7Enabled, ruleKeyForL7 helper, and the byte-stability proof points.
</output>
