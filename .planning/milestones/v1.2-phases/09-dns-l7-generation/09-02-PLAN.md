---
phase: 09-dns-l7-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - pkg/hubble/aggregator.go
  - pkg/hubble/aggregator_test.go
  - pkg/hubble/evidence_writer.go
  - pkg/hubble/evidence_writer_test.go
  - pkg/hubble/pipeline_l7_dns_test.go
autonomous: true
requirements:
  - DNS-01
  - DNS-02

must_haves:
  truths:
    - "Aggregator increments L7DNSCount whenever an observed flow carries Flow.L7.Dns (regardless of L7Enabled — counter is diagnostic)."
    - "VIS-01 empty-records warning gate (in pipeline.go Finalize) reads L7HTTPCount + L7DNSCount and fires only when BOTH are zero with --l7 set. Phase 8 already wired the DNS branch on the gate; Phase 9 makes it real because the counter now actually moves."
    - "evidenceWriter.convert populates RuleEvidence.L7 = &L7Ref{Protocol:\"dns\", DNSMatchName: <name>} when RuleAttribution.Key.L7 carries Protocol==\"dns\" — Phase 8 left a TODO for this branch; this plan removes the TODO."
    - "An end-to-end pipeline integration test feeds synthetic Flow.L7.Dns flows through Aggregator → BuildPolicy and asserts: (a) generated CNP has toFQDNs + dns rules + kube-dns companion (assertHasKubeDNSCompanion), (b) emitted RuleEvidence has L7Ref{Protocol:\"dns\", DNSMatchName: <name>}, (c) SessionStats.L7DNSCount > 0."
  artifacts:
    - path: "pkg/hubble/aggregator.go"
      provides: "DNS counter increment in Aggregator.Run"
      contains: "a.l7DNSCount++"
    - path: "pkg/hubble/evidence_writer.go"
      provides: "DNS branch in convert() — replaces Phase 8 TODO"
      contains: "DNSMatchName: a.Key.L7.DNSMatchName"
    - path: "pkg/hubble/pipeline_l7_dns_test.go"
      provides: "End-to-end DNS L7 integration test"
      contains: "TestPipeline_L7DNS"
  key_links:
    - from: "pkg/hubble/aggregator.go (Run loop)"
      to: "Flow.L7.Dns observation"
      via: "if f.GetL7().GetDns() != nil { a.l7DNSCount++ }"
      pattern: "GetDns\\(\\) != nil"
    - from: "pkg/hubble/evidence_writer.go (convert)"
      to: "evidence.L7Ref Protocol=dns"
      via: "switch on a.Key.L7.Protocol"
      pattern: "case \"dns\""
    - from: "pipeline.go Finalize VIS-01 gate"
      to: "agg.L7DNSCount()"
      via: "already wired in Phase 8 (no code change here, but the counter now moves)"
      pattern: "agg.L7HTTPCount\\(\\)\\+agg.L7DNSCount\\(\\)"
---

<objective>
Make Phase 9 plan 01's DNS codegen visible end-to-end at the pipeline + evidence layer:
1. Aggregator counts DNS records (parallel to the L7HTTPCount increment shipped in Phase 8). VIS-01's gate in pipeline.go already sums HTTP + DNS — Phase 8 declared the counter, this plan starts incrementing it.
2. evidenceWriter.convert fills the DNS branch (Phase 8 left a `TODO(phase-9)`). After this plan, RuleEvidence written to disk for DNS-bearing rules carries L7Ref{Protocol:"dns", DNSMatchName}.
3. New integration test pipeline_l7_dns_test.go feeds a synthetic DNS flow stream through Aggregator → BuildPolicy → evidenceWriter and asserts the full DNS shape end-to-end (companion + dns rules + L7Ref).

Purpose: Closes the integration gap between 09-01 (policy layer) and 09-03 (CLI layer). Without this, BuildPolicy emits the right shape but evidence loses the DNS attribution and VIS-01 misfires for DNS-only workloads.

Output: aggregator.go (DNS counter), evidence_writer.go (DNS branch), aggregator_test.go (counter test), evidence_writer_test.go (DNS conversion test), pipeline_l7_dns_test.go (integration). go test ./pkg/hubble/... green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dns-l7-generation/09-CONTEXT.md
@.planning/phases/09-dns-l7-generation/09-01-PLAN.md

@pkg/hubble/aggregator.go
@pkg/hubble/evidence_writer.go
@pkg/hubble/pipeline.go
@pkg/evidence/schema.go

# 09-01 SUMMARY (read at execution time after plan 01 lands).
# Provides: extractDNSQuery, ensureKubeDNSCompanion, assertHasKubeDNSCompanion test helper.

<interfaces>
From pkg/hubble/aggregator.go:
```go
type Aggregator struct {
    // ...
    l7HTTPCount uint64
    l7DNSCount  uint64  // declared Phase 8, never incremented
    // ...
}
func (a *Aggregator) L7DNSCount() uint64 { return a.l7DNSCount }

// Inside Run loop, on each flow received:
if f.GetL7().GetHttp() != nil { a.l7HTTPCount++ }
// MISSING: parallel block for f.GetL7().GetDns() — this plan adds it.
```

From pkg/hubble/evidence_writer.go convert():
```go
if a.Key.L7 != nil {
    switch a.Key.L7.Protocol {
    case "http":
        re.L7 = &evidence.L7Ref{Protocol: "http", HTTPMethod: ..., HTTPPath: ...}
    case "dns":
        // TODO(phase-9): emit L7Ref{Protocol:"dns", DNSMatchName: a.Key.L7.DNSMatchName}.
    }
}
```

From pkg/evidence/schema.go:
```go
type L7Ref struct {
    Protocol     string `json:"protocol"`
    HTTPMethod   string `json:"http_method,omitempty"`
    HTTPPath     string `json:"http_path,omitempty"`
    DNSMatchName string `json:"dns_matchname,omitempty"`
}
```

From pkg/policy (Phase 9 plan 01 added):
- extractDNSQuery, ensureKubeDNSCompanion, assertHasKubeDNSCompanion (test helper, exported via package _test or moved to a helper file usable cross-pkg if needed — defer to plan 09-01's actual layout).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggregator DNS counter + evidence DNS branch + unit tests</name>
  <files>pkg/hubble/aggregator.go, pkg/hubble/aggregator_test.go, pkg/hubble/evidence_writer.go, pkg/hubble/evidence_writer_test.go</files>
  <behavior>
    aggregator_test.go (extend or new test):
    - Aggregator.Run consumes a flow with Flow.L7.Dns.Query="api.example.com." → after the channel closes and Run returns, `agg.L7DNSCount() == 1`. agg.L7HTTPCount() unchanged.
    - Aggregator.Run consumes a flow with both Flow.L7.Http AND Flow.L7.Dns set (rare but possible) → both counters increment by 1.
    - DNS counter increments BEFORE keyFromFlow's skip check (parallel to existing HTTP counter behavior — diagnostic counter is independent of bucketing).
    - L7Enabled = false → counters STILL increment (counter is diagnostic, not gated by L7Enabled). Same contract as L7HTTPCount.

    evidence_writer_test.go (new test):
    - convert(RuleAttribution{Key: RuleKey{L7: &L7Discriminator{Protocol:"dns", DNSMatchName:"api.example.com"}, ...}, ...}) → returns RuleEvidence with re.L7 = &evidence.L7Ref{Protocol:"dns", DNSMatchName:"api.example.com"} and HTTPMethod/HTTPPath empty (omitempty in JSON).
    - convert with Key.L7.Protocol == "" or unrecognized → re.L7 stays nil (defensive — neither HTTP nor DNS branch hits).
    - Round-trip JSON marshal: marshal RuleEvidence with DNS L7Ref → unmarshal → DNS fields preserved, HTTP fields absent.
  </behavior>
  <action>
    1. In pkg/hubble/aggregator.go inside `Aggregator.Run`'s flow-receive case, ADD the parallel DNS increment immediately after the HTTP one:
       ```go
       if f.GetL7().GetHttp() != nil {
           a.l7HTTPCount++
       }
       if f.GetL7().GetDns() != nil {
           a.l7DNSCount++
       }
       ```
       Update the comment block above the HTTP increment to mention both counters power VIS-01.
       Also fix the L7DNSCount() doc comment: replace "Always 0 in Phase 8." with "Diagnostic counter; populated regardless of L7Enabled (Phase 9 wires the increment)."

    2. In pkg/hubble/evidence_writer.go inside `convert()`, REPLACE the `TODO(phase-9)` line with the real DNS branch:
       ```go
       case "dns":
           re.L7 = &evidence.L7Ref{
               Protocol:     "dns",
               DNSMatchName: a.Key.L7.DNSMatchName,
           }
       ```

    3. Add aggregator_test.go test cases per <behavior>. Use the existing test pattern (see existing aggregator_test.go for the runner shape; if there isn't a Run-level test today, build one with a stub flushingTracker and a buffered channel feeding flows then closed).

    4. Add evidence_writer_test.go test cases per <behavior>. If evidence_writer_test.go exists already, append; otherwise create a fresh _test.go in the hubble package.

    5. Run `go test ./pkg/hubble/...` — RED on tests before code, GREEN after.

    6. Commit: `feat(hubble): aggregator DNS counter + evidence DNS branch (DNS-01, VIS-01 gate)`.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg && go test ./pkg/hubble/... -run "TestAggregator|TestEvidenceWriter" -v</automated>
  </verify>
  <done>
    Aggregator increments l7DNSCount on every DNS-bearing flow. evidenceWriter.convert produces L7Ref{Protocol:"dns", DNSMatchName:...} for DNS attribution. Existing tests still pass. No Phase 8 byte-stability regression (HTTP counter behavior unchanged, evidence HTTP branch unchanged).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end pipeline integration test for DNS L7</name>
  <files>pkg/hubble/pipeline_l7_dns_test.go</files>
  <behavior>
    pipeline_l7_dns_test.go::TestPipeline_L7DNS_GenerationAndEvidence:
    - Build a stub flowsource.FlowSource that yields:
      a. One Flow with Source={ns=production, labels=[k8s:app=api]}, Destination={CIDR external 8.8.8.8 OR kube-dns labels}, L4.UDP.DestinationPort=53, Flow.L7.Dns.Query="api.example.com.", Verdict=DROPPED, Direction=EGRESS.
      b. Optionally a second flow with Query="www.example.org." for the same source workload.
    - Run RunPipelineWithSource with PipelineConfig{L7Enabled: true, EvidenceEnabled: true, OutputDir: t.TempDir(), EvidenceDir: t.TempDir(), FlushInterval: 50ms, ...}.
    - After the run completes, assertions:
      a. The generated YAML file under OutputDir contains `toFQDNs:` with `matchName: api.example.com` (no trailing dot).
      b. The YAML contains a kube-dns companion egress rule (read+parse the YAML, run assertHasKubeDNSCompanion-equivalent on the parsed CNP, OR grep for `k8s-app: kube-dns` + `port: "53"`).
      c. The YAML contains NO `matchPattern:` substring (DNS-03).
      d. The evidence JSON for the workload (ResolvePolicyPath) contains a rule with `"l7": {"protocol":"dns","dns_matchname":"api.example.com"}`.
      e. SessionStats logged values: L7DNSCount >= 1.
    - Use the existing flowsource stub pattern from pkg/hubble's other pipeline tests (cf. existing replay_test.go or aggregator_test.go).
  </behavior>
  <action>
    1. CREATE pkg/hubble/pipeline_l7_dns_test.go with TestPipeline_L7DNS_GenerationAndEvidence per <behavior>.

    2. Build helpers (or reuse from pkg/hubble's existing test files):
       - `newDNSFlow(srcLabels []string, srcNS, dstIP, query string) *flowpb.Flow` — emits a DROPPED EGRESS flow with Flow.L7.Dns + L4.UDP/53.
       - Use a minimal stub flowsource.FlowSource (look for one in existing pipeline tests; if absent, write one inline in the _test.go).

    3. Read the generated YAML from disk via os.ReadFile, unmarshal via sigs.k8s.io/yaml into ciliumv2.CiliumNetworkPolicy, then run the in-package equivalent of assertHasKubeDNSCompanion. Since assertHasKubeDNSCompanion is in pkg/policy (test helper), import it OR replicate the same checks inline (cleaner: replicate inline, ~10 lines, since cross-package test helpers add coupling).

    4. Read the evidence JSON via evidence.NewReader(...).Read(...) and assert pe.Rules contains one with L7.Protocol == "dns" + DNSMatchName == "api.example.com".

    5. Run: `go test ./pkg/hubble/... -run TestPipeline_L7DNS_GenerationAndEvidence -v`.

    6. Commit: `test(hubble): end-to-end DNS L7 integration test (DNS-01, DNS-02)`.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg && go test ./pkg/hubble/... -run TestPipeline_L7DNS_GenerationAndEvidence -v</automated>
  </verify>
  <done>
    End-to-end test passes: synthetic DNS flow → Aggregator → BuildPolicy → output writer → evidence writer all produce the expected DNS shape. assertHasKubeDNSCompanion-equivalent passes on the on-disk YAML. Evidence JSON has the dns L7Ref. SessionStats.L7DNSCount > 0.
  </done>
</task>

</tasks>

<verification>
1. `go test ./pkg/hubble/... -v` green.
2. `go test ./pkg/policy/... -v` still green (no regression from 09-01).
3. `go vet ./...` clean.
4. `grep -n "TODO(phase-9)" pkg/hubble/evidence_writer.go` returns zero matches.
5. The Phase 8 VIS-01 warning logic in pipeline.go (line ~199) is unchanged but now actually fires correctly when DNS-only workloads have no L7 — observed via the integration test if you add an inverse case (optional).
</verification>

<success_criteria>
- DNS-01 (integration layer): RuleEvidence carries L7Ref{Protocol:"dns", DNSMatchName} for DNS-bearing rules.
- DNS-02 (end-to-end): The companion rule is present in the on-disk YAML produced by the pipeline (verified via the integration test parsing the file).
- VIS-01 gate becomes truthful: L7DNSCount actually moves on DNS observations, so the empty-records check fires only when both HTTP and DNS counts are zero.
- No regression in Phase 7 / Phase 8 tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dns-l7-generation/09-02-SUMMARY.md` capturing:
- aggregator.go DNS counter increment site.
- evidence_writer.go DNS branch (Phase 8 TODO removed).
- pipeline_l7_dns_test.go integration test scope and shape.
- Notes: VIS-01 gate already wired in Phase 8 — this plan makes it accurate.
</output>
