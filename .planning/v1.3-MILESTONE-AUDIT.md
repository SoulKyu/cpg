---
milestone: v1.3
milestone_name: Cluster Health Surfacing
audited: 2026-04-26
status: passed
verification_model: unit-tests-and-plan-summaries
note: per-phase gsd-verifier was skipped during autonomous run; verification is anchored on (a) atomic TDD-first commits per task with failing-test-then-impl pattern, (b) full-suite green (`go test ./... -race`), (c) plan SUMMARY.md acceptance criteria
scores:
  requirements: 13/13
  phases: 4/4
  plans: 7/7
  tests: 418/418 across 10 packages
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: all
    items:
      - "Future v1.4: CI script that diffs pkg/dropclass taxonomy against latest Cilium proto on go.mod bump"
      - "Future v1.4: AUTH_REQUIRED dual-path SPIRE inspection (currently classified as Policy with needs_review annotation)"
      - "Future v1.4+: OpenMetrics/Prometheus export (deferred — gather field feedback first)"
      - "Future v1.4+: Semantic policy intersection ('would existing CNP already allow this?')"
deferred:
  - "Per-flow timestamps in cluster-health.json (counters suffice)"
  - "ANSI color when stdout is a TTY (v1.4 polish)"
  - "--include-noise flag (Noise class currently silently discarded)"
nyquist:
  status: skipped
  reason: research_enabled=true but no per-phase RESEARCH.md generated — milestone-level research SUMMARY.md provided full context; Nyquist artifacts not applicable to this autonomous run
---

# Milestone v1.3 Audit — Cluster Health Surfacing

**Status:** ✅ passed (autonomous-mode verification model)

## Definition of Done

> Distinguish policy drops from infrastructure-level Hubble drops (CT map full, BPF errors, etc.) so cpg only generates CNPs for true policy denials and surfaces cluster-critical issues separately for SRE attention.

**Trigger:** 2026-04-26 production observation — `mmtro-adserver` ingress drop with reason `CT_MAP_INSERTION_FAILED` (Cilium conntrack map full, infra issue) generated a useless `cpg-mmtro-adserver` CNP. v1.3 closes this class of bug.

## Phases Delivered

| Phase | Name | Plans | Plans complete | REQs |
|-------|------|-------|----------------|------|
| 10 | Classifier Core | 2 | 2/2 | CLASSIFY-01, CLASSIFY-02, CLASSIFY-03 |
| 11 | Aggregator Suppression + Health Writer | 2 | 2/2 | HEALTH-01, HEALTH-02, HEALTH-04, HEALTH-05 |
| 12 | Session Summary Block | 1 | 1/1 | HEALTH-03 |
| 13 | Flags + Exit Code | 3 | 3/3 | FILTER-01, FILTER-02, FILTER-03, EXIT-01, EXIT-02 |

**Total:** 4 phases, 8 plans, 13/13 requirements satisfied.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Description | Phase | Plan | Source SUMMARY | Status |
|--------|-------------|-------|------|----------------|--------|
| CLASSIFY-01 | Static drop-reason taxonomy covering all Cilium ≥1.14 enum values | 10 | 10-01 | ✓ | satisfied |
| CLASSIFY-02 | Unknown reasons default to Unknown bucket + dedup WARN | 10 | 10-02 | ✓ | satisfied |
| CLASSIFY-03 | classifierVersion semver embedded in cluster-health.json | 10 | 10-01 | ✓ | satisfied |
| HEALTH-01 | No CNP for infra/transient drops | 11 | 11-01 | ✓ | satisfied |
| HEALTH-02 | cluster-health.json atomic write with counters + remediation hints | 11 | 11-02 | ✓ | satisfied |
| HEALTH-03 | Session summary block with severity sort + top-3 nodes/workloads + path | 12 | 12-01 | ✓ | satisfied |
| HEALTH-04 | --dry-run suppresses cluster-health.json write | 11 | 11-02 | ✓ | satisfied |
| HEALTH-05 | Infra/transient drops still increment flowsSeen counter | 11 | 11-01 | ✓ | satisfied |
| FILTER-01 | --ignore-drop-reason flag (repeatable, comma-separated, case-insensitive) | 13 | 13-01, 13-02 | ✓ | satisfied |
| FILTER-02 | Unknown reason name → flag-parse error with valid reasons listed | 13 | 13-02 | ✓ | satisfied |
| FILTER-03 | WARN on redundant infra/transient reason in --ignore-drop-reason | 13 | 13-02 | ✓ | satisfied |
| EXIT-01 | --fail-on-infra-drops → exit 1 when ≥1 infra drop; default exit 0 | 13 | 13-03 | ✓ | satisfied |
| EXIT-02 | README documents exit codes + CI cron pattern | 13 | 13-03 | ✓ | satisfied |

## Test Coverage

- **Total:** 418 tests across 10 packages, `-race` clean
- **By package:** dropclass (33), hubble (~110 incl. 11+ new healthWriter, 9 summary, 6 ignore-drop-reason), evidence, flowsource, k8s, labels, output, policy, diff, cmd/cpg
- **TDD evidence:** every plan ships a RED commit (failing tests) followed by a GREEN commit (impl) — verifiable via `git log --oneline | grep -E '^(test|feat)\(1[0-3]'`

## Cross-Phase Integration

End-to-end flow verified by code review across phase boundaries:

1. **Phase 10 → 11:** `pkg/dropclass.Classify()` consumed by aggregator gate at `pkg/hubble/aggregator.go` Run() loop position 3 (after ignoreDropReasons + ignoreProtocols filters). `pkg/dropclass.RemediationHint()` consumed by `pkg/hubble/health_writer.go` per-DropEvent. `pkg/dropclass.ClassifierVersion` embedded in `clusterHealthReport.classifier_version`.
2. **Phase 11 → 12:** `healthWriter.Snapshot()` consumed by `PrintClusterHealthSummary()` in `pkg/hubble/summary.go`. SessionStats.InfraDropTotal gates whether the block is printed.
3. **Phase 12 → 13:** `--ignore-drop-reason` filter at aggregator position 1 (highest precedence — user explicit exclusion beats auto-classification). `--fail-on-infra-drops` consumes `SessionStats.InfraDropTotal` returned by `RunPipelineWithSource` and translates to `os.Exit(1)` via `ExitCodeError` sentinel.

## Backward Compatibility

| Promise | Status |
|---------|--------|
| Default cpg behavior (no flags) generates same set of CNPs as v1.2 | ✗ INTENTIONAL CHANGE — cpg now SKIPS infra/transient drops (closing the bug). Policy drops still produce identical CNPs. This is the explicit milestone goal. |
| Default exit code = 0 | ✓ Preserved unconditionally (Pitfall P3). Only opt-in `--fail-on-infra-drops` changes behavior. |
| Existing flags (`--ignore-protocol`, `--dry-run`, `--cluster-dedup`, `--l7`) unchanged | ✓ |
| Evidence schema v2 unchanged | ✓ (v1.3 adds a parallel `cluster-health.json`, does not modify per-policy evidence) |

## Tech Debt & Deferred Items

**Future v1.4+ candidates** (also tracked in REQUIREMENTS.md "Future"):
- CI script to diff pkg/dropclass taxonomy against upstream Cilium proto on go.mod bump
- AUTH_REQUIRED dual-path disambiguation via SPIRE status inspection (currently Policy with `needs_review` annotation)
- OpenMetrics/Prometheus export of drop counters
- Semantic policy intersection (NP-hard, low ROI vs `cpg apply --diff` workflow)
- ANSI color in summary block when stdout is a TTY
- `--include-noise` flag (Noise class currently silently discarded)

## Verification Model Note

This audit uses a lightweight verification model appropriate for the autonomous run:
- **No per-phase gsd-verifier agent run** (each phase's verification is encoded in plan SUMMARY.md acceptance criteria + atomic TDD commits)
- **No Nyquist VALIDATION.md** (research artifacts are milestone-level in `.planning/research/SUMMARY.md`, not per-phase)
- **Anchor:** `go test ./... -race` green at every plan completion + `go build ./...` clean

For higher-rigor sign-off, run `/gsd:verify-work` per phase to perform conversational UAT of the v1.3 features against a live cluster.

---
*Audit generated 2026-04-26 by autonomous /gsd:autonomous run.*
