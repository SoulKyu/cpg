---
phase: 08-http-l7-generation
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - cmd/cpg/replay_test.go
  - README.md
autonomous: true
requirements: [HTTP-01, HTTP-02, HTTP-03, HTTP-04, HTTP-05, VIS-01]
must_haves:
  truths:
    - "End-to-end: cpg replay --l7 testdata/flows/l7_http.jsonl emits CNP YAML with http: block, correct method+path, no headerMatches/host/hostExact"
    - "Phase 7's TestReplay_L7FlagByteStable still passes (--l7 toggle is a no-op when fixture has no L7 records)"
    - "VIS-01 fires when --l7 is set against the empty-L7 fixture"
    - "README contains the #l7-prerequisites anchor placeholder so the VIS-01 hint link works"
  artifacts:
    - path: "cmd/cpg/replay_test.go"
      provides: "End-to-end TestReplay_L7HTTPGeneration + TestReplay_L7VIS01OnEmptyFixture"
    - path: "README.md"
      provides: "L7 prerequisites anchor placeholder for VIS-01 hint link"
      contains: "l7-prerequisites"
  key_links:
    - from: "cmd/cpg/replay_test.go::TestReplay_L7HTTPGeneration"
      to: "testdata/flows/l7_http.jsonl"
      via: "FileSource via newReplayCmd"
      pattern: "l7_http\\.jsonl"
    - from: "README.md"
      to: "VIS-01 warning hint"
      via: "anchor #l7-prerequisites"
      pattern: "l7-prerequisites"
---

<objective>
Lock down Phase 8 with end-to-end replay tests covering all 6 acceptance success criteria, and reserve the README anchor that Phase 9 will populate.

Purpose: Promotes Plan 08-03's pipeline-level integration tests to true CLI-level e2e (binary entrypoint → fixture → on-disk YAML). Confirms HTTP-01..HTTP-05 + VIS-01 are observable through the user-facing surface. Adds the README anchor that VIS-01's hint link references — Phase 9 fills in the actual content.
Output: Two new replay tests (`TestReplay_L7HTTPGeneration`, `TestReplay_L7VIS01OnEmptyFixture`); README placeholder section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-http-l7-generation/08-CONTEXT.md
@.planning/phases/08-http-l7-generation/08-03-PLAN.md
@cmd/cpg/replay_test.go
@cmd/cpg/replay.go

<interfaces>
<!-- Reuse existing replay test scaffolding patterns. -->

From cmd/cpg/replay_test.go:
```go
// initLoggerForTesting(t) — sets the global logger for tests
// newReplayCmd() — returns the cobra command
// walkRelFiles(t, root) — sorted relative file paths
// assertTreesByteEqual / assertTreesSameShape — existing helpers
```

To use observed logger in cmd/cpg test (VIS-01 assertion), either:
- Inject a custom zap.Logger via initLoggerForTesting, OR
- Read pre-existing pattern from pkg/hubble/pipeline_l7_test.go (Plan 08-03) and mirror it in cmd/cpg.

testdata/flows/l7_http.jsonl is committed by Plan 08-03.
testdata/flows/small.jsonl exists (no L7 records).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add e2e replay tests for HTTP generation + VIS-01</name>
  <files>cmd/cpg/replay_test.go</files>
  <action>
    Append two new tests to `cmd/cpg/replay_test.go`:

    TestReplay_L7HTTPGeneration:
      - Set output + evidence dirs via t.TempDir()
      - Run `cpg replay testdata/flows/l7_http.jsonl --output-dir <out> --evidence-dir <ev> --l7 --flush-interval 100ms`
      - Walk output dir, find generated CNP YAML for the api-server workload
      - Assert YAML content (string match):
        - Contains "rules:" followed by "http:"
        - Contains "method: GET" AND "method: POST"
        - Contains `path: ^/api/v1/users$` (regex-anchored, escaped)
        - Contains `path: ^/healthz$`
        - Does NOT contain "headerMatches"
        - Does NOT contain "host:" (be careful: avoid false-positive on Cilium's `hostExact` or unrelated `host` substrings; assert specifically there is no `hostExact:` field and no top-level `host:` field on http rules. A pragmatic regex: `\\bhostExact:` and `\\b\\s+host:` should match zero occurrences.)
      - Read evidence file for api-server, assert at least one rule has L7.Protocol="http" with non-empty HTTPMethod and HTTPPath

    TestReplay_L7VIS01OnEmptyFixture:
      - Use observer logger (zap/zaptest/observer.New) to capture warnings
      - Inject the observed logger into the global `logger` for the duration of the test (use t.Cleanup to restore)
      - Run `cpg replay testdata/flows/small.jsonl --output-dir <out> --evidence-dir <ev> --l7`
      - Assert exactly ONE log entry with message "--l7 set but no L7 records observed"
      - Assert that entry has fields: workloads (non-empty), flows (>0), hint (contains "#l7-prerequisites")
      - Assert generated CNP YAML contains NO "http:" sub-block
      - Critically: this test must coexist with Phase 7's TestReplay_L7FlagByteStable — both run small.jsonl through `--l7=true`, so re-confirm both still pass.

    If injecting the observed logger via the package-level `logger` variable is awkward, an alternative is to extend `initLoggerForTesting(t)` with an optional observed-output mode. Implementation detail at executor's discretion.

    Commit: `test(08-04): end-to-end replay tests for HTTP L7 generation and VIS-01`
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg &amp;&amp; go test ./cmd/cpg/ -run 'TestReplay_L7' -v 2>&amp;1 | tail -40</automated>
  </verify>
  <done>Both new tests GREEN. Phase 7's TestReplay_L7FlagByteStable still GREEN. Full `go test ./...` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add README #l7-prerequisites anchor placeholder</name>
  <files>README.md</files>
  <action>
    Add a new section to README.md (place it logically — after the "Usage" or "Replay" section, wherever workflow docs live; if README has no L7 section yet, add one).

    Section content (minimal — Phase 9 ships the actual content):

    ```markdown
    ## L7 Prerequisites <a id="l7-prerequisites"></a>

    > **Note:** This section is a placeholder reserved by Phase 8. The full
    > two-step workflow (deploy L4 → enable L7 visibility → re-run with
    > `--l7`), starter visibility CNP, and capture-window guidance ship in
    > Phase 9 (v1.2 DNS L7 Generation + Docs).
    >
    > For now: if `cpg generate --l7` (or `cpg replay --l7`) emits the warning
    > `--l7 set but no L7 records observed`, the target workloads are not
    > producing L7 flow records via Hubble. This requires either an existing
    > L7 CiliumNetworkPolicy on those workloads or the (legacy)
    > `policy.cilium.io/proxy-visibility` annotation. See the [Cilium L7
    > visibility docs](https://docs.cilium.io/en/stable/observability/visibility/)
    > until Phase 9 lands.
    ```

    Critical: the HTML anchor `<a id="l7-prerequisites"></a>` MUST be present so the VIS-01 warning's "see README #l7-prerequisites" hint resolves to a real fragment.

    Verify the markdown anchor matches the string in pkg/hubble/pipeline.go's VIS-01 warning hint field exactly: `#l7-prerequisites`.

    Commit: `docs(08-04): reserve README #l7-prerequisites anchor for VIS-01 hint`
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg &amp;&amp; grep -n 'l7-prerequisites' README.md pkg/hubble/pipeline.go</automated>
  </verify>
  <done>README contains the anchor; pipeline.go's VIS-01 warning references the same anchor string.</done>
</task>

</tasks>

<verification>
1. `go test ./cmd/cpg/ -run TestReplay_L7 -v` — TestReplay_L7HTTPGeneration, TestReplay_L7VIS01OnEmptyFixture, and existing TestReplay_L7FlagByteStable all GREEN.
2. `go test ./...` — full repo GREEN.
3. `grep -n 'l7-prerequisites' README.md pkg/hubble/pipeline.go` — both files reference the anchor with the exact same string.
4. Manual sanity: `cpg replay testdata/flows/l7_http.jsonl --output-dir /tmp/x --l7 --no-evidence` produces YAML with http rules; `cpg replay testdata/flows/small.jsonl --output-dir /tmp/y --l7 --no-evidence` logs the VIS-01 warning.
5. `go vet ./...` — clean.
</verification>

<success_criteria>
- HTTP-01..HTTP-05 + VIS-01 verified end-to-end through the `cpg replay --l7` CLI surface.
- Phase 7's byte-stability invariant for `--l7` toggle preserved (L7-empty fixture).
- VIS-01 warning hint links to a real README anchor.
- v1.2 Phase 8 acceptance criteria from ROADMAP all observable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-http-l7-generation/08-04-SUMMARY.md` summarizing: e2e tests added, README anchor reserved, and confirmation that all 6 Phase 8 success criteria are now testable from the CLI.
</output>
