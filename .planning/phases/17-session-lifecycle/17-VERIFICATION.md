---
phase: 17-session-lifecycle
verified: 2026-07-21T08:18:00Z
status: gaps_found
score: 4/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/5
  gaps_closed:
    - "Truth 2 (SESS-03) — WR-01 (old, general case): a genuine, non-context pipeline error (relay reset, auth expiry, etc.) now autonomously transitions Session.State to stopped and surfaces the error via the new Session.pipelineErr atomic slot — closes the original 'capturing forever' defect for that whole error class. Independently re-confirmed: pkg/session/manager.go:219-241, pkg/session/session.go:107-114/218-223, TestManager_PipelineErrorAutonomouslyStopsSession passes under -race."
    - "Truth 4 (SESS-05) — WR-02 (old): Shutdown's s.cancel() now reaches a mid-setup Start() call via context.AfterFunc(sessionCtx, setupCancel) (manager.go:180), so a ctx-observing resolveSetupFn aborts promptly instead of orphaning a tmpdir. Independently re-confirmed via a NEW ctx-inspecting -race test (TestManager_Start_ShutdownCancelsSetupCtx, which blocks on <-setupCtx.Done() — the old TestManager_Start_ShutdownRacesSetup blocks on a manually-closed release channel and was structurally unable to prove ctx cancellation)."
    - "Truth 4 (SESS-05) — WR-03 (old): cmd/cpg/mcp_tools.go's parseOptionalDuration now rejects timeout/flush_interval > 24h (maxSessionDuration), closing the unbounded-duration backstop gap. Independently re-confirmed: TestParseOptionalDuration (8 cases) passes."
    - "Truth 5 (SESS-06) — D-02 documentation reconciliation: ROADMAP.md Phase 17 SC5 and REQUIREMENTS.md SESS-06 now read 'unknown, purged, or replaced' and both cite D-02 explicitly. Independently re-confirmed present in both files by direct read."
  gaps_remaining:
    - "Truth 2 (SESS-03) reopened NARROWER than before: a SCOPED context.DeadlineExceeded from a healthy, never-cancelled sessionCtx (pkg/hubble/client.go:109-121's waitForConnReady dial timeout — the unreachable/typo'd --server scenario, one of the three scenarios the WR-01 fix's own code comment names as its target) is misclassified by manager.go:228's `errors.Is(err, context.DeadlineExceeded)` filter as 'expected cancellation', so the autonomous transition never fires and get_status still reports \"capturing\" forever for exactly this one realistic scenario. Independently, EMPIRICALLY reproduced by this verifier (throwaway -race test added to pkg/session, run, confirmed the bug, then deleted — git status clean before and after)."
  regressions:
    - "NEW (not reachable before 17-05): the first-ever stop_session call following an autonomous crash transition reports already_stopped: true even though no prior explicit Stop() call ever happened for that session — violates D-03's 'second/idempotent call' marker contract and the tool's own documented description (\"a second stop returns... an already-stopped marker\") plus its IdempotentHint: true annotation (cmd/cpg/mcp_tools.go). Pre-17-05, Session.State could only reach StateStopped via an explicit Stop() call (through stopOnce.Do), so this code path was unreachable; 17-05's new autonomous-transition pathway (manager.go:234-237) makes State==StateStopped reachable WITHOUT stopOnce ever running, and Stop()'s early-return branch (manager.go:388-390) keys AlreadyStopped purely off state, not off whether Stop() itself was previously called. Deterministically confirmed via code-path tracing (not a race — the sequencing is enforced by the existing test's own require.Eventually gate) and corroborated by TestManager_PipelineErrorAutonomouslyStopsSession, which calls Stop() in this exact state and asserts on stopRes.Error but never asserts on stopRes.AlreadyStopped — a test that passes without exercising the documented behavior it implicitly touches."
gaps:
  - truth: "LLM calls get_status(session_id) at any point and receives coarse state — capturing/stopped, elapsed time, artifact file counts on disk"
    status: partial
    reason: "Confirmed by direct source tracing AND by an independent, empirical repro run by this verifier (throwaway test, deleted after the run). manager.go:228's genuine-failure guard is `err != nil && !errors.Is(err, context.Canceled) && !errors.Is(err, context.DeadlineExceeded)`. This assumes a DeadlineExceeded can only originate from sessionCtx/m.rootCtx being cancelled — it does not. pkg/session/pipeline_config.go:76 sets PipelineConfig.Timeout to args.Timeout (10s default); pkg/hubble/pipeline.go:164 constructs the gRPC Client with that same Timeout as c.timeout; pkg/hubble/client.go:109-121's waitForConnReady derives its OWN scoped `dialCtx, cancel := context.WithTimeout(ctx, timeout)` from the ctx it receives — which is sessionCtx itself, propagated unchanged through RunPipelineWithSource(ctx, cfg, source) -> source.StreamDroppedFlows(ctx, ...) (pipeline.go:170-171) -> client.go's StreamDroppedFlows(ctx, ...) (client.go:71) -> waitForConnReady(ctx, conn, c.timeout). When the dial to an unreachable or typo'd --server address does not complete before this SCOPED deadline, dialCtx.Err() == context.DeadlineExceeded propagates back as the pipeline's terminal error while sessionCtx.Err() stays nil throughout (only the derived child context timed out, not the parent). errors.Is(err, context.DeadlineExceeded) is true for this error, so manager.go:228's guard treats it as an 'expected cancellation' and skips both s.pipelineErr.Store and the State transition. This verifier independently reproduced the exact scenario: a runPipeline stand-in that derives context.WithTimeout(ctx, 20ms) from the passed sessionCtx and returns that child's Err() -- 300ms later, get_status still returned state=\"capturing\", error=\"\" for a pipeline goroutine that had already exited. No test in manager_test.go exercises this path (grep confirms zero DeadlineExceeded/dialCtx-shaped fakes); TestManager_PipelineErrorAutonomouslyStopsSession uses a plain errors.New(...) sentinel that can never be mistaken for a context error, so it cannot catch this gap."
    artifacts:
      - path: "pkg/session/manager.go"
        issue: "Line 228's `errors.Is(err, context.DeadlineExceeded)` filter also matches a SCOPED dial-timeout DeadlineExceeded derived from a healthy, never-cancelled sessionCtx (pkg/hubble/client.go's waitForConnReady), not just an actual sessionCtx cancellation -- so the autonomous State transition added by 17-05 never fires for an unreachable/typo'd --server address, silently reopening the 'capturing forever' defect for this one specific, realistic scenario."
    missing:
      - "Classify based on whether sessionCtx itself was actually cancelled (e.g. gate on `sessionCtx.Err() == nil` as the sole 'this was on purpose' signal) instead of on the identity/kind of the returned error -- a scoped, unrelated timeout can produce the identical DeadlineExceeded sentinel. See 17-REVIEW.md's WR-01 fix sketch (current review, not the closed one)."
      - "Add a regression test using a runPipeline stand-in that derives its own context.WithTimeout from the passed-in (healthy) ctx and returns that scoped ctx.Err(), asserting get_status still autonomously transitions to \"stopped\" -- this verifier's throwaway repro (deleted after this run) is a ready template."
  - truth: "stop_session's already_stopped field correctly marks only a genuinely redundant (second-or-later) call, per D-03 (17-CONTEXT.md) and the tool's own documented \"Idempotent — a second stop returns the same summary with an already-stopped marker, never an error\" contract (cmd/cpg/mcp_tools.go) plus its IdempotentHint: true annotation"
    status: partial
    reason: "Not a literal ROADMAP/REQUIREMENTS success-criterion clause, but a decision (D-03) and a self-declared tool contract this phase's own code and docs commit to -- included as a gap because it is a real, deterministically-confirmed regression introduced by 17-05, not a hypothetical. Stop() (manager.go:371-412) decides AlreadyStopped purely from `state == StateStopped` (the early-return at 388-390: `if state == StateStopped { return s.buildSummary(true, healthPath), nil }`). Before 17-05, State could reach StateStopped ONLY via Stop()'s own stopOnce.Do, so this early-return branch, by construction, could only ever be hit by a genuinely-second-or-later call. 17-05 added a second, independent way to reach StateStopped: the launch goroutine's autonomous transition on a genuine pipeline failure (manager.go:234-237), which never touches stopOnce. Consequently, the FIRST-EVER stop_session call for a session that crashed on its own now also takes the `state == StateStopped` branch and reports AlreadyStopped: true -- indistinguishable, from the caller's perspective, from an actual redundant/duplicate call. This is deterministic, not racy: TestManager_PipelineErrorAutonomouslyStopsSession (manager_test.go:692-721) already drives exactly this sequence (Start -> crash -> require.Eventually State==\"stopped\" -> Stop()) and its Stop() call is unambiguously the first one for that session, yet the test never asserts on stopRes.AlreadyStopped, so the bug passes silently. A client that keys behavior off this field (e.g. \"only surface a stop notification when !already_stopped\") will incorrectly suppress the notification for a session that crashed and was never actually reported as stopped to the caller before this call."
    artifacts:
      - path: "pkg/session/manager.go"
        issue: "Stop()'s early-return branch (lines 388-390) sets AlreadyStopped purely from `state == StateStopped`, which the WR-01 autonomous transition (lines 228-239, specifically 234-237) can now also produce WITHOUT any prior stopOnce.Do execution -- so the first-ever stop_session call after an autonomous crash incorrectly reports already_stopped: true, contradicting the documented idempotency-marker contract. Unreachable before 17-05."
    missing:
      - "Track whether an explicit Stop() call has previously completed independently of why State is already Stopped -- e.g. an `explicitStopSeen atomic.Bool` on Session, `Swap(true)` at both of Stop()'s buildSummary call sites (the early-return AND the stopOnce.Do path), per 17-REVIEW.md's fix sketch (current review)."
      - "Strengthen TestManager_PipelineErrorAutonomouslyStopsSession (or add a new test) asserting `AlreadyStopped == false` on the first stop_session call following an autonomous crash, and `AlreadyStopped == true` on a subsequent second call for the same session."
human_verification: []
---

# Phase 17: Session Lifecycle Verification Report

**Phase Goal:** An LLM can start, monitor, and stop a live Hubble capture session through MCP tools, with the process robustly cleaning up on every exit path.
**Verified:** 2026-07-21T08:18:00Z
**Status:** gaps_found
**Re-verification:** Yes — after gap closure (plans 17-05, 17-06, 17-07; prior report `status: gaps_found`, score 3/5)

## Goal Achievement

This is a full re-verification, not a rubber stamp of the gap-closure SUMMARYs. Every previously-FAILED truth got full 3-level (+wiring) re-verification; every previously-VERIFIED truth got a regression check; and — per the task's explicit instruction — the two NEW warnings from the fresh post-gap-closure `17-REVIEW.md` code review were independently re-derived from source (not taken on the review's word) and, for the higher-severity one, empirically reproduced with a throwaway test.

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | `start_session` returns opaque `session_id`; background goroutine on detached cancellable ctx; ephemeral `os.MkdirTemp` tmpdir; second `start_session` while active is rejected (actionable, names active session), never queued/silently replaced | ✓ VERIFIED (regression check) | Unchanged code path (`pkg/session/manager.go` `Start()` 104-245): slot claim under `m.mu` before slow setup, `context.WithCancel(m.rootCtx)` (130), `os.MkdirTemp` (154), actionable rejection (109-112). Re-ran `go test ./pkg/session/... -race -count=1 -v`: all `TestManager_Start*` tests pass, including `TestManager_Start_ConcurrentStartRejectsSecond`. Full repo `go test ./... -race -count=1`: 11/11 packages green (this run). The 17-05/17-06 diffs to manager.go are additive (pipelineErr field, AfterFunc merge, launch-goroutine guard) and do not touch this logic. |
| 2 | `get_status(session_id)` returns coarse state — capturing/stopped, elapsed time, artifact file counts on disk | ✗ FAILED (partial — reopened, narrower) | **General case CLOSED (WR-01 old):** a genuine non-context pipeline error now autonomously flips State to stopped and surfaces the error (`manager.go` 219-241, `session.go` 107-114/218-223), proven by `TestManager_PipelineErrorAutonomouslyStopsSession` (re-run, passes under `-race`). **Gap reopened (WR-01 new, narrower):** a SCOPED `context.DeadlineExceeded` from a healthy, never-cancelled `sessionCtx` (the gRPC dial-timeout in `pkg/hubble/client.go:109-121`'s `waitForConnReady`, hit by an unreachable/typo'd `--server` address) is misclassified by `manager.go:228`'s `errors.Is(err, context.DeadlineExceeded)` filter as an intentional cancellation, so the transition never fires. **Independently, empirically reproduced by this verifier**: added a throwaway `-race` test to `pkg/session` with a `runPipeline` stand-in that derives its own scoped `context.WithTimeout` from the passed (healthy) `sessionCtx` and returns that child's `Err()` — 300ms after the 20ms scoped deadline fired, `get_status` still returned `state="capturing", error=""`. Test deleted after confirming (`git status` clean before/after). See gaps YAML for the full evidence trail and fix sketch. |
| 3 | `stop_session(session_id)`: pipeline ctx cancelled, artifacts finalized (`cluster-health.json`, session stats), final summary returned | ✓ VERIFIED (regression check; one related, non-SC-blocking gap flagged below) | `Stop()` (manager.go:371-412) unchanged in its core shape: `s.stopOnce.Do` cancel+bounded-wait+finalize, `buildSummary` returns real `SessionStats`-derived counters plus the new `Error` field. Re-ran `TestManager_Stop` (still `FlowsSeen==2`, tmpdir retained, `Error` empty on a clean drain) and `TestManager_Stop_Idempotent` — both green. **Flagged separately (not an SC3-text violation):** the fresh code review's WR-02 (new) — the `already_stopped` boolean is incorrectly `true` on the FIRST stop_session call following an autonomous crash — is a real, deterministically-confirmed regression against D-03's documented "second/idempotent call" contract. It does not stop a final summary from being correctly returned (SC3's literal 3 clauses hold), so this truth stays VERIFIED, but the defect is tracked as a gap in the frontmatter (see `gaps:` entry 2 and the Anti-Patterns table) because leaving it undocumented would be exactly the kind of "SUMMARY says pass, code says otherwise" gap this verification exists to catch. |
| 4 | Killing the transport (stdin EOF, harness crash) cancels the session ctx, closes the port-forward, removes the tmpdir — each step bounded so one wedged cleanup cannot block process exit | ✓ VERIFIED (full re-verification — was FAILED/partial last round) | **WR-02 (old) CLOSED:** `context.AfterFunc(sessionCtx, setupCancel)` (manager.go:180, confirmed present via `rg`) merges `Shutdown`'s `s.cancel()` into a mid-setup `Start()`'s `setupCtx`, so a `resolveSetupFn` step that observes its ctx aborts promptly. Independently re-confirmed via the NEW test `TestManager_Start_ShutdownCancelsSetupCtx` (manager_test.go:740-790), which — unlike the old `TestManager_Start_ShutdownRacesSetup` (still present, unchanged, still passing) — blocks on `<-setupCtx.Done()` directly instead of a manually-closed release channel, so it structurally proves ctx-cancellation reachability, not just goroutine gating. Re-ran it standalone under `-race -v`: PASS (0.11s, well inside its bounded window). **WR-03 (old) CLOSED:** `maxSessionDuration = 24 * time.Hour` (cmd/cpg/mcp_tools.go:52) + rejection guard (mcp_tools.go:76-78), proven by `TestParseOptionalDuration` (8 table-driven cases, re-run, all pass), removing the unbounded-duration backstop gap that compounded WR-02. **Accepted, documented residual (unchanged from before, not new):** `k8s.LoadKubeConfig()` still takes no ctx (manager.go:261,280) — a hang specifically inside kubeconfig load is reachable by neither the timeout nor this cancellation; explicitly documented in a code comment at the merge point (manager.go:174) and in 17-06's threat register (T-17-06-03, accepted disposition) — does not block process exit since `Shutdown`'s own fan-out is independently bounded. No new gap found for this truth by the fresh review or by this verifier's own pass. |
| 5 | Any session-scoped tool called with an unknown, purged, or replaced `session_id` returns a crisp "session not found or expired" error; a retained STOPPED session stays queryable per D-02 and does not trigger this error | ✓ VERIFIED (regression check; D-02 doc-reconciliation gap CLOSED) | **Behavior unchanged, still correct:** `TestManager_UnknownSessionID` and `TestManager_Status_StoppedSessionStaysQueryable` re-run, both pass. **Doc reconciliation (17-07) CLOSED:** `.planning/ROADMAP.md` Phase 17 SC5 now reads "unknown, purged, or replaced" and cites "D-02" explicitly (confirmed by direct read: `grep -n "D-02" .planning/ROADMAP.md` matches inside the Phase 17 SC list); `.planning/REQUIREMENTS.md` SESS-06 carries the equivalent reconciliation, preserves "(SEP-2567 handle semantics)", and names the SESS-06 ↔ QRY-04 tension D-02 resolves. Both files independently re-read in full for this verification — the stale "unknown or already-stopped" / "unknown or stopped" phrasing is confirmed gone. |

**Score:** 4/5 ROADMAP truths fully verified (Truth 2 FAILED — partial, reopened narrower). **Plus one additional, non-SC-text gap** (the `already_stopped` field's D-03 contract violation under Truth 3) tracked in the gaps YAML — see the Gaps Summary for why this is reported as a gap without flipping Truth 3's top-line status.

### Required Artifacts

**Original phase artifacts (17-01..17-04) — regression check, unaffected by 17-05/17-06/17-07's additive diffs:**

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `pkg/hubble/pipeline.go` | `OnFinal func(SessionStats)` nil-safe hook (D-08) | ✓ VERIFIED | Unchanged: field + call site confirmed present (lines 95-101, 336-338). |
| `pkg/session/session.go` | State model, result shapes, `buildSummary` | ✓ VERIFIED | 247 lines (grew from 219 — new `pipelineErr` field + `Error` fields + WR-04 fix). Re-read in full this pass. |
| `pkg/session/pipeline_config.go` | `defaultDuration` + `buildPipelineConfig` | ✓ VERIFIED | 109 lines, unchanged. Re-read in full this pass; confirms `Timeout: defaultDuration(args.Timeout, 10*time.Second)` (line 76) is the value that flows into `waitForConnReady`'s scoped dial timeout (Truth 2 gap's evidence chain). |
| `cmd/cpg/mcp.go` | Manager wiring + `Shutdown()` after `Run` | ✓ VERIFIED | Unchanged: `session.NewManager(ctx, ...)` (94), `mgr.Shutdown()` unconditional after `server.Run` (104). Re-read in full this pass. |

**Gap-closure artifacts (17-05/17-06/17-07) — full verification (exists, substantive, wired):**

| Artifact | Expected (from PLAN must_haves) | Status | Details |
|---|---|---|---|
| `pkg/session/session.go` (17-05) | `pipelineErr` atomic field; `Error` on `StatusResult`+`StopResult`; WR-04 ok-check | ✓ VERIFIED | `pipelineErr atomic.Pointer[error]` (114); `Error string` on both result shapes (163, 174); `buildSummary` surfaces it before the `stats==nil` early return (221-223); WR-04's `name, ok := flowpb.DropReason_name[...]` + `UNKNOWN(%d)` fallback (239-241) confirmed. `gsd-sdk query verify.artifacts` (17-05-PLAN.md): 3/3 pass. |
| `pkg/session/manager.go` (17-05+17-06) | Launch-goroutine capture+transition; Status surfaces error; `AfterFunc` merge | ✓ VERIFIED (with the Truth 2 classification gap noted above) | `pipelineErr.Store` (229), guarded `State`/`StoppedAt` transition (234-237), `Status` loads `pipelineErr` into `StatusResult.Error` (349-352), `context.AfterFunc(sessionCtx, setupCancel)` (180) with deferred stop (181). `gsd-sdk query verify.artifacts`: pass for both plans. |
| `pkg/session/manager_test.go` (17-05+17-06) | `-race` tests proving both fixes | ✓ VERIFIED | `TestManager_PipelineErrorAutonomouslyStopsSession` (692-721) and `TestManager_Start_ShutdownCancelsSetupCtx` (740-790) both re-run standalone under `-race -v`: PASS. `rg -c 'pipelineErr'` → 3 matches (satisfies the plan's literal-string gate via the doc comment, while the test genuinely exercises the field indirectly through the public API). |
| `cmd/cpg/mcp_tools.go` (17-06) | `maxSessionDuration` ceiling in `parseOptionalDuration` | ✓ VERIFIED | `const maxSessionDuration = 24 * time.Hour` (52); `d > maxSessionDuration` guard (76-78); jsonschema descriptions mention "max 24h" (lines 29, 31). |
| `cmd/cpg/mcp_session_test.go` (17-06) | Test proving the duration ceiling | ✓ VERIFIED | `TestParseOptionalDuration` (227-262): 8 table-driven cases including exactly-at-ceiling and one-second-over; re-run standalone, all pass. |
| `.planning/ROADMAP.md` / `.planning/REQUIREMENTS.md` (17-07) | SC5/SESS-06 reconciled to cite D-02 | ✓ VERIFIED | Confirmed by direct read (see Truth 5 evidence above). `gsd-sdk query verify.artifacts` (17-07-PLAN.md): 2/2 pass. |

**`gsd-sdk verify.artifacts` (automated cross-check):** 9/9 gap-closure artifacts pass across all 3 plans (all `exists: true`, `issues: []`); 11/11 original-phase artifacts previously confirmed and unaffected by this round's diffs.

### Key Link Verification

`gsd-sdk query verify.key-links` again returns `"Source file not found"` for all 9 declared gap-closure links — the same known tooling/path-parsing artifact documented in the prior report (the plans' `from:` fields embed descriptive text after the file path, which the verb does not resolve as a bare path). Not evidence of missing wiring — every link below was independently verified by direct source reading.

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `manager.go` launch goroutine | `Session.pipelineErr` | `s.pipelineErr.Store(&err)` | ✓ WIRED | Line 229, inside the genuine-failure guard. |
| `manager.go` launch goroutine | `Session.State` | guarded `m.session==s && State==StateCapturing` under `m.mu` | ✓ WIRED (partially — see Truth 2) | Lines 231-238. Fires correctly for a genuine non-context error; does NOT fire for a scoped `DeadlineExceeded` (Truth 2 gap) — the guard itself is sound, the upstream error classification feeding it is not. |
| `manager.go Status` | `StatusResult.Error` | `pipelineErr.Load()` outside `m.mu` | ✓ WIRED | Lines 349-352, 361. |
| `session.go buildSummary` | `StopResult.Error` | `pipelineErr.Load()` before the `stats==nil` early return | ✓ WIRED | Lines 221-223. |
| `manager.go Start` | `setupCtx` | `context.AfterFunc(sessionCtx, setupCancel)` | ✓ WIRED | Line 180, deferred stop at 181; `context.WithTimeout(reqCtx, timeout)` (160) retained unchanged alongside it. |
| `manager.go Shutdown` | mid-setup `resolveSetupFn` abort | `s.cancel()` → `sessionCtx` cancel → `AfterFunc` → `setupCancel()` | ✓ WIRED | Proven by `TestManager_Start_ShutdownCancelsSetupCtx`'s bounded-return + no-orphan-tmpdir assertions, independently re-run. |
| `mcp_tools.go parseOptionalDuration` | rejection of oversized durations | `d > maxSessionDuration` guard | ✓ WIRED | Lines 76-78; both `timeout` and `flush_interval` flow through this one parser. |
| `ROADMAP.md` SC5 | `17-CONTEXT.md` D-02 | explicit "D-02" citation | ✓ WIRED | Confirmed by direct read. |
| `REQUIREMENTS.md` SESS-06 | `17-CONTEXT.md` D-02 | explicit "D-02" citation | ✓ WIRED | Confirmed by direct read; SEP-2567 note preserved. |
| `manager.go Stop` (early-return) | `StopResult.AlreadyStopped` | `state == StateStopped` → `buildSummary(true, ...)` | ⚠️ WIRED BUT SEMANTICALLY WRONG | Lines 388-390. Wiring itself is intact (the field is populated and returned), but the boolean it computes is incorrect for the "first call after an autonomous crash" case — see Truth 3 / gaps entry 2. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `get_status` → `StatusResult.Error` | `Error` | `mgr.Status()` ← `Session.pipelineErr` ← launch goroutine's genuine-failure guard | Partially | ⚠️ PARTIAL — flows correctly for a genuine non-context pipeline error (real, non-hardcoded); does NOT populate for a scoped-`DeadlineExceeded` dial-timeout exit (Truth 2 gap) — not a hollow prop, but an upstream classification gap that leaves this field empty when it should be populated. |
| `stop_session` → `StopResult.Error` | `Error` | `mgr.Stop()` → `buildSummary()` ← `Session.pipelineErr` | Yes (for detected crashes) | ✓ FLOWING — `TestManager_PipelineErrorAutonomouslyStopsSession` confirms a real, non-hardcoded error string reaches the summary for a detected crash. |
| `stop_session` → `StopResult.AlreadyStopped` | `AlreadyStopped` | `mgr.Stop()` → `state == StateStopped` boolean | Yes, but semantically wrong in one case | ⚠️ PARTIAL — the value is a real (not hardcoded/stubbed) computation, but the computation itself is incorrect for the first-call-after-autonomous-crash case (Truth 3 gap). This is a logic bug, not a data-flow disconnection. |
| `start_session`/other fields unaffected by this round's changes | — | — | Yes | ✓ FLOWING — unchanged, previously verified, re-confirmed by the full `-race` suite passing. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `cpg mcp` subcommand still registered | `go run ./cmd/cpg --help` | Lists `mcp   Run cpg as a readonly MCP server over stdio` | ✓ PASS |
| `cpg mcp --help` does not hang | `timeout 10 go run ./cmd/cpg mcp --help` | Returns immediately | ✓ PASS |
| Full `pkg/session` + `cmd/cpg` + `pkg/hubble` `-race` suite | `go test ./... -race -count=1` | 11/11 packages green | ✓ PASS |
| WR-01(old) fix test, standalone | `go test ./pkg/session/... -race -run 'TestManager_PipelineErrorAutonomouslyStopsSession' -v` | PASS (0.01s) | ✓ PASS |
| WR-02(old)/WR-03(old) fix tests, standalone | `go test ./pkg/session/... -race -run 'TestManager_Start_ShutdownCancelsSetupCtx' -v` | PASS (0.11s) | ✓ PASS |
| WR-03(old) duration ceiling test | `go test ./cmd/cpg/... -race -run 'TestParseOptionalDuration' -v` | PASS (8/8 subtests) | ✓ PASS |
| `go vet` | `go vet ./pkg/session/... ./cmd/cpg/... ./pkg/hubble/...` | No issues | ✓ PASS |
| `golangci-lint`, both changed packages | `golangci-lint run ./pkg/session/... ./cmd/cpg/...` | 6 pre-existing `errcheck` findings, all in unrelated `cmd/cpg/explain_render.go` (tracked v1.5 LINT-01..03 debt in PROJECT.md) — zero findings in any Phase-17 file | ✓ PASS |
| **WR-01(new) independent empirical repro** (this verifier, throwaway test) | Custom `-race` test: `runPipeline` stand-in derives `context.WithTimeout(sessionCtx, 20ms)`, returns the child's `Err()`; poll `Status()` 300ms later | `state="capturing", error=""` — pipeline goroutine already exited 280ms earlier | ✗ FAIL — **confirms the Truth 2 gap is real, reproducible, not merely theoretical.** Test file created, run, logged, then deleted; `git status` clean before and after. |

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention in this repository and none declared in the PLAN/SUMMARY files for 17-05/17-06/17-07. **SKIPPED** (no probe-based verification applies to this Go-native test-suite project).

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| SESS-01 | 17-02, 17-03, 17-04 | `start_session` returns opaque `session_id`, background goroutine, detached ctx, ephemeral tmpdir | ✓ SATISFIED | Truth 1, VERIFIED (regression). |
| SESS-02 | 17-03, 17-04 | Exactly one concurrent session, actionable rejection | ✓ SATISFIED | Truth 1, VERIFIED (regression). |
| SESS-03 | 17-02, 17-03, 17-04, 17-05 | `get_status` coarse state/elapsed/file counts | ⚠ PARTIAL (reopened) | Truth 2, FAILED (partial) — narrower reopening (WR-01 new). REQUIREMENTS.md currently marks SESS-03 "Complete" (updated by 17-05's `requirements-completed` metadata); this verification finds it should be reopened again pending the WR-01(new) classification fix. |
| SESS-04 | 17-01, 17-02, 17-03, 17-04 | `stop_session` cancels, finalizes, returns summary | ✓ SATISFIED (with a flagged, non-blocking contract gap) | Truth 3, VERIFIED. The literal requirement text holds; the related `already_stopped`/D-03 contract bug (WR-02 new) is tracked as a gap but does not, on its own, invalidate this requirement's literal wording. |
| SESS-05 | 17-03, 17-04, 17-06 | Transport-termination bounded cleanup fan-out | ✓ SATISFIED | Truth 4, VERIFIED (full re-verification — was PARTIAL last round, now closed). |
| SESS-06 | 17-03, 17-04, 17-07 | Unknown/purged/replaced `session_id` → crisp error; retained-stopped stays queryable | ✓ SATISFIED | Truth 5, VERIFIED — D-02 doc reconciliation confirmed landed in both ROADMAP.md and REQUIREMENTS.md. |

No orphaned requirements: REQUIREMENTS.md's traceability table still maps exactly SESS-01..06 to Phase 17, and all 6 appear in at least one plan's `requirements:` frontmatter across 17-01..17-07 (17-05→SESS-03, 17-06→SESS-05, 17-07→SESS-06, in addition to the original 17-03/17-04 coverage of all six). REQUIREMENTS.md currently marks all six "Complete" — this verification finds **SESS-03 should be reopened** (Truth 2 gap); SESS-04 stays "Complete" but with a documented, non-blocking follow-up (Truth 3's `already_stopped` gap).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER scan across all 12 phase-modified code files (re-run this pass) | — | ℹ️ INFO — zero matches. No debt markers, no stub patterns. |
| `pkg/session/manager.go` | 228 | `errors.Is(err, context.DeadlineExceeded)` misclassifies a scoped, unrelated dial-timeout as session teardown | 🛑 **Blocker** (Truth 2 gap, WR-01 new) | `get_status` cannot detect an autonomously-dead session for the unreachable/typo'd `--server` scenario. Independently reproduced by this verifier. |
| `pkg/session/manager.go` | 388-390 | `AlreadyStopped` computed purely from `state == StateStopped`, which the WR-01 autonomous transition can now set without `stopOnce` ever running | ⚠️ **Warning** (Truth 3 gap, WR-02 new — a regression introduced by 17-05) | First-ever `stop_session` call after an autonomous crash reports `already_stopped: true`, violating D-03's documented idempotency-marker contract and the tool's `IdempotentHint: true` annotation. Does not prevent a correct final summary from being returned. |
| `pkg/session/manager.go` / `pipeline_config.go` | manager.go:385-386, pipeline_config.go:69-71 | `outputHash`/`healthPath` formula duplicated in two places (deliberate, per comment) | ℹ️ Info | Unaddressed from the prior review (IN-01) — no functional bug today, nothing enforces the two stay in sync if either formula changes independently. |
| `pkg/session/session.go` | 89, 93, 99 | Stale `//nolint:unused` directives on `cancel`/`done`/`stopOnce` — these fields are actively consumed by `manager.go` in the same package | ℹ️ Info | New from the fresh review (IN-02) — misleading to a reader (implies unused/load-bearing suppression when neither is true); `golangci-lint` confirmed 0 issues with the comments removed. Cosmetic, no functional impact. |
| `pkg/session/manager_test.go` | 293 | `assert.GreaterOrEqual(t, status.EvidenceFileCount, 0)` is tautological (an `int` can never be negative) | ℹ️ Info | New from the fresh review (IN-03) — provides no real coverage that evidence files are actually being counted; unlike the adjacent `PolicyFileCount > 0` check a few lines above. |
| `pkg/session/manager.go` | 130, 393 (absence) | **This verifier's own finding (not in 17-REVIEW.md):** a session that reaches `StateStopped` via the WR-01 autonomous transition never has `s.cancel()` (the `sessionCtx`'s `context.CancelFunc`) invoked on it — not by the launch goroutine itself, not by `Stop()` (its early-return skips `stopOnce.Do`, the only place that calls `s.cancel()`), not by `Shutdown()` (`state == StateCapturing` gate is false), not by `Start`'s silent-purge (doesn't reference `.cancel` at all) | ℹ️ Info | Bounded, slow resource growth: each such session leaks one `context.WithCancel(m.rootCtx)` child-registration for the remainder of the process's life (released only at process exit via `m.rootCtx` itself cancelling). Does not affect correctness or any Phase-17 must-have (cancelling an already-exited pipeline's context is a no-op either way); worth a follow-up but not blocking. Not raised by `17-REVIEW.md`. |

**Note:** The two Warning/Blocker findings above were independently re-derived by this verifier directly from source — confirmed to match `17-REVIEW.md`'s (the fresh, post-gap-closure review, `status: issues_found`, 0 critical / 2 warning / 3 info) line-level claims exactly, and for the Blocker-tier one, independently and empirically reproduced with a standalone test (see Behavioral Spot-Checks).

### Human Verification Required

None. Both findings in this report are concrete, deterministic code-level gaps with clear fix sketches (WR-01 new: a classification-guard fix; WR-02 new: an explicit-stop-tracking fix) that a closure plan can implement and prove with `-race` tests, exactly like 17-05/17-06 did for the previous round. Neither requires human/UX judgment — this is a backend Go MCP server with no visual, real-time, or subjective-quality surface.

### Gaps Summary

**What closed cleanly this round:** 17-05, 17-06, and 17-07 genuinely closed everything the prior verification asked for, at the code level, independently re-confirmed here (not just re-reading the SUMMARYs):
- WR-01 (old): a genuine, non-context pipeline error now autonomously transitions the session to stopped and surfaces the error — closing the general "capturing forever" defect. Proven by a real `-race` test, re-run standalone.
- WR-02 (old) + WR-03 (old): `Shutdown` now aborts a ctx-observing mid-setup `Start()`, and MCP-supplied durations are bounded above — closing the mid-setup orphan-tmpdir race. Proven by a genuinely ctx-inspecting `-race` test (an actual improvement over the prior, structurally-insufficient test), re-run standalone.
- The D-02 documentation reconciliation landed in both ROADMAP.md and REQUIREMENTS.md, independently re-read and confirmed.
- Full repo `go test ./... -race -count=1`: 11/11 packages green, `go vet` clean, `golangci-lint` shows zero new findings in any Phase-17 file — all independently re-run by this verifier, not taken from SUMMARY claims.

**What is still open, per the task's explicit instruction to evaluate the fresh `17-REVIEW.md`'s two new warnings against this phase's must-haves:**

1. **WR-01 (new) — BLOCKER, reopens Truth 2 / SESS-03, narrower than before:** the classification guard added by 17-05 (`errors.Is(err, context.DeadlineExceeded)`) treats ANY `DeadlineExceeded`-shaped error as "the session was torn down on purpose." That is false for a scoped, unrelated timeout — specifically `pkg/hubble/client.go`'s gRPC dial timeout, which derives its OWN `context.WithTimeout` from the still-healthy `sessionCtx` and can legitimately fire on an unreachable or typo'd `--server` address (one of the three scenarios the WR-01 fix's own code comment explicitly names as its target). This verifier independently traced the full call chain from `manager.go`'s launch goroutine through `pipeline_config.go` → `pipeline.go` → `client.go`, confirmed no test covers it, and then empirically reproduced it with a throwaway `-race` test: 300ms after a 20ms scoped dial timeout fired, `get_status` still reported `"capturing"` for a session whose pipeline goroutine had already exited. This directly undermines the phase's own "monitor" promise for a realistic, commonly-hit failure mode, so it is kept as a hard gap (not deferred, not overridden).

2. **WR-02 (new) — WARNING, a genuine regression tied to Truth 3 / SESS-04 but not a literal SC3/SESS-04 text violation:** 17-05's autonomous-transition fix opened a second way for `Session.State` to reach `StateStopped` (bypassing `stopOnce`), which `Stop()`'s existing `already_stopped` logic was never designed to handle — so the first-ever `stop_session` call after an autonomous crash now incorrectly reports `already_stopped: true`, contradicting D-03 and the tool's own documented idempotency contract. This is deterministically confirmed (not a race) and is exactly the kind of "a fix that closes one gap opens an adjacent one" interaction `17-REVIEW.md` itself calls out in its preamble. It is reported as a gap (with a concrete fix sketch) rather than silently dropped, but it does not flip Truth 3's or SESS-04's top-line status, because the requirement's literal text (pipeline cancelled/artifacts finalized/summary returned) still holds and no data is lost — only a boolean metadata field's semantics are wrong in one specific, if plausible, scenario.

**Deferred-item check (Step 9b):** Neither gap is addressed by a later milestone phase. Phase 18 (Query Tools, QRY-01..05) is entirely about read-side artifact tools and does not touch crash detection or stop-idempotency semantics. Phase 19 (Security Hardening & E2E Validation) SC3 — "an end-to-end stdio integration test... plus an ungraceful-disconnect variant proving the port-forward and tmpdir are cleaned up within a bounded deadline" — is a broader *test* of the transport-kill/cleanup scenario (Truth 4/SESS-05, already closed this round), not a fix or test for `get_status` state accuracy on a dial-timeout crash or for the `already_stopped` field's correctness. No later phase's goal or success criteria text matches either gap's concern. Both are kept as active, actionable gaps for Phase 17.

**Recommendation:** A fourth gap-closure plan (17-08 or similar) scoped to these two findings, mirroring 17-05's shape:
- Task A: change `manager.go:228`'s classification guard to key off `sessionCtx.Err() == nil` (whether the session's OWN context was cancelled) rather than the identity of the returned error, plus a regression test using a scoped-timeout `runPipeline` stand-in (this verifier's throwaway repro is a ready template).
- Task B: add an `explicitStopSeen atomic.Bool` (or equivalent) to `Session`, consumed via `Swap(true)` at both of `Stop()`'s `buildSummary` call sites, so `AlreadyStopped` reflects "was `Stop()` itself previously called," not "is `State` already `Stopped` for any reason" — plus a regression test asserting `AlreadyStopped == false` on the first post-crash `stop_session` call.

Both fixes are small, localized, and follow the exact same TDD-with-`-race`-proof pattern 17-05/17-06 already demonstrated works well in this codebase.

---

_Verified: 2026-07-21T08:18:00Z_
_Verifier: Claude (gsd-verifier)_
