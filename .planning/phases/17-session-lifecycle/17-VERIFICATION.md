---
phase: 17-session-lifecycle
verified: 2026-07-21T09:51:56Z
status: gaps_found
score: 4/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "Truth 2 (SESS-03) — WR-01 (prior round's fresh finding, scoped context.DeadlineExceeded): the launch goroutine's genuine-failure guard (pkg/session/manager.go:234) now reads `err != nil && sessionCtx.Err() == nil` instead of `err != nil && !errors.Is(err, context.Canceled) && !errors.Is(err, context.DeadlineExceeded)`. A scoped dial-timeout DeadlineExceeded from pkg/hubble/client.go:109-121's waitForConnReady (unreachable/typo'd --server, healthy sessionCtx) now correctly autonomously transitions the session to stopped. Independently re-derived from source (not taken from SUMMARY/REVIEW): `rg -n 'sessionCtx\\.Err\\(\\) == nil' pkg/session/manager.go` → exactly 1 match at line 234; `rg -n 'errors\\.Is\\(err,' pkg/session/manager.go` → 0 matches (old guard gone); `rg -n 'errors\\.Is\\(pfErr,' pkg/session/manager.go` → 1 match preserved (resolveSetup's unrelated setup-timeout classification untouched). `go test ./pkg/session/... -race -count=1 -run TestManager_ScopedDialTimeoutAutonomouslyStopsSession -v` → PASS standalone."
    - "Truth 3 (SESS-04) — WR-02 (prior round's fresh finding, already_stopped/D-03 contract violation): `Session.explicitStopSeen atomic.Bool` (session.go:124) with `Swap(true)` at both of Stop's buildSummary call sites (manager.go:411 early-return, manager.go:439 post-stopOnce) decouples `AlreadyStopped` from `State`, so the first stop_session after an autonomous crash correctly reports false. Independently re-derived from source: `rg -n 'explicitStopSeen\\s+atomic\\.Bool' pkg/session/session.go` → 1 match; `rg -n 'explicitStopSeen\\.Swap\\(true\\)' pkg/session/manager.go` → exactly 2 matches; `rg -n 'buildSummary\\(true,|buildSummary\\(false,' pkg/session/manager.go` → 0 matches (literals gone). `go test ./pkg/session/... -race -count=1 -run TestManager_FirstStopAfterAutonomousCrashIsNotAlreadyStopped -v` → PASS standalone; the strengthened assertion added to `TestManager_PipelineErrorAutonomouslyStopsSession` (`assert.False(t, stopRes.AlreadyStopped, ...)`, line 719-720) also confirmed present and passing."
  gaps_remaining: []
  regressions: []
gaps:
  - truth: "LLM calls get_status(session_id) at any point and receives coarse state — capturing/stopped, elapsed time, artifact file counts on disk"
    status: partial
    reason: "Both gaps tracked by the prior 17-VERIFICATION.md round are genuinely closed (see gaps_closed above — independently re-derived, not taken on faith). But the fix landed by 17-08 is asymmetric by its own design: the launch goroutine's guard is `if err != nil && sessionCtx.Err() == nil`, which requires err != nil. A pipeline that exits with a CLEAN nil error while sessionCtx is still healthy — e.g. pkg/hubble/client.go's streamFromSource treating a clean io.EOF as harmless end-of-stream (client.go:157-159, closes errCh without ever sending to it) — never enters this guard at all, so no State transition happens, ever. This is the exact scenario the phase's own test suite exercises as its PRIMARY 'successful capture' fixture: `closedFlowSource` (pkg/session/manager_test.go), used by TestManager_Status, TestManager_Stop, TestManager_Stop_Idempotent, TestManager_UnknownSessionID, TestManager_Status_StoppedSessionStaysQueryable, and TestManager_Start_PurgesStoppedSession, ALL of which only ever observe this state via an EXPLICIT Stop() call, never by polling get_status after a natural drain with zero stop_session calls made. This verifier independently and empirically reproduced the gap: a throwaway test (pkg/session/verifier_throwaway_nilexit_test.go, created, run, logged, then deleted — `git status` clean before and after) started a session against closedFlowSource, waited for the policy file to land on disk (proving the pipeline had fully drained both flows and logged its session summary), slept an additional 500ms of pure slack, then called Status(): result was `state=\"capturing\" error=\"\"` — the session never transitioned despite the pipeline goroutine having long since exited. A follow-up Start() call for a NEW session was then rejected with 'already running ... call stop_session first', confirming the wedge also blocks the single-slot invariant, not just get_status's truthfulness. This directly contradicts the phase goal's own text ('the process robustly cleaning up on every exit path') — a clean/nil exit is an exit path, and manager.go's own code comment (lines 230-233) explicitly acknowledges the clean-drain case is 'deliberately left untouched ... so every pre-existing test keeps passing unchanged', a test-preservation rationale rather than a product one. This is NOT a regression introduced by 17-05/17-08 — tracing the pre-17-05 launch goroutine (which had no classification logic of any kind and only ever wrote s.done, never State) confirms a clean/nil exit has been unhandled since the state machine was first introduced in 17-03; 17-05/17-08 only ever targeted non-nil-error exits by their own explicit scope statements, so this sub-case survived two rounds of gap closure unrecognized until the fresh post-17-08 code review surfaced it by name."
    artifacts:
      - path: "pkg/session/manager.go"
        issue: "Line 234's guard `if err != nil && sessionCtx.Err() == nil` never fires for a clean/nil pipeline exit (sessionCtx healthy, err == nil) — e.g. a Hubble Relay closing the gRPC stream cleanly (io.EOF), which pkg/hubble/client.go's streamFromSource treats as harmless and does not surface as an error. No State transition, no s.cancel() release, no pipelineErr. Session.State is wedged at StateCapturing forever with zero stop_session calls made, and a subsequent start_session is rejected as if the session were still genuinely active."
    missing:
      - "Broaden the autonomous-exit guard to run on ANY exit while sessionCtx.Err() == nil (nil or non-nil err), keeping the error-surfacing/logging bits conditional on err != nil — i.e. `if sessionCtx.Err() == nil { if err != nil { store pipelineErr, Warn log } else { Info log }; do the State transition + s.cancel() unconditionally }`. This is exactly the fix sketch in the fresh 17-REVIEW.md's WR-01 (this round's finding, distinct from the two WR-01s already closed)."
      - "Add a regression test that proves a closedFlowSource-driven (or equivalent nil-returning) pipeline autonomously transitions State to \"stopped\" via get_status with zero stop_session calls — this verifier's throwaway repro (deleted after this run) is a ready template. Also audit/adjust any existing closedFlowSource-based test whose timing assumptions implicitly depend on State staying \"capturing\" until an explicit Stop (none currently assert State==\"capturing\" after a natural drain, per direct inspection, but this should be a explicit acceptance-criteria check in the closure plan, not an assumption)."
human_verification: []
---

# Phase 17: Session Lifecycle Verification Report

**Phase Goal:** An LLM can start, monitor, and stop a live Hubble capture session through MCP tools, with the process robustly cleaning up on every exit path.
**Verified:** 2026-07-21T09:51:56Z
**Status:** gaps_found
**Re-verification:** Yes — after gap-closure plan 17-08 (prior report `status: gaps_found`, score 4/5, blocking on the scoped-DeadlineExceeded WR-01 and the already_stopped/D-03 WR-02)

## Goal Achievement

This is a full re-verification, not a rubber stamp of 17-08-SUMMARY.md's claims or the fresh post-17-08 `17-REVIEW.md`'s findings. Per the task's explicit instruction, every claim from both documents was independently re-derived from source (`rg` source gates re-run myself, not copied), every regression-sensitive test was re-run standalone under `-race`, and the fresh review's two NEW warnings (WR-01/WR-02 of *this* round) were weighed independently rather than inherited — including one empirical, throwaway-test reproduction of the higher-severity one.

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | `start_session` returns opaque `session_id`; background goroutine on detached cancellable ctx; ephemeral `os.MkdirTemp` tmpdir; second `start_session` while active is rejected (actionable, names active session), never queued/silently replaced | ✓ VERIFIED (regression check) | Unchanged code path (`pkg/session/manager.go` `Start()` 104-259) — `git diff 738dcd5..HEAD --stat` for `cmd/cpg/mcp.go`/`mcp_tools.go`/`mcp_session_test.go`/`mcp_harness_test.go` is empty (zero changes since the prior verification commit). 17-08's diff touches only `manager.go`'s launch goroutine + `Stop()`, `session.go`'s new field, and `manager_test.go`'s new tests — confirmed via `git show --stat` on all 4 of 17-08's commits (f47b837/cca60a1/0690fd8/0159b66). Full suite re-run: `go test ./pkg/session/... -race -count=1 -v` — all 24 tests pass including `TestManager_Start`, `TestManager_Start_RejectsConcurrent`, `TestManager_Start_ConcurrentStartRejectsSecond`. **Noted, not a Truth-1 violation:** Truth 2's gap below has a second-order symptom where a *wedged-but-dead* session also triggers this same rejection path — the rejection behavior itself is correct per its literal wording (actionable, names the session); the defect is that get_status/State don't know the session is actually dead, which is a Truth 2 concern, not a Truth 1 one. |
| 2 | `get_status(session_id)` returns coarse state — capturing/stopped, elapsed time, artifact file counts on disk | ✗ FAILED (partial — reopened again, narrower still) | **Both previously-tracked gaps CLOSED, independently re-confirmed via source + standalone `-race` test runs** (see frontmatter `gaps_closed` for exact `rg` evidence). **A third, narrower gap found and empirically reproduced by this verifier:** the fixed guard (`err != nil && sessionCtx.Err() == nil`, manager.go:234) only fires for a non-nil error — a CLEAN pipeline exit (nil error, sessionCtx healthy) never transitions State. This is reachable via ordinary means: `pkg/hubble/client.go`'s own stream-reader treats `io.EOF` as harmless and does not surface it as an error (client.go:157-159), and the phase's OWN test suite's primary "successful capture" fixture (`closedFlowSource`) drives exactly this nil-return shape. Empirically reproduced: a throwaway `-race` test (created, run, logged, deleted; `git status` clean before/after) started a `closedFlowSource`-backed session, waited for full drain (policy file on disk) + 500ms slack, then called `Status()`: result was `state="capturing", error=""` — wedged forever, with a follow-up `start_session` rejected as "already running." See gaps YAML for the full trail. |
| 3 | `stop_session(session_id)`: pipeline ctx cancelled, artifacts finalized (`cluster-health.json`, session stats), final summary returned | ✓ VERIFIED (prior round's flagged gap now CLOSED) | `Stop()` (manager.go:385-439) unchanged in its core shape: `stopOnce.Do` cancel+bounded-wait+finalize, `buildSummary` returns real `SessionStats`-derived counters plus `Error`. **WR-02 (prior round) CLOSED:** `s.explicitStopSeen.Swap(true)` at both `buildSummary` call sites (411, 439) — confirmed via source `rg` (2 matches, 0 literal `true`/`false` args remain) and via `TestManager_FirstStopAfterAutonomousCrashIsNotAlreadyStopped` (PASS standalone) plus the strengthened assertion in `TestManager_PipelineErrorAutonomouslyStopsSession` (`assert.False(t, stopRes.AlreadyStopped)`, PASS). Re-ran `TestManager_Stop`, `TestManager_Stop_Idempotent`, `TestManager_ConcurrentStop` standalone — all green, confirming clean-drain/idempotent/concurrent semantics are unchanged. **New, independently-verified Info-level finding (non-blocking, does not violate this truth's literal text):** a `Stop()` call racing a concurrent `Start()`'s D-04 purge (manager.go:114-121) can return a well-formed summary whose `TmpDir`/`ClusterHealthPath` point at a just-removed directory — traced through the source myself (Stop captures `s`/`tmpDir` under `m.mu`, unlocks at line 394, and the early-return branch never re-validates `m.session == s` before returning). Benign staleness in a narrow concurrent-caller race window; a single sequential MCP client (the phase's actual usage shape) does not hit it. Tracked in Anti-Patterns, not gated. |
| 4 | Killing the transport (stdin EOF, harness crash) cancels the session ctx, closes the port-forward, removes the tmpdir — each step bounded so one wedged cleanup cannot block process exit | ✓ VERIFIED (regression check) | Unchanged since the full re-verification two rounds ago: `context.AfterFunc(sessionCtx, setupCancel)` (manager.go:180) and `Shutdown()` (448-486) untouched by 17-08's diff (confirmed via `git show --stat` on all 4 commits — zero touches to this code path). Re-ran `TestManager_Shutdown`, `TestManager_Shutdown_WedgedStepDoesNotBlock`, `TestManager_Start_ShutdownRacesSetup`, `TestManager_Start_ShutdownCancelsSetupCtx`, `TestManager_ConcurrentShutdownAndStop` standalone — all green. Accepted, unchanged residual: `k8s.LoadKubeConfig()` still takes no ctx (manager.go:275, 294) — documented at the merge point and in 17-06's threat register (T-17-06-03, accepted). |
| 5 | Any session-scoped tool called with an unknown, purged, or replaced `session_id` returns a crisp "session not found or expired" error; a retained STOPPED session stays queryable per D-02 and does not trigger this error | ✓ VERIFIED (regression check) | `TestManager_UnknownSessionID` and `TestManager_Status_StoppedSessionStaysQueryable` re-run standalone, both pass. `git diff 738dcd5..HEAD --stat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows only an appended "Gap Closure" changelog line in ROADMAP.md (17-08 entry) — SC5's text and both files' "D-02" citations independently re-confirmed present and unchanged via direct `grep`. |

**Score:** 4/5 ROADMAP truths verified (Truth 2 FAILED — partial, reopened a third time, narrower). Both gaps tracked by the prior verification round are genuinely closed; a new, narrower sub-case of the same underlying "get_status must truthfully reflect every exit path" defect was found and empirically reproduced.

### Required Artifacts

**Prior-round artifacts (17-01..17-07) — regression check, unaffected by 17-08's diff:**

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `pkg/hubble/pipeline.go` | `OnFinal func(SessionStats)` nil-safe hook (D-08) | ✓ VERIFIED | Unchanged: field + call site present (lines 101, 336-338). Not in 17-08's `files_modified`. |
| `pkg/session/pipeline_config.go` | `defaultDuration` + `buildPipelineConfig` | ✓ VERIFIED | 109 lines, byte-for-byte unchanged (re-read in full this pass; not in 17-08's `files_modified`). |
| `cmd/cpg/mcp.go` / `mcp_tools.go` | Manager wiring, `Shutdown()` after `Run`, `maxSessionDuration` ceiling | ✓ VERIFIED | `git diff 738dcd5..HEAD --stat` empty for both files — zero changes since prior verification commit. `mgr.Status()`/`mgr.Stop()` results returned as-is (mcp_tools.go:151-153, 162-165) — no field re-mapping, so `Error`/`AlreadyStopped` flow through to `structuredContent` automatically; re-confirmed by direct read this pass. |
| `.planning/ROADMAP.md` / `.planning/REQUIREMENTS.md` | SC5/SESS-06 reconciled to cite D-02 | ✓ VERIFIED | Confirmed by direct `grep -n "D-02"` on both files (see Truth 5 evidence). |

**17-08 gap-closure artifacts — full verification (exists, substantive, wired):**

| Artifact | Expected (from 17-08-PLAN must_haves) | Status | Details |
|---|---|---|---|
| `pkg/session/manager.go` | Launch-goroutine crash classification keyed on `sessionCtx.Err()==nil`; `s.cancel()` on autonomous-exit path; both `Stop` `buildSummary` sites use `explicitStopSeen.Swap(true)` | ✓ VERIFIED (with the Truth 2 classification gap noted above — the guard is correctly keyed on `sessionCtx.Err()==nil`, but is still gated behind `err != nil`, which is the residual gap) | All 3 acceptance-criteria source gates independently re-run via `rg` and match exactly: `sessionCtx\.Err\(\) == nil` (1), `errors\.Is\(err,` (0), `errors\.Is\(pfErr,` (1), `explicitStopSeen\.Swap\(true\)` (2), `buildSummary\(true,`/`buildSummary\(false,` (0 each), `s\.cancel\(\)` present inside the autonomous-exit guard (line 252) as well as in `stopOnce.Do` (line 415). |
| `pkg/session/session.go` | `explicitStopSeen atomic.Bool` field on `Session` | ✓ VERIFIED | Line 124, correctly placed after `pipelineErr` (114), doc-commented. `StopResult.AlreadyStopped`'s doc comment (185-189) updated to describe the new semantics — re-read in full this pass. |
| `pkg/session/manager_test.go` | `-race` regression tests: `TestManager_ScopedDialTimeoutAutonomouslyStopsSession`, `TestManager_FirstStopAfterAutonomousCrashIsNotAlreadyStopped` | ✓ VERIFIED | Both present (`rg` confirms 1 match each), both re-run standalone under `-race -v`: PASS. Both carry a load-bearing comment documenting they fail against pre-fix code (read in full — the comments are accurate, matching what I independently traced through the pre-fix guard logic). |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `manager.go` launch goroutine | `Session.State` autonomous transition | `err != nil && sessionCtx.Err() == nil` classification | ⚠️ WIRED, but incompletely — see Truth 2 | Line 234. Correctly fires for ANY non-nil error while sessionCtx is healthy (general case + scoped-DeadlineExceeded case, both closed this round and last). Does NOT fire for a nil error while sessionCtx is healthy (this round's new gap) — the wiring that exists is sound; the condition gating it is incomplete. |
| `manager.go` Stop (both call sites) | `StopResult.AlreadyStopped` | `s.explicitStopSeen.Swap(true)` | ✓ WIRED | Lines 411, 439 — independently confirmed via `rg` (exactly 2 matches) and via both regression-sensitive tests passing. |
| `mcp_tools.go` get_status/stop_session handlers | `session.StatusResult`/`StopResult` (incl. `Error`/`AlreadyStopped`) | Direct pass-through, no field re-mapping | ✓ WIRED | `mgr.Status(args.SessionID)` / `mgr.Stop(args.SessionID)` results returned as-is (mcp_tools.go:151-153, 162-165) — confirmed by direct read this pass; the new fields reach `structuredContent` with no additional wiring required. |
| `manager.go Start` purge (D-04) | `Stop()`'s early-return summary | *(none — this is the IN-02 finding)* | ⚠️ NOT RE-VALIDATED | `Stop()` captures `s`/`tmpDir` under lock, unlocks, and its `state == StateStopped` early-return never re-checks `m.session == s` before returning a summary — a concurrent `Start()`'s D-04 purge in that window can remove the tmpdir out from under an in-flight `Stop()` call. Independently traced through source this pass (not merely cited from the review). Info-tier: narrow race window, single-sequential-client usage doesn't hit it, does not violate Truth 3's literal text. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `get_status` → `StatusResult.State`/`Error` | `State`, `Error` | `mgr.Status()` ← `Session.State`/`pipelineErr` ← launch goroutine's classification guard | Only for a subset of exit paths | ⚠️ PARTIAL — flows correctly for ANY genuine non-nil-error exit (general case + scoped-DeadlineExceeded, both closed); does NOT populate/transition for a clean nil-error exit (this round's Truth 2 gap) — empirically confirmed by this verifier's throwaway test. Not a hollow prop — a real, upstream classification gap. |
| `stop_session` → `StopResult.Error` | `Error` | `mgr.Stop()` → `buildSummary()` ← `Session.pipelineErr` | Yes (for detected crashes) | ✓ FLOWING — `TestManager_PipelineErrorAutonomouslyStopsSession` and `TestManager_ScopedDialTimeoutAutonomouslyStopsSession` both confirm a real, non-hardcoded error string reaches the summary. |
| `stop_session` → `StopResult.AlreadyStopped` | `AlreadyStopped` | `mgr.Stop()` → `s.explicitStopSeen.Swap(true)` | Yes, and now semantically correct | ✓ FLOWING — `TestManager_FirstStopAfterAutonomousCrashIsNotAlreadyStopped` confirms the D-03 contract holds (first call false, second call true), closing the prior round's gap. |
| `start_session`/`Truth 4`/`Truth 5` fields | — | — | Yes | ✓ FLOWING — unchanged, re-confirmed by the full `-race` suite passing (11/11 packages) and standalone re-runs of the relevant tests. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full repo `-race` suite | `go test ./... -race -count=1` (via `rtk proxy go test`) | 11/11 packages green (`ok` for cmd/cpg, pkg/diff, pkg/dropclass, pkg/evidence, pkg/flowsource, pkg/hubble, pkg/k8s, pkg/labels, pkg/output, pkg/policy, pkg/session) | ✓ PASS |
| `go vet` | `go vet ./pkg/session/... ./cmd/cpg/... ./pkg/hubble/...` | No output (clean) | ✓ PASS |
| Full `pkg/session` suite, verbose | `go test ./pkg/session/... -race -count=1 -v` | All 24 tests PASS (full list captured — includes both new 17-08 tests plus every pre-existing test, none removed/skipped) | ✓ PASS |
| Full `cmd/cpg` MCP suite, verbose | `go test ./cmd/cpg/... -race -count=1 -v -run 'TestMCP'` | All 7 tests PASS (`TestMCPStdoutPurity`, `TestMCPSessionToolsListed`, `TestMCPSessionUnknownIDReturnsIsError`, `TestMCPSessionLifecycleWiringAndStdoutPurity`, `TestMCPModeStdoutNeverDefaultsToRealStdout`, `TestMCPCobraFlagErrorStaysOffStdout`, `TestMCPLoggingBridgesToZapStderr`) | ✓ PASS |
| 17-08's two new tests, standalone | `go test ./pkg/session/... -race -count=1 -run 'TestManager_ScopedDialTimeoutAutonomouslyStopsSession\|TestManager_FirstStopAfterAutonomousCrashIsNotAlreadyStopped' -v` | Both PASS | ✓ PASS |
| `golangci-lint`, phase-17 packages | `golangci-lint run ./pkg/session/... ./cmd/cpg/... ./pkg/hubble/...` | 11 pre-existing `errcheck` findings: 6 in `cmd/cpg/explain_render.go` (git-blamed to pre-v1.5 commits, unrelated LINT-01..03 debt), 5 in `pkg/hubble/client.go:146` + `health_writer.go:151/152/156/160` (git-blamed to 2026-04-21/2026-04-26, predate Phase 17 entirely) — zero findings in any file this phase actually modified | ✓ PASS (all findings independently confirmed pre-existing via `git blame`, not introduced by Phase 17) |
| **This verifier's independent empirical repro of the new Truth 2 gap** (throwaway test, deleted after) | Custom `-race` test: `closedFlowSource`-backed session, wait for full drain + 500ms slack, then `Status()` | `state="capturing", error=""` — pipeline had fully drained and logged its summary; a follow-up `Start()` was rejected as "already running" | ✗ FAIL — **confirms the Truth 2 gap is real and reproducible with the phase's own primary test fixture, not a remote edge case.** Test file created (`pkg/session/verifier_throwaway_nilexit_test.go`), run, logged, then deleted; `git status --porcelain` empty before and after. |

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention in this repository and none declared in 17-08's PLAN/SUMMARY. **SKIPPED** (no probe-based verification applies to this Go-native test-suite project).

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| SESS-01 | 17-02, 17-03, 17-04 | `start_session` opaque `session_id`, background goroutine, detached ctx, ephemeral tmpdir | ✓ SATISFIED | Truth 1, VERIFIED (regression). |
| SESS-02 | 17-03, 17-04 | Exactly one concurrent session, actionable rejection | ✓ SATISFIED | Truth 1, VERIFIED (regression). Literal wording holds even in the wedged-session scenario (rejection is still actionable and correctly named) — the underlying staleness is a SESS-03 concern. |
| SESS-03 | 17-02, 17-03, 17-04, 17-05, 17-08 | `get_status` coarse state/elapsed/file counts | ⚠ PARTIAL (reopened a third time, narrower) | Truth 2, FAILED (partial). REQUIREMENTS.md currently marks SESS-03 "Complete" and ROADMAP.md's progress table marks Phase 17 "8/8 Complete" — both were updated by 17-08's own `requirements-completed` metadata immediately after 17-08 landed, before this fresh gap was discovered by the post-17-08 code review. This verification finds SESS-03 should be reopened again pending the clean/nil-exit classification fix. |
| SESS-04 | 17-01, 17-02, 17-03, 17-04, 17-08 | `stop_session` cancels, finalizes, returns summary | ✓ SATISFIED | Truth 3, VERIFIED. The prior round's `already_stopped`/D-03 contract gap (WR-02) is now closed and independently re-confirmed. The new IN-02 finding (Stop racing a concurrent purge) is Info-tier and does not affect this requirement's literal wording. |
| SESS-05 | 17-03, 17-04, 17-06 | Transport-termination bounded cleanup fan-out | ✓ SATISFIED | Truth 4, VERIFIED (regression) — unaffected by 17-08's diff. |
| SESS-06 | 17-03, 17-04, 17-07 | Unknown/purged/replaced `session_id` → crisp error; retained-stopped stays queryable | ✓ SATISFIED | Truth 5, VERIFIED (regression) — unaffected by 17-08's diff. |

No orphaned requirements: REQUIREMENTS.md's traceability table still maps exactly SESS-01..06 to Phase 17, and all 6 appear in at least one plan's `requirements:` frontmatter (17-08 declares `[SESS-03, SESS-04]`, matching this round's scope). REQUIREMENTS.md currently marks all six "Complete" — this verification finds **SESS-03 should be reopened** (Truth 2 gap, third time, narrower each time).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER debt-marker scan across all 12 files the fresh review touched, re-run independently this pass (per-file `grep`, not the multi-file `rg` form that mis-fires under this environment's tool wrapper) | — | ℹ️ INFO — zero matches. The 3 lowercase "placeholder" hits found are legitimate design vocabulary ("publish the placeholder slot", `manager.go:123/126`) or an unrelated error string ("kubeconfig not available") — not stub markers. |
| `pkg/session/manager.go` | 234 | Autonomous-exit guard `err != nil && sessionCtx.Err() == nil` never fires for a clean/nil pipeline exit with a healthy sessionCtx | 🛑 **Blocker** (Truth 2 gap, this round's WR-01) | `get_status` cannot detect an autonomously-dead session that exited cleanly (e.g. relay-initiated stream EOF). Independently, empirically reproduced by this verifier. |
| `pkg/session/manager_test.go` | 597-598/637-639, 656-657/663-665, 836-837/873-875 | Three tests assert "no orphaned tmpdir" via a process-global `filepath.Glob(os.TempDir(), "cpg-session-*")`, which `cmd/cpg`'s own session-lifecycle test can perturb under Go's default parallel package execution | ⚠️ **Warning** (test-infrastructure flake vector, NOT a truth/SC violation) | This is the SAME issue already root-caused, evidenced, and formally deferred in `.planning/phases/17-session-lifecycle/deferred-items.md` during 17-05 ("Cross-package os.TempDir() glob race") — the fresh review restates it with more precise line numbers and a concrete `tmpRoot`-scoping fix, but it is not a new discovery and was already correctly triaged as out-of-scope test-infra debt, not a production defect. Does not affect any of the 5 ROADMAP truths. Recommend folding the review's more complete fix sketch into `deferred-items.md`'s existing entry rather than treating it as a new gap. |
| `pkg/session/manager.go` | 385-411 | `Stop()`'s early-return branch never re-validates `m.session == s` after releasing the lock, so a concurrent `Start()`'s D-04 purge can return a summary pointing at an already-removed tmpdir | ℹ️ Info (IN-02, independently source-traced by this verifier, not merely cited from the review) | Benign staleness; requires a genuinely concurrent stop_session + start_session from separate callers, which a single sequential MCP client does not produce. Does not violate Truth 3/SESS-04's literal text. |
| `pkg/session/manager_test.go` | 225, 238-242 | `TestManager_Start_ConcurrentStartRejectsSecond`'s loser-not-queryable assertion is dead code — the loser's `Start` always returns a zero `SessionID`, so the guarded `if loserID != ""` block never executes | ℹ️ Info (IN-03, carried from fresh review, plausible on inspection — the loser branch at line 219-227 confirms `o.res.SessionID` is unset on the error path) | Test-coverage gap, not a production bug — the winner-side assertions and Shutdown/tmpdir check remain meaningful. |
| `pkg/session/manager.go` / `pipeline_config.go` | manager.go:399-400, pipeline_config.go:69-71 | `outputHash`/`healthPath` formula duplicated in two places (deliberate, per comment) | ℹ️ Info | Carried unaddressed from the original review (IN-01/IN-04 across rounds) — no functional bug, nothing enforces the two stay in sync if either formula changes independently. |
| `pkg/session/session.go` | 89, 93, 99 | Stale `//nolint:unused` directives on `cancel`/`done`/`stopOnce`, actively consumed by `manager.go` in the same package | ℹ️ Info | Carried unaddressed from two prior reviews — misleading to a reader, cosmetic only, `golangci-lint` confirmed clean with them removed. |
| `pkg/session/manager_test.go` | 293 | `assert.GreaterOrEqual(t, status.EvidenceFileCount, 0)` is tautological for an `int` | ℹ️ Info | Carried unaddressed from two prior reviews — no real coverage of evidence-file counting; a positive-count assertion is achievable with the existing `twoFlows()` fixture. |
| `pkg/session/manager.go` | 245-252 | 17-08's own `s.cancel()` safety comment asserts a happens-before ordering relative to `Start`'s deferred `stopSetupOnShutdown()` that does not actually hold (the launch goroutine can run concurrently with `Start`'s own defers on an instantly-failing pipeline) | ℹ️ Info (IN-01, carried from fresh review — the code itself is safe because `setupCancel` is idempotent; only the justifying comment's reasoning is imprecise) | Purely a comment-accuracy issue; no behavioral impact. Not independently re-derived in full depth by this verifier (correctly low-stakes per INFO-tier gate policy), but plausible on inspection of the surrounding code's goroutine-launch ordering. |
| `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` | SESS-03 row / Phase 17 progress row | Both currently marked "Complete" / "8/8 Complete" (updated by 17-08's own `requirements-completed` metadata) | ℹ️ Info (this verifier's own observation) | Now stale given Truth 2's re-reopening — flagged here for whoever authors the next gap-closure plan or roadmap update; not something this verifier edits directly. |

### Human Verification Required

None. The one blocking finding in this report is a concrete, deterministic, empirically-reproduced code-level gap (an incomplete boolean condition in a single `if` guard) with a clear, narrow fix sketch that a closure plan can implement and prove with a `-race` test, following the exact same pattern 17-05/17-06/17-08 already used successfully three times. This is a backend Go MCP server with no visual, real-time, or subjective-quality surface requiring human judgment.

### Gaps Summary

**What closed cleanly this round, independently re-confirmed (not taken from 17-08-SUMMARY.md or 17-REVIEW.md's word):**
- WR-01 (prior round, scoped `context.DeadlineExceeded`): the launch goroutine's genuine-failure guard now classifies on `sessionCtx.Err() == nil` — the ONLY true "this was cancelled on purpose" signal — instead of the returned error's identity/kind. Proven by a real `-race` test re-run standalone, plus a full independent `rg` re-derivation of every one of 17-08's source-gate acceptance criteria (all matched exactly: 1x `sessionCtx.Err() == nil`, 0x the old `errors.Is(err,` pattern, 1x the preserved unrelated `errors.Is(pfErr,` in `resolveSetup`).
- WR-02 (prior round, `already_stopped`/D-03 contract): `Session.explicitStopSeen atomic.Bool`, swapped at both of `Stop()`'s `buildSummary` call sites, decouples "was `Stop()` itself already called" from "is `State` already `Stopped`" — closing the regression where the first post-crash `stop_session` wrongly reported `already_stopped: true`. Proven the same way: standalone test re-run, exact `rg` source-gate re-derivation (2x `Swap(true)`, 0x either literal boolean).
- Full repo `go test ./... -race -count=1`: 11/11 packages green; `go vet` clean; `golangci-lint` on all 3 phase-17 packages shows exactly 11 pre-existing `errcheck` findings, every single one independently `git blame`-traced to commits predating Phase 17 (2026-04-21 through 2026-07-20, before 17-CONTEXT.md was even gathered) — zero findings in any file this phase actually modified.
- Zero debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) across all 12 files the fresh review examined, independently re-scanned this pass.

**What is newly open, found by weighing (not inheriting) the fresh `17-REVIEW.md`'s findings against this phase's must-haves:**

1. **WR-01 (this round) — BLOCKER, reopens Truth 2 / SESS-03 a third time, narrower each round:** the crash-classification guard 17-08 landed correctly handles every non-nil pipeline exit (both the general case closed in 17-05 and the scoped-DeadlineExceeded case closed in 17-08), but is unconditionally gated behind `err != nil`. A pipeline that exits cleanly (nil error) while `sessionCtx` is still healthy — the exact shape produced whenever a Hubble Relay closes its gRPC stream without an error (io.EOF, treated as harmless by `pkg/hubble/client.go`'s own design, and the exact behavior of the phase's own `closedFlowSource` test double) — is never classified at all. This verifier did not stop at re-deriving the review's source citations: it built and ran a throwaway `-race` test reproducing the exact scenario end-to-end (start a session, let it drain fully and log its summary, wait 500ms of pure slack, then poll `get_status`) and confirmed the session reports `"capturing"` forever, with a follow-up `start_session` rejected as if the dead session were still active. The test file was deleted immediately after confirming the failure; `git status` is clean. This directly undermines the phase goal's own "robustly cleaning up on every exit path" language for a plausible, reachable exit path, so it is kept as a hard gap.

2. **WR-02 (this round, cross-package tmpdir glob flake) — WARNING, but NOT a new gap:** independently confirmed the underlying mechanism is real (three tests glob a process-global `os.TempDir()` path that `cmd/cpg`'s own session test can also write to), but this is the identical issue already discovered, root-caused, and formally deferred in `deferred-items.md` during 17-05 — the fresh review adds precise line numbers and a cleaner fix (a test-scoped `tmpRoot` field) but is not surfacing new information. Not gated; recommend folding the improved fix sketch into the existing deferred-items.md entry.

3. **Six Info-tier findings** (three carried unaddressed across all rounds — IN-04/05/06 in review numbering — plus IN-01/02/03 either newly found or newly independently source-traced by this verifier) — all cosmetic or narrow-race-window issues with no bearing on any of the 5 ROADMAP truths. Listed in full in the Anti-Patterns table for visibility, not gating.

**Deferred-item check (Step 9b):** Neither the Truth 2 gap nor the tmpdir-glob flake is addressed by a later milestone phase, independently re-confirmed by re-querying Phase 18 and Phase 19's goal/success-criteria text this pass (not assumed from the prior round). Phase 18 (Query Tools, QRY-01..05) is entirely read-side and does not touch crash detection. Phase 19 (Security Hardening & E2E Validation) SC3 — an E2E integration test proving port-forward/tmpdir cleanup on ungraceful disconnect — tests the transport-kill scenario (Truth 4/SESS-05, already fully closed), not `get_status`'s state-accuracy on a clean pipeline exit. Both items remain active, actionable items for Phase 17 (the tmpdir-glob flake as already-tracked test-infra debt, not a gate item; the clean-exit classification gap as a hard blocker).

**Recommendation:** A fifth gap-closure plan (17-09 or similar), narrowly scoped, mirroring the exact TDD-with-`-race`-proof shape 17-05/17-06/17-08 already used successfully three times:
- Broaden the autonomous-exit guard from `if err != nil && sessionCtx.Err() == nil` to `if sessionCtx.Err() == nil { ... }`, keeping the `pipelineErr`/Warn-log surfacing conditional on `err != nil` inside it (Info-log or silent on a clean nil exit), with the State transition + `s.cancel()` release now unconditional within that outer guard.
- Add a regression test using a `closedFlowSource`-shaped (or equivalent nil-returning) pipeline, asserting `get_status` autonomously reports `"stopped"` with zero `stop_session` calls — this verifier's throwaway repro (deleted after this run) is a ready template.
- Optionally fold the fresh review's `tmpRoot`-scoping fix for the pre-existing tmpdir-glob flake into the same plan (or a separate test-infra-only plan), since it is now fully specified with a concrete fix.

---

_Verified: 2026-07-21T09:51:56Z_
_Verifier: Claude (gsd-verifier)_
