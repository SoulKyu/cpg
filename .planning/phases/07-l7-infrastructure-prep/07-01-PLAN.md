---
phase: 07-l7-infrastructure-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/policy/merge.go
  - pkg/policy/merge_test.go
  - pkg/policy/builder.go
  - pkg/policy/builder_test.go
  - pkg/policy/dedup.go
  - pkg/policy/dedup_test.go
  - pkg/policy/attribution.go
  - pkg/policy/attribution_test.go
autonomous: true
requirements:
  - EVID2-02
  - EVID2-03
  - EVID2-04
must_haves:
  truths:
    - "mergePortRules preserves the Rules field of every PortRule across merges (no data loss)"
    - "A regression test for the Rules-drop bug lands in a FAILING commit BEFORE the fix lands in a separate commit (TDD, auditable in git history)"
    - "normalizeRule deterministically sorts Rules.HTTP by (Method, Path) and Rules.DNS by MatchName so YAML output is byte-stable across runs"
    - "RuleKey carries an optional L7 discriminator (HTTP method+path or DNS matchName); L4-only rules produce IDENTICAL key strings to v1.1 (omitempty)"
    - "All v1.1 fixtures continue to pass: existing builder/merge/dedup/attribution tests are green with no expectation changes"
    - "go test ./pkg/policy/... is green"
  artifacts:
    - path: "pkg/policy/merge.go"
      provides: "mergePortRules preserves Rules across merges"
      contains: "Rules"
    - path: "pkg/policy/merge_test.go"
      provides: "TestMergePortRules_PreservesRules regression test for the dropped-Rules bug"
      contains: "TestMergePortRules_PreservesRules"
    - path: "pkg/policy/builder.go"
      provides: "normalizeRule sorts Rules.HTTP and Rules.DNS deterministically"
      contains: "normalizeRule"
    - path: "pkg/policy/attribution.go"
      provides: "RuleKey with optional L7 discriminator"
      contains: "L7"
  key_links:
    - from: "pkg/policy/merge.go::mergePortRules"
      to: "api.PortRule.Rules"
      via: "field assignment in merged PortRule"
      pattern: "Rules"
    - from: "pkg/policy/builder.go::normalizeRule"
      to: "Rules.HTTP / Rules.DNS sort helpers"
      via: "sort.SliceStable on each PortRule.Rules"
      pattern: "sort\\."
    - from: "pkg/policy/attribution.go::RuleKey.String"
      to: "L7 discriminator string"
      via: "appended only when L7 != nil"
      pattern: "omitempty"
---

<objective>
Land the three correctness changes in pkg/policy that Phase 8 (HTTP) and Phase 9 (DNS) depend on, with ZERO user-visible behavior change for v1.1 inputs:

1. Fix the latent `mergePortRules` bug that silently drops `PortRule.Rules` across merges (EVID2-03). Today it's harmless because no code populates `Rules`; the moment Phase 8 ships HTTP rule attachment, this becomes silent L7 data loss.
2. Extend `normalizeRule` to deterministically sort L7 sub-lists so future L7 YAML output is byte-stable across runs (EVID2-04).
3. Extend `RuleKey` with an optional L7 discriminator so two rules differing only by HTTP method/path do not dedup into the same evidence bucket (EVID2-02). L4-only rules MUST produce identical `RuleKey.String()` to v1.1.

Purpose: Foundation for L7 codegen. All three are pkg/policy-internal changes; no schema/CLI/pipeline surface affected. v1.1 fixtures pass byte-identically.
Output: Modified merge.go, builder.go, attribution.go + new TDD-ordered test commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-l7-infrastructure-prep/07-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

@pkg/policy/builder.go
@pkg/policy/merge.go
@pkg/policy/attribution.go

<interfaces>
<!-- Key Cilium API types this plan touches. Already in vendored cilium/cilium v1.19.1. -->
<!-- Executor uses these directly — no exploration needed. -->

From github.com/cilium/cilium/pkg/policy/api (verified in research/STACK.md):
```go
// PortRule already has a Rules field; today's mergePortRules drops it.
type PortRule struct {
    Ports            []PortProtocol
    TerminatingTLS   *TLSContext
    OriginatingTLS   *TLSContext
    ServerNames      []string
    Listener         *Listener
    Rules            *L7Rules   // <-- preserved-or-dropped is the bug
}
type L7Rules struct {
    HTTP    PortRulesHTTP   // []PortRuleHTTP
    DNS     PortRulesDNS    // []PortRuleDNS  (PortRuleDNS is a type alias of FQDNSelector)
    Kafka   []kafka.PortRule
    L7Proto string
    L7      PortRulesL7
}
type PortRuleHTTP struct {
    Path    string
    Method  string
    Host    string
    Headers []string
    HeaderMatches []*HeaderMatch
}
// PortRuleDNS is a type alias of FQDNSelector — fields: MatchName, MatchPattern.
```

From pkg/policy/attribution.go (current state — TO EXTEND):
```go
type RuleKey struct {
    Direction string
    Peer      Peer
    Port      string
    Protocol  string
    // ADD: optional L7 discriminator with omitempty semantics
}
func (k RuleKey) String() string  // currently: "%s:%s:%s:%s" — must extend conditionally
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD-first — failing regression test for mergePortRules Rules drop (EVID2-03)</name>
  <files>pkg/policy/merge_test.go</files>
  <behavior>
    Add a NEW test `TestMergePortRules_PreservesRules` (table-driven, descriptive `name` per the project convention) asserting:
    - Given two `api.PortRules` slices, where at least one entry has a non-nil `Rules` field with a populated `HTTP` list (e.g. `[{Method: "GET", Path: "/api"}]`),
      `mergePortRules(existing, incoming)` returns a result whose merged PortRule on the matching (port, protocol) key has `Rules` non-nil with the HTTP list preserved.
    - Sub-cases:
      a) existing has Rules, incoming has none on same port → result preserves existing Rules.
      b) incoming has Rules, existing has none on same port → result picks up incoming Rules.
      c) both have Rules on the same port — HTTP lists union-merge by (Method, Path) (no duplicates, both observations preserved).
      d) different ports, each with Rules → both PortRules preserved with their Rules intact.
    - L4-only path (no Rules anywhere) MUST keep the v1.1 output unchanged: assert byte-equivalence vs current expected ports order on an existing fixture.

    This test MUST FAIL on master (current `mergePortRules` flattens into `result[0].Ports` and discards `Rules` entirely).
  </behavior>
  <action>
    Per D-EVID2-03 (TDD-first invariant from CONTEXT.md): commit ONLY this failing test first. Do NOT touch merge.go in this commit.

    Implementation steps:
    1. Open pkg/policy/merge_test.go (create if it doesn't exist; otherwise append).
    2. Add `TestMergePortRules_PreservesRules` with the four sub-cases above. Use `api.PortRule`, `api.PortRules`, `api.L7Rules`, `api.PortRuleHTTP` from `github.com/cilium/cilium/pkg/policy/api` directly.
    3. Run `go test ./pkg/policy/ -run TestMergePortRules_PreservesRules` and CONFIRM it fails (the assertion on `Rules != nil` after merge will fail because current code returns a single flattened PortRule with no Rules).
    4. Commit with message: `test(07-01): add failing regression test for mergePortRules Rules-drop bug (EVID2-03)` — note this is the audit trail required by CONTEXT specifics: "test files land in failing commits before implementation".
    5. Do NOT proceed to Task 2 until this commit is on the branch and the test demonstrably fails.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg && go test ./pkg/policy/ -run TestMergePortRules_PreservesRules 2>&1 | grep -E "(FAIL|PASS)" </automated>
  </verify>
  <done>Test file exists, the four sub-cases are present, the test FAILS on the current `mergePortRules` implementation, and the failing-test commit is on the branch with the conventional commit subject.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix mergePortRules to preserve Rules + extend normalizeRule for L7 sort + extend RuleKey with L7 discriminator</name>
  <files>pkg/policy/merge.go, pkg/policy/builder.go, pkg/policy/dedup.go, pkg/policy/attribution.go, pkg/policy/attribution_test.go, pkg/policy/dedup_test.go</files>
  <behavior>
    After this task:
    - `TestMergePortRules_PreservesRules` (Task 1) PASSES.
    - All existing v1.1 tests in pkg/policy pass UNCHANGED — no expectation edits to existing tests are permitted (this is the byte-stability invariant).
    - New `TestNormalizeRule_SortsL7Lists` asserts: given a PortRule with `Rules.HTTP` in `[POST /b, GET /a]` order, after `normalizeRule` the order is `[GET /a, POST /b]` (lex by Method then Path). Given DNS with `[{MatchName:"b.example.com"}, {MatchName:"a.example.com"}]` → sorted to `[a..., b...]`. Empty/nil `Rules` is a no-op.
    - New `TestRuleKey_L7Discriminator` asserts: a `RuleKey` with no L7 discriminator stringifies IDENTICALLY to the v1.1 form (`direction:peer:proto:port`); a key with L7 = `&L7Discriminator{Protocol:"http", HTTPMethod:"GET", HTTPPath:"/api"}` stringifies to a SUPERSET that includes those values; two keys differing only by HTTPPath stringify to different strings.
  </behavior>
  <action>
    Per CONTEXT.md decisions: mergePortRules-preservation lands BEFORE schema v2 (separate plan 07-02), so this task is pkg/policy-only.

    1. **Fix `pkg/policy/merge.go::mergePortRules`** (per D-EVID2-03):
       - Replace the current flatten-into-result[0] implementation with a per-port-protocol grouping that preserves Rules:
         - Build a map keyed by `port + "/" + protocol` mapping to a merged `*api.PortRule`.
         - For each incoming `PortProtocol`, merge into the existing entry for that key.
         - When both existing and incoming entries on the same key have `Rules` non-nil, union-merge:
           - `Rules.HTTP`: dedup by `(Method, Path, Host, Headers, HeaderMatches)` (use a deterministic string key — research/PITFALLS recommends `Method|Path|sortedHeaders`); preserve order of first observation, append new.
           - `Rules.DNS`: dedup by `(MatchName, MatchPattern)`; same merge semantics.
           - L4-only entries (Rules == nil on both) keep v1.1 behavior — produce a single PortRule with merged Ports list, Rules == nil.
         - Mixing HTTP+DNS on the same port-protocol within a single merged PortRule is structurally illegal in Cilium (`L7Rules` is a oneof-style union per research/STACK.md, Pitfall 6). For Phase 7, do NOT special-case this — Phase 8/9 will add an explicit refusal-with-warn. A code comment naming this future work is sufficient.
       - Critical: the L4-only fast path MUST produce byte-identical YAML to v1.1. If the existing code's "flatten into result[0]" was producing a particular ordering of Ports, preserve that ordering exactly. Run all v1.1 fixtures (existing builder/dedup tests) and confirm byte equality.

    2. **Extend `pkg/policy/builder.go::normalizeRule`** (per D-EVID2-04):
       - Locate `normalizeRule` (search `func normalizeRule`). After the existing port sort, for each PortRule with non-nil `Rules`:
         - If `Rules.HTTP` non-empty: `sort.SliceStable` by `(Method, Path)` lexicographic.
         - If `Rules.DNS` non-empty: `sort.SliceStable` by `MatchName` lexicographic (MatchPattern stays untouched in v1.2 — `matchPattern` glob is deferred per DNS-03; only MatchName literals are emitted, so MatchName is the sole sort key needed in this phase).
       - Empty/nil Rules: no-op.
       - Preserve nil-vs-empty-list distinction (Pitfall 12): `Rules == nil` stays nil; `Rules.HTTP == []` (empty list, not nil) stays an empty list. Use only `len() > 1` as the guard for sorting; do NOT replace nil with empty.

    3. **Extend `pkg/policy/attribution.go::RuleKey`** (per D-EVID2-02):
       - Add an optional pointer field: `L7 *L7Discriminator` (or whatever name fits — Claude's discretion). Document the omitempty contract: when nil, `String()` produces the v1.1 format byte-for-byte.
       - Add the `L7Discriminator` struct: fields `Protocol string` ("http" | "dns"), `HTTPMethod string` (omitempty), `HTTPPath string` (omitempty), `DNSMatchName string` (omitempty).
       - Modify `RuleKey.String()`: append `":l7=" + l7Encode(k.L7)` ONLY when `k.L7 != nil`. Otherwise return the current 4-segment form.
       - This means: today, all callers (builder.go) construct RuleKey with `L7: nil`, so Strings stay identical. Phase 8 wires L7 population.

    4. **Add tests:**
       - `pkg/policy/merge_test.go`: TestMergePortRules_PreservesRules now passes. Also add `TestMergePortRules_L4OnlyByteIdentical` asserting the existing L4-only merge path output is unchanged from v1.1 (use a fixture with two PortRules merged into one and confirm exact slice ordering).
       - `pkg/policy/dedup_test.go` (or builder_test.go if normalizeRule is private and tests live there): `TestNormalizeRule_SortsL7Lists` covering shuffled HTTP / DNS lists; round-trip equivalence test (two PortRules differing only in L7 order are equivalent under PoliciesEquivalent).
       - `pkg/policy/attribution_test.go`: `TestRuleKey_L7Discriminator` covering nil-discriminator (v1.1 format), populated discriminator (string contains method+path), and inequality between two keys with different paths.

    5. **Verify v1.1 byte stability:**
       - Run `go test ./...` — every existing test passes with no expectation changes.
       - In particular, any builder snapshot/fixture tests in pkg/policy and pkg/output must show no diff.

    6. Commit message: `fix(policy): preserve Rules in mergePortRules + sort L7 lists in normalizeRule + L7 discriminator on RuleKey (EVID2-02, EVID2-03, EVID2-04)`. Note the explicit requirement IDs in the commit body for git-grep traceability per CONTEXT specifics.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/cpg && go test ./pkg/policy/... -count=1 && go test ./... -count=1</automated>
  </verify>
  <done>
    All pkg/policy tests pass including the three new tests. The full repo test suite (`go test ./...`) is green with no expectation edits to pre-existing tests. `git log --oneline` shows TWO commits for plan 07-01: the failing-test commit (Task 1) followed by the fix commit (Task 2), in that order.
  </done>
</task>

</tasks>

<verification>
- Failing-test commit lands BEFORE the fix commit (audit `git log --oneline` for the test commit subject preceding the fix commit subject).
- `go test ./pkg/policy/... -count=1 -v` shows TestMergePortRules_PreservesRules, TestNormalizeRule_SortsL7Lists, TestRuleKey_L7Discriminator all green.
- `go test ./... -count=1` is green across the repo (no expectation edits).
- Manual sanity: `git diff master -- pkg/policy/builder.go pkg/policy/merge.go pkg/policy/attribution.go` shows targeted edits only — no churn in unrelated functions.
- v1.1 byte-stability check (manual or scripted): regenerate one of the existing test fixtures via `go test -run TestBuildPolicy_Snapshot -update` (if such a flag exists) — no diff. If no snapshot infra, confirm via the existing fixture-based assertions in builder_test.go passing without modification.
</verification>

<success_criteria>
- mergePortRules preserves PortRule.Rules across merges, deduplicating L7 sub-lists by canonical key.
- normalizeRule sorts Rules.HTTP by (Method, Path) and Rules.DNS by MatchName, deterministically.
- RuleKey carries an optional L7 discriminator; nil-L7 keys stringify identically to v1.1.
- Two-commit history on the branch demonstrating TDD: failing test → fix.
- Zero behavior change for v1.1 inputs (all existing tests green, no expectation edits).
</success_criteria>

<output>
After completion, create `.planning/phases/07-l7-infrastructure-prep/07-01-SUMMARY.md` documenting:
- Files modified and key code locations (file:line references for mergePortRules, normalizeRule, RuleKey).
- The two commits (test commit SHA + fix commit SHA).
- Confirmation that v1.1 fixtures pass unchanged.
- Note that no schema/CLI/pipeline surface was affected — this is pure pkg/policy internal correctness work.
</output>
