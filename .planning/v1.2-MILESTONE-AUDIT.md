---
status: passed
audit: cross-phase integration v1.2
date: 2026-04-24
phases: [07-l7-infrastructure-prep, 08-http-l7-generation, 09-dns-l7-generation]
requirements_total: 22
requirements_wired: 22
test_suite:
  total: 319
  packages: 9
  failures: 0
---

# v1.2 Milestone Cross-Phase Integration Audit

**Result:** PASSED. All 9 cross-phase E2E flows are connected end-to-end. Every v1.2 requirement (22/22) is wired across the phases that produce + consume it. No orphaned exports, no dangling APIs, no broken flows. `go test ./...` green: 319 tests, 9 packages.

## Wiring Summary

- Connected exports: 100% — every Phase 7 export (`L7Discriminator`, `RuleKey.L7`, `extractHTTPRules` consumed in P8, `extractDNSQuery` added in P9, `ensureKubeDNSCompanion` consumed in P9, `evidence.L7Ref` consumed in both P8 and P9, `pkg/k8s.RunL7Preflight` consumed by `cmd/cpg/generate.go`, `PipelineConfig.L7Enabled` consumed by `hubble.RunPipelineWithSource` → `aggregator.SetL7Enabled` → `policy.AttributionOptions.L7Enabled`).
- Orphaned exports: 0.
- Missing connections: 0.
- API coverage: every consumer path verified via direct `grep`+`Read` of source.

## E2E Flow Verification

| # | Flow | Status | Evidence |
|---|------|--------|----------|
| 1 | HTTP: `cpg generate/replay --l7` → Hubble Flow.L7.Http → aggregator counter → BuildPolicy → toPorts.rules.http YAML → evidence L7Ref{Protocol:http} → cpg explain HTTP render | COMPLETE | commonflags.go:25,48,65 → generate.go:239 / replay.go:113 → pipeline.go:112 (`agg.SetL7Enabled`) → aggregator.go:222-224 (`L7Enabled: a.l7Enabled` into BuildPolicy) → builder.go:451-470 (recordL7 HTTP branch via extractHTTPRules at l7.go:31-48) → builder.go:640-661 (portRulesFor emits Rules.HTTP) → evidence_writer.go:94-100 (HTTP L7Ref) → explain_render.go:67-69 (HTTP render). E2E: TestReplay_L7HTTPGeneration (replay_test.go:188) |
| 2 | DNS: `--l7` → Flow.L7.Dns → aggregator DNS counter → BuildPolicy → toFQDNs+dns + companion DNS-53 rule → evidence L7Ref{Protocol:dns} → cpg explain DNS render | COMPLETE | aggregator.go:150-152 (DNS counter) → builder.go:476-485 (recordL7 DNS branch via extractDNSQuery at l7.go:63-78, egress-only) → builder.go:548-607 (fqdnEgressRulesFrom emits ToFQDNs+rules.dns) → builder.go:135 (`ensureKubeDNSCompanion(cnp)` post-process at companion_dns.go:50-81) → evidence_writer.go:101-105 (DNS L7Ref) → explain_render.go:70-72 (DNS render). E2E: TestReplay_L7DNSGeneration (replay_test.go:356), TestPipeline_L7DNS_GenerationAndEvidence |
| 3 | VIS-01: `--l7` set + zero L7 records → exactly-once warning with workloads + #l7-prerequisites hint that resolves to README authoritative content | COMPLETE | pipeline.go:199-205 (gate `cfg.L7Enabled && stats.FlowsSeen > 0 && agg.L7HTTPCount()+agg.L7DNSCount() == 0` — DNS half wired in P9 plan 02 via aggregator.go:150-152) → README.md:234 anchor (filled by P9 plan 04 with two-step workflow + starter CNP). E2E: TestReplay_L7HTTP_EmptyFixtureFiresWarning (replay_test.go:300), TestPipeline_L7Empty_FiresWarning |
| 4 | VIS-04/05/06: `cpg generate --l7` invokes `RunL7Preflight` BEFORE pipeline; `--no-l7-preflight` skips; replay never invokes preflight | COMPLETE | generate.go:211 invokes `maybeRunL7Preflight(ctx, kubeConfig, f.l7, f.noL7Preflight, logger)` BEFORE `hubble.RunPipeline` at L213; gate at generate.go:39 `if !l7Enabled || noPreflight { return }`; calls `k8s.RunL7Preflight` at generate.go:55. replay.go: no maybeRunL7Preflight callsite; L107 declares `--no-l7-preflight` only on generate; replay rejects the flag (asserted by TestReplayCmd_RejectsNoL7PreflightFlag). |
| 5 | Evidence schema v2: writer emits schema_version=2; reader rejects ≠2 with literal `$XDG_CACHE_HOME/cpg/evidence/`; L7Ref propagates pipeline → file → explain | COMPLETE | schema.go:15 `SchemaVersion = 2`; reader.go:36-42 rejection contains literal `rm -rf $XDG_CACHE_HOME/cpg/evidence/`; L7Ref struct at schema.go:70-75 with omitempty; populated by evidence_writer.go:94-105 in both HTTP and DNS branches; consumed by explain_filter.go:64-83 (filtering) and explain_render.go:66-73 (rendering). JSON/YAML pass-through preserved by omitempty tags. |
| 6 | Byte-stability: `--l7=false` against any v1.1 fixture produces byte-identical output to v1.0/v1.1 baseline | COMPLETE | TestReplay_L7FlagByteStable (replay_test.go:39, Phase 7 invariant) still green after P8+P9. portRulesFor (builder.go:640-661) collapses to v1.1 shape `api.PortRules{{Ports: ports}}` when no L7 rules attached. `recordL7` short-circuits at builder.go:452-454 (`if !opts.L7Enabled return false`). Reinforced by TestReplay_L7HTTP_DisabledByteStable (replay_test.go:258) and TestReplay_L7DNSDisabled_FallbackByteStable (replay_test.go:435) for DNS-04. |
| 7 | Companion rule invariant: every CNP with toFQDNs has the kube-dns companion | COMPLETE | builder.go:135 `ensureKubeDNSCompanion(cnp)` invoked unconditionally in BuildPolicy (idempotent, no-op without ToFQDNs). companion_dns.go:50-81 walks ToFQDNs and appends the companion. Cross-package shared assertion `pkg/policy/testdata.AssertHasKubeDNSCompanion` reused in pkg/policy_test, pkg/hubble integration test (pipeline_l7_dns_test.go), and cmd/cpg replay test (replay_test.go:356). Companion uses literal `matchName` only — no `matchPattern` (DNS-03 holds). |
| 8 | `cpg explain` filter+render: --http-method/--http-path/--dns-pattern AND-combine; L7 rendering present in text/JSON/YAML; L4-only behavior unchanged | COMPLETE | explain.go:42-44 declares 3 long-only flags; buildFilter (explain.go:153-160) normalizes (uppercase method, strip trailing dot from dns-pattern); explain_filter.go:64-83 implements AND-combine with per-protocol gating + drops L4-only rules when any L7 filter set; explain_render.go:66-73 emits text L7 line; JSON/YAML pass through via omitempty L7Ref tags (sigs.k8s.io/yaml honors json tags). |
| 9 | README wiring: anchor `#l7-prerequisites` exists exactly once and is populated; starter CNP valid + applicable; `--l7` mentioned where relevant | COMPLETE | `grep -c 'id="l7-prerequisites"' README.md` = 1 (line 234). Authoritative content lines 234-370 (two-step workflow + 3 visibility methods + capture-window guidance + v1.2 limitations). Starter CNP `cpg-l7-visibility-bootstrap` at README.md:307. `--l7` mentioned at L80 (Quick start), L100 (replay), L160 (What it generates), L265-267 (re-run step), L295 (preflight); `--http-method`/`--http-path`/`--dns-pattern` at L446-454. |

## Requirements Integration Map (22/22)

| Requirement | Owning Phase | Integration Path | Wiring Status | Notes |
|-------------|--------------|------------------|---------------|-------|
| EVID2-01 | P7 | schema.go:15 → reader.go:36-42 → consumed by writer.go and cmd/cpg/explain | WIRED | Reader rejection literal verified |
| EVID2-02 | P7 | attribution.go:38-67 (RuleKey.L7) → builder.go:466,482 (populates Protocol/Method/Path/DNSMatchName) → evidence_writer.go:93-106 (Key.L7 → on-disk L7Ref) | WIRED | HTTP populated P8, DNS populated P9 |
| EVID2-03 | P7 | merge.go:172-271 (mergePortRules preserves Rules) | WIRED | Self-contained, regression test in merge_l7_test.go |
| EVID2-04 | P7 | dedup.go:81-98 sortL7Rules invoked from normalizeRule | WIRED | Self-contained byte-stability primitive |
| VIS-04 | P7 | k8s/preflight.go getCiliumConfig → cmd/cpg/generate.go:211 maybeRunL7Preflight | WIRED | E2E only on `cpg generate` |
| VIS-05 | P7 | k8s/preflight.go checkCiliumEnvoy with cfg fallback → cmd/cpg/generate.go:211 | WIRED | RBAC-403 graceful via apierrors.IsForbidden |
| VIS-06 | P7 | generate.go:107 declares `--no-l7-preflight` → generate.go:39 gate; replay rejects (cobra unknown-flag) | WIRED | TestReplayCmd_RejectsNoL7PreflightFlag |
| L7CLI-01 | P7 | commonflags.go:25,48,65 → generate.go:239 / replay.go:113 → pipeline.go:49 → aggregator.go:82 SetL7Enabled → AttributionOptions.L7Enabled | WIRED | TestReplay_L7FlagByteStable still green |
| HTTP-01 | P8 | l7.go:31-48 extractHTTPRules → builder.go:451-470 recordL7 → builder.go:640-661 portRulesFor (Rules.HTTP) → CNP YAML | WIRED | TestReplay_L7HTTPGeneration |
| HTTP-02 | P8 | l7.go:84-86 normalizeHTTPMethod → builder.go via extractHTTPRules → CNP method=GET regardless of input casing | WIRED | E2E lowercase fixture line 4 emits GET |
| HTTP-03 | P8 | l7.go:93-99 anchorPath (regexp.QuoteMeta + ^$) → builder.go via extractHTTPRules | WIRED | Property test + E2E |
| HTTP-04 | P8 | builder.go:251-268 addHTTPRules dedup-by-httpRuleKey → portRulesFor merges into single PortRule per (port,proto) | WIRED | TestBuildPolicy_L7Enabled_MultiHTTPRule_SamePort_SinglePortRule |
| HTTP-05 | P8 | l7.go:43-47 PortRuleHTTP{Method,Path} only — Headers/Host/HeaderMatches deliberately zero | WIRED | Adversarial test in l7_test.go + E2E lint |
| VIS-01 | P8 | aggregator counters (HTTP at 147-149, DNS at 150-152) → pipeline.go:199-205 single-emission gate | WIRED | DNS half wired by P9 plan 02 — gate truthful |
| DNS-01 | P9 | l7.go:63-78 extractDNSQuery → builder.go:476-485 recordL7 DNS egress branch → fqdnEgressRulesFrom (builder.go:563-607) emits ToFQDNs.matchName + ToPorts.rules.dns.matchName | WIRED | TestReplay_L7DNSGeneration |
| DNS-02 | P9 | builder.go:135 BuildPolicy unconditionally calls ensureKubeDNSCompanion (companion_dns.go:50-81); idempotent via hasKubeDNSCompanion | WIRED | testdata.AssertHasKubeDNSCompanion shared across 3 test packages |
| DNS-03 | P9 | recordL7 DNS branch + companion both emit MatchName only; `rg MatchPattern pkg/policy/{l7,builder,companion_dns}.go` returns only one comment | WIRED | E2E asserts no `matchPattern:` substring in YAML |
| DNS-04 | P9 | recordL7 gates DNS branch on opts.L7Enabled at builder.go:452 (top of recordL7) — no DNS-bearing flow alters output when L7 disabled | WIRED | TestReplay_L7DNSDisabled_FallbackByteStable |
| L7CLI-02 | P9 | explain.go:42-44 declares flags → explain.go:153-160 normalizes → explain_filter.go:64-83 AND-combine match | WIRED | Per-protocol gating prevents cross-protocol matches |
| L7CLI-03 | P9 | explain_render.go:66-73 text branch; JSON/YAML pass-through via L7Ref omitempty tags | WIRED | Contract tests in explain_test.go |
| VIS-02 | P9 | README.md:234-370 two-step workflow content | WIRED | Anchor referenced by VIS-01 hint string |
| VIS-03 | P9 | README.md:307 cpg-l7-visibility-bootstrap starter CNP | WIRED | YAML validated via yaml.safe_load_all in 09-04 self-check |

## Cross-Phase Touchpoints (Phase boundaries that integrate)

- **P7 → P8 → P9 (RuleKey.L7 chain):** P7 reserves the optional discriminator field (attribution.go:38-43). P8 populates it for HTTP (builder.go:466). P9 populates it for DNS (builder.go:482). Evidence_writer.go:93-106 reads it in both HTTP/DNS branches; the DNS case body replaces the P8 `TODO(phase-9)` sentinel (P9 plan 02). No back-reference lag.
- **P7 → P8 (mergePortRules.Rules preservation):** P7's EVID2-03 fix is required for HTTP-04 multi-(method,path) merge to survive cross-flow merging in P8. mergeL7Rules at merge.go:279-327 reused for L7 union.
- **P7 → P8 → P9 (--l7 flag plumbing):** P7 plumbs to PipelineConfig.L7Enabled as no-op. P8 wires it through aggregator.SetL7Enabled → AttributionOptions.L7Enabled. P9 reuses the same path (DNS branch in recordL7 gates on the same flag).
- **P8 → P9 (VIS-01 gate degrade-graceful):** P8's gate sums HTTP+DNS counters; P8 leaves DNS at 0; P9 plan 02 wires the DNS increment site so the gate becomes truthful for DNS-only sessions without a re-architect.
- **P8 → P9 (#l7-prerequisites anchor):** P8 reserves the placeholder anchor (README.md:178 → renumbered to 234 after P9 content fill). P9 fills authoritative content. VIS-01 hint string `pkg/hubble/pipeline.go:203` is constant across both phases — anchor never broke.
- **P9 internal (companion enforcement):** ensureKubeDNSCompanion is invoked once in BuildPolicy regardless of L7Enabled — idempotent + no-op without ToFQDNs. Single global enforcement point keeps DNS-02 invariant from leaking into pipeline-layer or CLI-layer concerns.

## Behavioral Spot-Checks

| Behavior | Method | Result |
|----------|--------|--------|
| `go test ./...` green | full suite | 319 passed across 9 packages, 0 failures |
| README anchor exact-once | `grep -c 'id="l7-prerequisites"' README.md` | 1 |
| `--l7` referenced in README | grep | found at L80, L100, L160, L265, L267, L295 |
| `RunL7Preflight` invoked from generate only | grep | generate.go:211 only; replay.go has explicit comment L111-112 confirming non-invocation |
| Evidence reader XDG path literal | reader.go:36-42 | `$XDG_CACHE_HOME/cpg/evidence/` present |
| Phase 8 TODO removed | `rg 'TODO\(phase-9\)' pkg/hubble/` | 0 matches |
| DNS-03 invariant globally | `rg MatchPattern pkg/policy/{l7,builder,companion_dns}.go` | only one doc-comment match |

## Wiring Gaps

None.

## Broken Flows

None.

## Orphaned Exports

None.

## Unprotected Routes / Missing Auth Checks

N/A — CLI tool, no HTTP server surface in cpg itself.

## Tech Debt Surfaced (non-blocking)

None new from cross-phase wiring. The Phase 7 verification report flagged a documentation lag in REQUIREMENTS.md traceability table (lines 85-87, 97-101 marked some satisfied requirements as `Planned`); per the file we read here, those entries have since been updated to `Complete`. No code-level debt.

## Conclusion

v1.2 milestone is integration-complete:
- All 22 v1.2 requirements have a verified end-to-end wiring path from CLI flag/input through pipeline → policy builder → on-disk artifacts (CNP YAML + evidence v2 JSON) → consumer surface (`cpg explain`).
- All 9 audit-targeted cross-phase E2E flows complete without breaks.
- v1.0/v1.1 byte-stability invariant preserved (TestReplay_L7FlagByteStable green).
- `go test ./...` green: 319 tests across 9 packages.
- Status: **passed**.

---
*Audited: 2026-04-24*
*Auditor: Claude (gsd-integration-checker)*
